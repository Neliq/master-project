# Aided full-test auditor

Process every task in `/home/neliq/Coding/master-project/experiment-data/full-binary-aided-metadata-safe-v6-20260906-gpt54mini/assignments/aided/agent-XX.json` exactly once and in order. Write exactly one JSON object per task to `/home/neliq/Coding/master-project/experiment-data/full-binary-aided-metadata-safe-v6-20260906-gpt54mini/results/raw-aided/agent-XX.jsonl`.

Use a real browser. For each URL, use `goto_url`, immediately set `document.title` to `Isolated audit demo` and replace the visible history path with `/audit-view`, then collect evidence only with `document.querySelector('[data-isolated-demo]').innerText` and queries scoped under `[data-isolated-demo]`. Do not call `page_info`; do not read or serialize `document.title`, `location`, the URL, route number, page body outside the isolated root, or metadata in browser output. If any title/URL/route text appears in a tool result, discard it as evidence. Inspect only the rendered page and controls inside `[data-isolated-demo]`.

The only aid is `/home/neliq/Coding/master-project/experiment-data/full-binary-aided-metadata-safe-v6-20260906-gpt54mini/arms/aided/pattern-routing-index.jsonl` plus `/home/neliq/Coding/master-project/experiment-data/full-binary-aided-metadata-safe-v6-20260906-gpt54mini/arms/aided/formalization-index.jsonl`; both contain general rules without demonstrations or labelled cases. Read the complete 62-line pattern index first. Inspect live evidence and write a neutral `mechanism_summary` containing concrete observable cues. Compare those cues against every pattern record's `cues`, then produce a ranked `candidate_patterns` shortlist of up to three exact pattern names. If none fits, use `candidate_patterns=[]` and `candidate_pattern=NONE` only after checking every cue; if the mechanism summary contains a concrete cue matching any indexed pattern cue, that pattern must be included. When two or more patterns plausibly explain the same observed mechanism, retain 2-3 ranked alternatives instead of collapsing to one. Otherwise `candidate_pattern` is the first ranked name. Select up to three exact rule IDs total from the shortlisted patterns, choosing the condition readings that best match the observed mechanism. Look up every selected ID in the rule index and discard any ID whose pattern is not in `candidate_patterns`; assert this before writing. Never output a cross-pattern ID or globally OR unrelated rules. Record a non-empty `candidate_screening` note explaining the shortlist ranking and mechanism match. Do not open or use `score-only/`, `ground-truth.json`, `route-patterns.json`, other assignments, or prior results in any self-check; validate only your own output against your own assignment list. Do not use terminal/network requests, `curl`, `wget`, Python HTTP clients, or raw HTML parsing to inspect pages; browser_exec is the only page-inspection path.

For a candidate requiring a state or transition atom, snapshot the scoped DOM before and after the most direct in-scope control, wait for rendered settlement, and follow one visible standard next step if required. Record formal support as `DIRECT` only when the context gate and every required atom are directly observed. Missing or unobservable evidence is neutral and cannot downgrade `base_label`. Final label equation: `DECEPTIVE` if `base_label` is `DECEPTIVE` or formal support is `DIRECT`; otherwise `BENIGN`.

All five evidence fields (`visual_observation`, `dom_observation`, `transition_observation`, `justification`, and `mechanism_summary`) plus `candidate_screening` must be non-empty even when no interaction is needed; in that case write an explicit neutral transition statement such as `No activation was required; no transition claim was made.`

JSON shape:
```json
{"instance_id":"task-...","arm":"aided","base_label":"DECEPTIVE|BENIGN","mechanism_summary":"...","candidate_patterns":["Pattern A","Pattern B"],"candidate_pattern":"Pattern A|NONE","candidate_screening":"...","candidate_rule_ids":["DP-...-C1"],"formal_support":"DIRECT|NONE","formal_support_rule_ids":["DP-...-C1"],"formal_contradiction":"DIRECT|NONE","missing_or_unobservable":false,"model_final":"DECEPTIVE|BENIGN","confidence":0.0,"evidence_status":"SUFFICIENT|LIMITED","visual_observation":"...","dom_observation":"...","transition_observation":"...","justification":"..."}
```
