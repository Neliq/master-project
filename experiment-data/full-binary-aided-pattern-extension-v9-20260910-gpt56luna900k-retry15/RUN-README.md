# full-binary-aided-pattern-extension-v9-20260910-gpt56luna900k-retry15

Unique additive retry of `full-binary-aided-pattern-extension-v8-20260910-gpt56luna900k` for exactly Nagging (63) and Games For Other Purposes (64).

- The v8 directory and all prior raw outputs are preserved and read-only.
- Assignment order, opaque instance IDs, score-only records, route index, and aided-rule index are copied unchanged from v8.
- All 24 raw rows in this directory are to be produced by fresh live Playwright browser inspections; no old judgment rows are copied.
- No aggregate metrics are emitted until the strict validator passes.

Setup command:

    python3 /home/neliq/.hermes/kanban/workspaces/t_92cc044e/build_retry.py

Capture method:

    Playwright browser navigation to every assigned URL; inspect only the rendered [data-isolated-demo] root, record visible text/controls/layout, attempt one direct in-scope transition when available, and write raw JSONL in assignment order.

Validation command after all 24 rows are present:

    FULL_BINARY_OUT=/home/neliq/Coding/master-project/experiment-data/full-binary-aided-pattern-extension-v9-20260910-gpt56luna900k-retry15 REQUIRE_AID_SCREENING=1 python3 /home/neliq/Coding/master-project/experiment-data/tools/aggregate_full_binary_aided_test.py

The generic validator expects the 372-instance full run; a focused extension validator must first confirm the 12-instance design and raw schema before any aggregate is retained.
