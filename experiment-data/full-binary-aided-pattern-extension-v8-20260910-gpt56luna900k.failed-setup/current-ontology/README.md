# Two-pattern extension ontology

This additive bundle contains only the two newly added application patterns: Nagging (63) and Games For Other Purposes (64).
The preserved `base-ontology-62.json` is copied from the prior v7 run and is not modified. Pattern-specific semantic cards are copied from the active thesis ontology.
The extension does not overwrite, recompute, or relabel any original-run observation.
