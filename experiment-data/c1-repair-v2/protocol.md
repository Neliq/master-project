# C1 repair loop v2 — formalization revision v3

This remains a C1-only development diagnostic on the same 20 outcome-selected regressions used by repair v1. C0 is fixed and is not rerun.

Changes from repair v1:
- A pattern-specific domain/context gate is hard: a card cannot be applied to a structurally different mechanism.
- Explicit same-interface qualitative comparisons can satisfy qualitative relations when both operands and direction are visible.
- A complete visible assigned account scope supports an absence predicate; omitted pages remain UNKNOWN.
- A reward/play loop cannot satisfy an infinite-scroll/feed condition without scroll/feed boundary and append evidence.
- A formal review can upgrade on complete proof or downgrade on direct contradiction/core-context failure. Missing secondary measurements alone do not replace the provisional evidence-only judgment.

Labels, prior outputs, and selection rationale remain outside this auditor-visible directory.
