STATE s1
CloudNine Smart Lamp Dimmable smart lamp with app control and schedules. $59.00 In stock — 5 remaining Live stock feed — backend source 5 units Load count (simulated reloads) 1 Transaction log (t1 → t2) 0 purchases Session clock (the delay) 3s Reload page Someone bought one

STATE s2
CloudNine Smart Lamp Dimmable smart lamp with app control and schedules. $59.00 In stock — 5 remaining Live stock feed — backend source 5 units Load count (simulated reloads) 2 Transaction log (t1 → t2) 0 purchases Session clock (the delay) 4s Reload page Someone bought one Quantifier is verifiable Reloading keeps stock at 5, matching the live backend feed — it only moved because no transaction occurred, so the number is stable. The stock level is displayed exactly as the backend reports it: no fabricated low-stock claim.

STATE s3
CloudNine Smart Lamp Dimmable smart lamp with app control and schedules. $59.00 In stock — 5 remaining Live stock feed — backend source 5 units Load count (simulated reloads) 3 Transaction log (t1 → t2) 0 purchases Session clock (the delay) 4s Reload page Someone bought one Quantifier is verifiable Reloading keeps stock at 5, matching the live backend feed — it only moved because no transaction occurred, so the number is stable. The stock level is displayed exactly as the backend reports it: no fabricated low-stock claim.

STATE s4
CloudNine Smart Lamp Dimmable smart lamp with app control and schedules. $59.00 In stock — 5 remaining Live stock feed — backend source 5 units Load count (simulated reloads) 4 Transaction log (t1 → t2) 0 purchases Session clock (the delay) 4s Reload page Someone bought one Quantifier is verifiable Reloading keeps stock at 5, matching the live backend feed — it only moved because no transaction occurred, so the number is stable. The stock level is displayed exactly as the backend reports it: no fabricated low-stock claim.

