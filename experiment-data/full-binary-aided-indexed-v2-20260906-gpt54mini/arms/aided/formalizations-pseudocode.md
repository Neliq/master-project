# Full formalization pseudocode

General rules only: no demonstrations, page examples, or labelled cases.
Formalizations are candidate-specific evidence support, not a replacement classifier.
Missing or unobservable evidence is neutral: it gives no support and cannot negate the live-evidence base judgment.
Apply at most three plausible candidate rules to one page; never combine all rules globally.

## DP-001-C1 — Immortal Accounts — Asymmetrical Navigational Depth

Operational reading: Measured deletion path is longer than creation path

Pseudocode:
- `CONTEXT GATE: identify the rule's specific object/context and mechanism anchor before evaluation.`
- `IF only a generic UI object, generic price, generic CTA, or mere absence is observed: provide no support.`
- `IF a required atom is unavailable: provide no support, preserve the base judgment, and record the evidence as missing.`
- `IF a required atom depends on a UI transition: snapshot the scoped DOM, activate the most direct in-scope control once, wait for rendered settlement, and snapshot again.`
- `IF a visible standard next step is required by the rule: follow one such step and capture the resulting scoped DOM; an unverified click is missing evidence, not a negative.`
- `INPUT evidence_ledger, with state IDs and modality availability.`
- `CHECK the pattern-specific domain/context gate.`
- `IF the core context is contradicted: RETURN NO_MATCH.`
- `PARSE the original formula while preserving every AND, OR, NOT, quantifier, and grouping.`
- `EVALUATE each atomic operand only from directly observed evidence.`
- `EVALUATE implication only as an observed premise-and-consequence proof obligation; never use vacuous truth.`
- `IF the formula result is FALSE: RETURN FALSE.`
- `IF direct_counterevidence is observed: RETURN FALSE.`
- `RETURN TRUE.`

Required observable atoms:
- The creation and deletion actions are identified from the same starting state.
- The shortest interaction path for both actions is observed or directly countable.
- The stated deletion/creation ratio exceeds the formula threshold.

Direct counterevidence:
- The measured deletion path is not longer than the creation path.

## DP-001-C2 — Immortal Accounts — Visual Accessibility of Offboarding Vectors

Operational reading: Deletion affordance is visually subordinate to creation affordance

Pseudocode:
- `CONTEXT GATE: identify the rule's specific object/context and mechanism anchor before evaluation.`
- `IF only a generic UI object, generic price, generic CTA, or mere absence is observed: provide no support.`
- `IF a required atom is unavailable: provide no support, preserve the base judgment, and record the evidence as missing.`
- `IF a required atom depends on a UI transition: snapshot the scoped DOM, activate the most direct in-scope control once, wait for rendered settlement, and snapshot again.`
- `IF a visible standard next step is required by the rule: follow one such step and capture the resulting scoped DOM; an unverified click is missing evidence, not a negative.`
- `INPUT evidence_ledger, with state IDs and modality availability.`
- `CHECK the pattern-specific domain/context gate.`
- `IF the core context is contradicted: RETURN NO_MATCH.`
- `PARSE the original formula while preserving every AND, OR, NOT, quantifier, and grouping.`
- `EVALUATE each atomic operand only from directly observed evidence.`
- `EVALUATE implication only as an observed premise-and-consequence proof obligation; never use vacuous truth.`
- `IF the formula result is FALSE: RETURN FALSE.`
- `IF direct_counterevidence is observed: RETURN FALSE.`
- `RETURN TRUE.`

Required observable atoms:
- A deletion/offboarding affordance and a creation affordance are both identified in the same state.
- Their comparable visual areas are observed or measured.
- The ratio is below the stated threshold, using the same viewport and element-selection rule.

Direct counterevidence:
- The exit affordance has equal or greater comparable prominence.

## DP-001-C3 — Immortal Accounts — Absolute Absence of Deletion Vectors

Operational reading: No deletion vector in the complete inspected account scope

Pseudocode:
- `CONTEXT GATE: identify the rule's specific object/context and mechanism anchor before evaluation.`
- `IF only a generic UI object, generic price, generic CTA, or mere absence is observed: provide no support.`
- `IF a required atom is unavailable: provide no support, preserve the base judgment, and record the evidence as missing.`
- `IF a required atom depends on a UI transition: snapshot the scoped DOM, activate the most direct in-scope control once, wait for rendered settlement, and snapshot again.`
- `IF a visible standard next step is required by the rule: follow one such step and capture the resulting scoped DOM; an unverified click is missing evidence, not a negative.`
- `INPUT evidence_ledger, with state IDs and modality availability.`
- `CHECK the pattern-specific domain/context gate.`
- `IF the core context is contradicted: RETURN NO_MATCH.`
- `PARSE the original formula while preserving every AND, OR, NOT, quantifier, and grouping.`
- `EVALUATE each atomic operand only from directly observed evidence.`
- `EVALUATE implication only as an observed premise-and-consequence proof obligation; never use vacuous truth.`
- `IF the formula result is FALSE: RETURN FALSE.`
- `IF direct_counterevidence is observed: RETURN FALSE.`
- `RETURN TRUE.`

Required observable atoms:
- The assigned evidence exposes the complete visible account-settings/profile scope being evaluated.
- The deletion-keyword set and equivalent labelled controls are absent from that complete visible scope.

Direct counterevidence:
- A deletion, deactivation, close-account, or equivalent vector is present in the inspected scope.

## DP-002-C1 — Dead End — Topological Sink in the Navigational Graph

Operational reading: Evaluate the source formula over directly observed evidence; preserve every Boolean operator, quantifier, comparison, unit, scope, and threshold.

Pseudocode:
- `CONTEXT GATE: identify the rule's specific object/context and mechanism anchor before evaluation.`
- `IF only a generic UI object, generic price, generic CTA, or mere absence is observed: provide no support.`
- `IF a required atom is unavailable: provide no support, preserve the base judgment, and record the evidence as missing.`
- `IF a required atom depends on a UI transition: snapshot the scoped DOM, activate the most direct in-scope control once, wait for rendered settlement, and snapshot again.`
- `IF a visible standard next step is required by the rule: follow one such step and capture the resulting scoped DOM; an unverified click is missing evidence, not a negative.`
- `INPUT evidence_ledger, with state IDs and modality availability.`
- `CHECK the pattern-specific domain/context gate.`
- `IF the core context is contradicted: RETURN NO_MATCH.`
- `PARSE the original formula while preserving every AND, OR, NOT, quantifier, and grouping.`
- `EVALUATE each atomic operand only from directly observed evidence.`
- `EVALUATE implication only as an observed premise-and-consequence proof obligation; never use vacuous truth.`
- `IF the formula result is FALSE: RETURN FALSE.`
- `IF direct_counterevidence is observed: RETURN FALSE.`
- `RETURN TRUE.`

Required observable atoms:
- Every atomic clause in the original formula is identified and evaluated in the stated scope.

Direct counterevidence:
- A directly observed atom contradicts a mandatory requirement.

## DP-002-C2 — Dead End — Visual Absence of Dismissal Vectors

Operational reading: Evaluate the source formula over directly observed evidence; preserve every Boolean operator, quantifier, comparison, unit, scope, and threshold.

Pseudocode:
- `CONTEXT GATE: identify the rule's specific object/context and mechanism anchor before evaluation.`
- `IF only a generic UI object, generic price, generic CTA, or mere absence is observed: provide no support.`
- `IF a required atom is unavailable: provide no support, preserve the base judgment, and record the evidence as missing.`
- `IF a required atom depends on a UI transition: snapshot the scoped DOM, activate the most direct in-scope control once, wait for rendered settlement, and snapshot again.`
- `IF a visible standard next step is required by the rule: follow one such step and capture the resulting scoped DOM; an unverified click is missing evidence, not a negative.`
- `INPUT evidence_ledger, with state IDs and modality availability.`
- `CHECK the pattern-specific domain/context gate.`
- `IF the core context is contradicted: RETURN NO_MATCH.`
- `PARSE the original formula while preserving every AND, OR, NOT, quantifier, and grouping.`
- `EVALUATE each atomic operand only from directly observed evidence.`
- `EVALUATE implication only as an observed premise-and-consequence proof obligation; never use vacuous truth.`
- `IF the formula result is FALSE: RETURN FALSE.`
- `IF direct_counterevidence is observed: RETURN FALSE.`
- `RETURN TRUE.`

Required observable atoms:
- Every atomic clause in the original formula is identified and evaluated in the stated scope.

Direct counterevidence:
- A directly observed atom contradicts a mandatory requirement.

## DP-002-C3 — Dead End — Semantic Coercion Density in Trapped States

Operational reading: Evaluate the source formula over directly observed evidence; preserve every Boolean operator, quantifier, comparison, unit, scope, and threshold.

Pseudocode:
- `CONTEXT GATE: identify the rule's specific object/context and mechanism anchor before evaluation.`
- `IF only a generic UI object, generic price, generic CTA, or mere absence is observed: provide no support.`
- `IF a required atom is unavailable: provide no support, preserve the base judgment, and record the evidence as missing.`
- `IF a required atom depends on a UI transition: snapshot the scoped DOM, activate the most direct in-scope control once, wait for rendered settlement, and snapshot again.`
- `IF a visible standard next step is required by the rule: follow one such step and capture the resulting scoped DOM; an unverified click is missing evidence, not a negative.`
- `INPUT evidence_ledger, with state IDs and modality availability.`
- `CHECK the pattern-specific domain/context gate.`
- `IF the core context is contradicted: RETURN NO_MATCH.`
- `PARSE the original formula while preserving every AND, OR, NOT, quantifier, and grouping.`
- `EVALUATE each atomic operand only from directly observed evidence.`
- `EVALUATE implication only as an observed premise-and-consequence proof obligation; never use vacuous truth.`
- `IF the formula result is FALSE: RETURN FALSE.`
- `IF direct_counterevidence is observed: RETURN FALSE.`
- `RETURN TRUE.`

Required observable atoms:
- Every atomic clause in the original formula is identified and evaluated in the stated scope.

Direct counterevidence:
- A directly observed atom contradicts a mandatory requirement.

## DP-003-C1 — Forced Grace Period — Temporal Discrepancy Extraction

Operational reading: Evaluate the source formula over directly observed evidence; preserve every Boolean operator, quantifier, comparison, unit, scope, and threshold.

Pseudocode:
- `CONTEXT GATE: identify the rule's specific object/context and mechanism anchor before evaluation.`
- `IF only a generic UI object, generic price, generic CTA, or mere absence is observed: provide no support.`
- `IF a required atom is unavailable: provide no support, preserve the base judgment, and record the evidence as missing.`
- `IF a required atom depends on a UI transition: snapshot the scoped DOM, activate the most direct in-scope control once, wait for rendered settlement, and snapshot again.`
- `IF a visible standard next step is required by the rule: follow one such step and capture the resulting scoped DOM; an unverified click is missing evidence, not a negative.`
- `INPUT evidence_ledger, with state IDs and modality availability.`
- `CHECK the pattern-specific domain/context gate.`
- `IF the core context is contradicted: RETURN NO_MATCH.`
- `PARSE the original formula while preserving every AND, OR, NOT, quantifier, and grouping.`
- `EVALUATE each atomic operand only from directly observed evidence.`
- `EVALUATE implication only as an observed premise-and-consequence proof obligation; never use vacuous truth.`
- `IF the formula result is FALSE: RETURN FALSE.`
- `IF direct_counterevidence is observed: RETURN FALSE.`
- `RETURN TRUE.`

Required observable atoms:
- Every atomic clause in the original formula is identified and evaluated in the stated scope.

Direct counterevidence:
- A directly observed atom contradicts a mandatory requirement.

## DP-003-C2 — Forced Grace Period — Visual Conspicuity of Cancellation Affordance

Operational reading: Evaluate the source formula over directly observed evidence; preserve every Boolean operator, quantifier, comparison, unit, scope, and threshold.

Pseudocode:
- `CONTEXT GATE: identify the rule's specific object/context and mechanism anchor before evaluation.`
- `IF only a generic UI object, generic price, generic CTA, or mere absence is observed: provide no support.`
- `IF a required atom is unavailable: provide no support, preserve the base judgment, and record the evidence as missing.`
- `IF a required atom depends on a UI transition: snapshot the scoped DOM, activate the most direct in-scope control once, wait for rendered settlement, and snapshot again.`
- `IF a visible standard next step is required by the rule: follow one such step and capture the resulting scoped DOM; an unverified click is missing evidence, not a negative.`
- `INPUT evidence_ledger, with state IDs and modality availability.`
- `CHECK the pattern-specific domain/context gate.`
- `IF the core context is contradicted: RETURN NO_MATCH.`
- `PARSE the original formula while preserving every AND, OR, NOT, quantifier, and grouping.`
- `EVALUATE each atomic operand only from directly observed evidence.`
- `EVALUATE implication only as an observed premise-and-consequence proof obligation; never use vacuous truth.`
- `IF the formula result is FALSE: RETURN FALSE.`
- `IF direct_counterevidence is observed: RETURN FALSE.`
- `RETURN TRUE.`

Required observable atoms:
- Every atomic clause in the original formula is identified and evaluated in the stated scope.

Direct counterevidence:
- A directly observed atom contradicts a mandatory requirement.

## DP-003-C3 — Forced Grace Period — Semantic Proximity of Reversal

Operational reading: Evaluate the source formula over directly observed evidence; preserve every Boolean operator, quantifier, comparison, unit, scope, and threshold.

Pseudocode:
- `CONTEXT GATE: identify the rule's specific object/context and mechanism anchor before evaluation.`
- `IF only a generic UI object, generic price, generic CTA, or mere absence is observed: provide no support.`
- `IF a required atom is unavailable: provide no support, preserve the base judgment, and record the evidence as missing.`
- `IF a required atom depends on a UI transition: snapshot the scoped DOM, activate the most direct in-scope control once, wait for rendered settlement, and snapshot again.`
- `IF a visible standard next step is required by the rule: follow one such step and capture the resulting scoped DOM; an unverified click is missing evidence, not a negative.`
- `INPUT evidence_ledger, with state IDs and modality availability.`
- `CHECK the pattern-specific domain/context gate.`
- `IF the core context is contradicted: RETURN NO_MATCH.`
- `PARSE the original formula while preserving every AND, OR, NOT, quantifier, and grouping.`
- `EVALUATE each atomic operand only from directly observed evidence.`
- `EVALUATE implication only as an observed premise-and-consequence proof obligation; never use vacuous truth.`
- `IF the formula result is FALSE: RETURN FALSE.`
- `IF direct_counterevidence is observed: RETURN FALSE.`
- `RETURN TRUE.`

Required observable atoms:
- Every atomic clause in the original formula is identified and evaluated in the stated scope.

Direct counterevidence:
- A directly observed atom contradicts a mandatory requirement.

## DP-008-C1 — Privacy Maze — Asymmetrical Path Depth

Operational reading: Evaluate the source formula over directly observed evidence; preserve every Boolean operator, quantifier, comparison, unit, scope, and threshold.

Pseudocode:
- `CONTEXT GATE: identify the rule's specific object/context and mechanism anchor before evaluation.`
- `IF only a generic UI object, generic price, generic CTA, or mere absence is observed: provide no support.`
- `IF a required atom is unavailable: provide no support, preserve the base judgment, and record the evidence as missing.`
- `IF a required atom depends on a UI transition: snapshot the scoped DOM, activate the most direct in-scope control once, wait for rendered settlement, and snapshot again.`
- `IF a visible standard next step is required by the rule: follow one such step and capture the resulting scoped DOM; an unverified click is missing evidence, not a negative.`
- `INPUT evidence_ledger, with state IDs and modality availability.`
- `CHECK the pattern-specific domain/context gate.`
- `IF the core context is contradicted: RETURN NO_MATCH.`
- `PARSE the original formula while preserving every AND, OR, NOT, quantifier, and grouping.`
- `EVALUATE each atomic operand only from directly observed evidence.`
- `EVALUATE implication only as an observed premise-and-consequence proof obligation; never use vacuous truth.`
- `IF the formula result is FALSE: RETURN FALSE.`
- `IF direct_counterevidence is observed: RETURN FALSE.`
- `RETURN TRUE.`

Required observable atoms:
- Every atomic clause in the original formula is identified and evaluated in the stated scope.

Direct counterevidence:
- A directly observed atom contradicts a mandatory requirement.

## DP-008-C2 — Privacy Maze — Visual Prominence Disparity

Operational reading: Evaluate the source formula over directly observed evidence; preserve every Boolean operator, quantifier, comparison, unit, scope, and threshold.

Pseudocode:
- `CONTEXT GATE: identify the rule's specific object/context and mechanism anchor before evaluation.`
- `IF only a generic UI object, generic price, generic CTA, or mere absence is observed: provide no support.`
- `IF a required atom is unavailable: provide no support, preserve the base judgment, and record the evidence as missing.`
- `IF a required atom depends on a UI transition: snapshot the scoped DOM, activate the most direct in-scope control once, wait for rendered settlement, and snapshot again.`
- `IF a visible standard next step is required by the rule: follow one such step and capture the resulting scoped DOM; an unverified click is missing evidence, not a negative.`
- `INPUT evidence_ledger, with state IDs and modality availability.`
- `CHECK the pattern-specific domain/context gate.`
- `IF the core context is contradicted: RETURN NO_MATCH.`
- `PARSE the original formula while preserving every AND, OR, NOT, quantifier, and grouping.`
- `EVALUATE each atomic operand only from directly observed evidence.`
- `EVALUATE implication only as an observed premise-and-consequence proof obligation; never use vacuous truth.`
- `IF the formula result is FALSE: RETURN FALSE.`
- `IF direct_counterevidence is observed: RETURN FALSE.`
- `RETURN TRUE.`

Required observable atoms:
- Every atomic clause in the original formula is identified and evaluated in the stated scope.

Direct counterevidence:
- A directly observed atom contradicts a mandatory requirement.

## DP-008-C3 — Privacy Maze — Semantic Obfuscation of Privacy-Related Terminology

Operational reading: Evaluate the source formula over directly observed evidence; preserve every Boolean operator, quantifier, comparison, unit, scope, and threshold.

Pseudocode:
- `CONTEXT GATE: identify the rule's specific object/context and mechanism anchor before evaluation.`
- `IF only a generic UI object, generic price, generic CTA, or mere absence is observed: provide no support.`
- `IF a required atom is unavailable: provide no support, preserve the base judgment, and record the evidence as missing.`
- `IF a required atom depends on a UI transition: snapshot the scoped DOM, activate the most direct in-scope control once, wait for rendered settlement, and snapshot again.`
- `IF a visible standard next step is required by the rule: follow one such step and capture the resulting scoped DOM; an unverified click is missing evidence, not a negative.`
- `INPUT evidence_ledger, with state IDs and modality availability.`
- `CHECK the pattern-specific domain/context gate.`
- `IF the core context is contradicted: RETURN NO_MATCH.`
- `PARSE the original formula while preserving every AND, OR, NOT, quantifier, and grouping.`
- `EVALUATE each atomic operand only from directly observed evidence.`
- `EVALUATE implication only as an observed premise-and-consequence proof obligation; never use vacuous truth.`
- `IF the formula result is FALSE: RETURN FALSE.`
- `IF direct_counterevidence is observed: RETURN FALSE.`
- `RETURN TRUE.`

Required observable atoms:
- Every atomic clause in the original formula is identified and evaluated in the stated scope.

Direct counterevidence:
- A directly observed atom contradicts a mandatory requirement.

## DP-009-C1 — Labyrinthine Navigation — Excessive Navigational Depth

Operational reading: Evaluate the source formula over directly observed evidence; preserve every Boolean operator, quantifier, comparison, unit, scope, and threshold.

Pseudocode:
- `CONTEXT GATE: identify the rule's specific object/context and mechanism anchor before evaluation.`
- `IF only a generic UI object, generic price, generic CTA, or mere absence is observed: provide no support.`
- `IF a required atom is unavailable: provide no support, preserve the base judgment, and record the evidence as missing.`
- `IF a required atom depends on a UI transition: snapshot the scoped DOM, activate the most direct in-scope control once, wait for rendered settlement, and snapshot again.`
- `IF a visible standard next step is required by the rule: follow one such step and capture the resulting scoped DOM; an unverified click is missing evidence, not a negative.`
- `INPUT evidence_ledger, with state IDs and modality availability.`
- `CHECK the pattern-specific domain/context gate.`
- `IF the core context is contradicted: RETURN NO_MATCH.`
- `PARSE the original formula while preserving every AND, OR, NOT, quantifier, and grouping.`
- `EVALUATE each atomic operand only from directly observed evidence.`
- `EVALUATE implication only as an observed premise-and-consequence proof obligation; never use vacuous truth.`
- `IF the formula result is FALSE: RETURN FALSE.`
- `IF direct_counterevidence is observed: RETURN FALSE.`
- `RETURN TRUE.`

Required observable atoms:
- Every atomic clause in the original formula is identified and evaluated in the stated scope.

Direct counterevidence:
- A directly observed atom contradicts a mandatory requirement.

## DP-009-C2 — Labyrinthine Navigation — Visual Nesting Depth of Navigation Elements

Operational reading: Evaluate the source formula over directly observed evidence; preserve every Boolean operator, quantifier, comparison, unit, scope, and threshold.

Pseudocode:
- `CONTEXT GATE: identify the rule's specific object/context and mechanism anchor before evaluation.`
- `IF only a generic UI object, generic price, generic CTA, or mere absence is observed: provide no support.`
- `IF a required atom is unavailable: provide no support, preserve the base judgment, and record the evidence as missing.`
- `IF a required atom depends on a UI transition: snapshot the scoped DOM, activate the most direct in-scope control once, wait for rendered settlement, and snapshot again.`
- `IF a visible standard next step is required by the rule: follow one such step and capture the resulting scoped DOM; an unverified click is missing evidence, not a negative.`
- `INPUT evidence_ledger, with state IDs and modality availability.`
- `CHECK the pattern-specific domain/context gate.`
- `IF the core context is contradicted: RETURN NO_MATCH.`
- `PARSE the original formula while preserving every AND, OR, NOT, quantifier, and grouping.`
- `EVALUATE each atomic operand only from directly observed evidence.`
- `EVALUATE implication only as an observed premise-and-consequence proof obligation; never use vacuous truth.`
- `IF the formula result is FALSE: RETURN FALSE.`
- `IF direct_counterevidence is observed: RETURN FALSE.`
- `RETURN TRUE.`

Required observable atoms:
- Every atomic clause in the original formula is identified and evaluated in the stated scope.

Direct counterevidence:
- A directly observed atom contradicts a mandatory requirement.

## DP-009-C3 — Labyrinthine Navigation — Semantic Obfuscation

Operational reading: Evaluate the source formula over directly observed evidence; preserve every Boolean operator, quantifier, comparison, unit, scope, and threshold.

Pseudocode:
- `CONTEXT GATE: identify the rule's specific object/context and mechanism anchor before evaluation.`
- `IF only a generic UI object, generic price, generic CTA, or mere absence is observed: provide no support.`
- `IF a required atom is unavailable: provide no support, preserve the base judgment, and record the evidence as missing.`
- `IF a required atom depends on a UI transition: snapshot the scoped DOM, activate the most direct in-scope control once, wait for rendered settlement, and snapshot again.`
- `IF a visible standard next step is required by the rule: follow one such step and capture the resulting scoped DOM; an unverified click is missing evidence, not a negative.`
- `INPUT evidence_ledger, with state IDs and modality availability.`
- `CHECK the pattern-specific domain/context gate.`
- `IF the core context is contradicted: RETURN NO_MATCH.`
- `PARSE the original formula while preserving every AND, OR, NOT, quantifier, and grouping.`
- `EVALUATE each atomic operand only from directly observed evidence.`
- `EVALUATE implication only as an observed premise-and-consequence proof obligation; never use vacuous truth.`
- `IF the formula result is FALSE: RETURN FALSE.`
- `IF direct_counterevidence is observed: RETURN FALSE.`
- `RETURN TRUE.`

Required observable atoms:
- Every atomic clause in the original formula is identified and evaluated in the stated scope.

Direct counterevidence:
- A directly observed atom contradicts a mandatory requirement.

## DP-035-C1 — Customisation (Interface Nesting) — Path Depth Asymmetry

Operational reading: Evaluate the source formula over directly observed evidence; preserve every Boolean operator, quantifier, comparison, unit, scope, and threshold.

Pseudocode:
- `CONTEXT GATE: identify the rule's specific object/context and mechanism anchor before evaluation.`
- `IF only a generic UI object, generic price, generic CTA, or mere absence is observed: provide no support.`
- `IF a required atom is unavailable: provide no support, preserve the base judgment, and record the evidence as missing.`
- `IF a required atom depends on a UI transition: snapshot the scoped DOM, activate the most direct in-scope control once, wait for rendered settlement, and snapshot again.`
- `IF a visible standard next step is required by the rule: follow one such step and capture the resulting scoped DOM; an unverified click is missing evidence, not a negative.`
- `INPUT evidence_ledger, with state IDs and modality availability.`
- `CHECK the pattern-specific domain/context gate.`
- `IF the core context is contradicted: RETURN NO_MATCH.`
- `PARSE the original formula while preserving every AND, OR, NOT, quantifier, and grouping.`
- `EVALUATE each atomic operand only from directly observed evidence.`
- `EVALUATE implication only as an observed premise-and-consequence proof obligation; never use vacuous truth.`
- `IF the formula result is FALSE: RETURN FALSE.`
- `IF direct_counterevidence is observed: RETURN FALSE.`
- `RETURN TRUE.`

Required observable atoms:
- Every atomic clause in the original formula is identified and evaluated in the stated scope.

Direct counterevidence:
- A directly observed atom contradicts a mandatory requirement.

## DP-035-C2 — Customisation (Interface Nesting) — Visual Indentation Depth of Privacy Controls

Operational reading: Evaluate the source formula over directly observed evidence; preserve every Boolean operator, quantifier, comparison, unit, scope, and threshold.

Pseudocode:
- `CONTEXT GATE: identify the rule's specific object/context and mechanism anchor before evaluation.`
- `IF only a generic UI object, generic price, generic CTA, or mere absence is observed: provide no support.`
- `IF a required atom is unavailable: provide no support, preserve the base judgment, and record the evidence as missing.`
- `IF a required atom depends on a UI transition: snapshot the scoped DOM, activate the most direct in-scope control once, wait for rendered settlement, and snapshot again.`
- `IF a visible standard next step is required by the rule: follow one such step and capture the resulting scoped DOM; an unverified click is missing evidence, not a negative.`
- `INPUT evidence_ledger, with state IDs and modality availability.`
- `CHECK the pattern-specific domain/context gate.`
- `IF the core context is contradicted: RETURN NO_MATCH.`
- `PARSE the original formula while preserving every AND, OR, NOT, quantifier, and grouping.`
- `EVALUATE each atomic operand only from directly observed evidence.`
- `EVALUATE implication only as an observed premise-and-consequence proof obligation; never use vacuous truth.`
- `IF the formula result is FALSE: RETURN FALSE.`
- `IF direct_counterevidence is observed: RETURN FALSE.`
- `RETURN TRUE.`

Required observable atoms:
- Every atomic clause in the original formula is identified and evaluated in the stated scope.

Direct counterevidence:
- A directly observed atom contradicts a mandatory requirement.

## DP-035-C3 — Customisation (Interface Nesting) — Semantic Obfuscation of Privacy Toggle Labels

Operational reading: Evaluate the source formula over directly observed evidence; preserve every Boolean operator, quantifier, comparison, unit, scope, and threshold.

Pseudocode:
- `CONTEXT GATE: identify the rule's specific object/context and mechanism anchor before evaluation.`
- `IF only a generic UI object, generic price, generic CTA, or mere absence is observed: provide no support.`
- `IF a required atom is unavailable: provide no support, preserve the base judgment, and record the evidence as missing.`
- `IF a required atom depends on a UI transition: snapshot the scoped DOM, activate the most direct in-scope control once, wait for rendered settlement, and snapshot again.`
- `IF a visible standard next step is required by the rule: follow one such step and capture the resulting scoped DOM; an unverified click is missing evidence, not a negative.`
- `INPUT evidence_ledger, with state IDs and modality availability.`
- `CHECK the pattern-specific domain/context gate.`
- `IF the core context is contradicted: RETURN NO_MATCH.`
- `PARSE the original formula while preserving every AND, OR, NOT, quantifier, and grouping.`
- `EVALUATE each atomic operand only from directly observed evidence.`
- `EVALUATE implication only as an observed premise-and-consequence proof obligation; never use vacuous truth.`
- `IF the formula result is FALSE: RETURN FALSE.`
- `IF direct_counterevidence is observed: RETURN FALSE.`
- `RETURN TRUE.`

Required observable atoms:
- Every atomic clause in the original formula is identified and evaluated in the stated scope.

Direct counterevidence:
- A directly observed atom contradicts a mandatory requirement.

## DP-005-C1 — Intermediate Currency — Interception of the Fiat Checkout Flow

Operational reading: Evaluate the source formula over directly observed evidence; preserve every Boolean operator, quantifier, comparison, unit, scope, and threshold.

Pseudocode:
- `CONTEXT GATE: identify the rule's specific object/context and mechanism anchor before evaluation.`
- `IF only a generic UI object, generic price, generic CTA, or mere absence is observed: provide no support.`
- `IF a required atom is unavailable: provide no support, preserve the base judgment, and record the evidence as missing.`
- `IF a required atom depends on a UI transition: snapshot the scoped DOM, activate the most direct in-scope control once, wait for rendered settlement, and snapshot again.`
- `IF a visible standard next step is required by the rule: follow one such step and capture the resulting scoped DOM; an unverified click is missing evidence, not a negative.`
- `INPUT evidence_ledger, with state IDs and modality availability.`
- `CHECK the pattern-specific domain/context gate.`
- `IF the core context is contradicted: RETURN NO_MATCH.`
- `PARSE the original formula while preserving every AND, OR, NOT, quantifier, and grouping.`
- `EVALUATE each atomic operand only from directly observed evidence.`
- `EVALUATE implication only as an observed premise-and-consequence proof obligation; never use vacuous truth.`
- `IF the formula result is FALSE: RETURN FALSE.`
- `IF direct_counterevidence is observed: RETURN FALSE.`
- `RETURN TRUE.`

Required observable atoms:
- Every atomic clause in the original formula is identified and evaluated in the stated scope.

Direct counterevidence:
- A directly observed atom contradicts a mandatory requirement.

## DP-005-C2 — Intermediate Currency — Visual Obscuration of Real-Currency Equivalence

Operational reading: Evaluate the source formula over directly observed evidence; preserve every Boolean operator, quantifier, comparison, unit, scope, and threshold.

Pseudocode:
- `CONTEXT GATE: identify the rule's specific object/context and mechanism anchor before evaluation.`
- `IF only a generic UI object, generic price, generic CTA, or mere absence is observed: provide no support.`
- `IF a required atom is unavailable: provide no support, preserve the base judgment, and record the evidence as missing.`
- `IF a required atom depends on a UI transition: snapshot the scoped DOM, activate the most direct in-scope control once, wait for rendered settlement, and snapshot again.`
- `IF a visible standard next step is required by the rule: follow one such step and capture the resulting scoped DOM; an unverified click is missing evidence, not a negative.`
- `INPUT evidence_ledger, with state IDs and modality availability.`
- `CHECK the pattern-specific domain/context gate.`
- `IF the core context is contradicted: RETURN NO_MATCH.`
- `PARSE the original formula while preserving every AND, OR, NOT, quantifier, and grouping.`
- `EVALUATE each atomic operand only from directly observed evidence.`
- `EVALUATE implication only as an observed premise-and-consequence proof obligation; never use vacuous truth.`
- `IF the formula result is FALSE: RETURN FALSE.`
- `IF direct_counterevidence is observed: RETURN FALSE.`
- `RETURN TRUE.`

Required observable atoms:
- Every atomic clause in the original formula is identified and evaluated in the stated scope.

Direct counterevidence:
- A directly observed atom contradicts a mandatory requirement.

## DP-005-C3 — Intermediate Currency — Lexical Tokenization mapped to Forced Exchange

Operational reading: Evaluate the source formula over directly observed evidence; preserve every Boolean operator, quantifier, comparison, unit, scope, and threshold.

Pseudocode:
- `CONTEXT GATE: identify the rule's specific object/context and mechanism anchor before evaluation.`
- `IF only a generic UI object, generic price, generic CTA, or mere absence is observed: provide no support.`
- `IF a required atom is unavailable: provide no support, preserve the base judgment, and record the evidence as missing.`
- `IF a required atom depends on a UI transition: snapshot the scoped DOM, activate the most direct in-scope control once, wait for rendered settlement, and snapshot again.`
- `IF a visible standard next step is required by the rule: follow one such step and capture the resulting scoped DOM; an unverified click is missing evidence, not a negative.`
- `INPUT evidence_ledger, with state IDs and modality availability.`
- `CHECK the pattern-specific domain/context gate.`
- `IF the core context is contradicted: RETURN NO_MATCH.`
- `PARSE the original formula while preserving every AND, OR, NOT, quantifier, and grouping.`
- `EVALUATE each atomic operand only from directly observed evidence.`
- `EVALUATE implication only as an observed premise-and-consequence proof obligation; never use vacuous truth.`
- `IF the formula result is FALSE: RETURN FALSE.`
- `IF direct_counterevidence is observed: RETURN FALSE.`
- `RETURN TRUE.`

Required observable atoms:
- Every atomic clause in the original formula is identified and evaluated in the stated scope.

Direct counterevidence:
- A directly observed atom contradicts a mandatory requirement.

## DP-006-C1 — Disguised Ad — Cross-Origin Action Masking

Operational reading: Evaluate the source formula over directly observed evidence; preserve every Boolean operator, quantifier, comparison, unit, scope, and threshold.

Pseudocode:
- `CONTEXT GATE: identify the rule's specific object/context and mechanism anchor before evaluation.`
- `IF only a generic UI object, generic price, generic CTA, or mere absence is observed: provide no support.`
- `IF a required atom is unavailable: provide no support, preserve the base judgment, and record the evidence as missing.`
- `IF a required atom depends on a UI transition: snapshot the scoped DOM, activate the most direct in-scope control once, wait for rendered settlement, and snapshot again.`
- `IF a visible standard next step is required by the rule: follow one such step and capture the resulting scoped DOM; an unverified click is missing evidence, not a negative.`
- `INPUT evidence_ledger, with state IDs and modality availability.`
- `CHECK the pattern-specific domain/context gate.`
- `IF the core context is contradicted: RETURN NO_MATCH.`
- `PARSE the original formula while preserving every AND, OR, NOT, quantifier, and grouping.`
- `EVALUATE each atomic operand only from directly observed evidence.`
- `EVALUATE implication only as an observed premise-and-consequence proof obligation; never use vacuous truth.`
- `IF the formula result is FALSE: RETURN FALSE.`
- `IF direct_counterevidence is observed: RETURN FALSE.`
- `RETURN TRUE.`

Required observable atoms:
- Every atomic clause in the original formula is identified and evaluated in the stated scope.

Direct counterevidence:
- A directly observed atom contradicts a mandatory requirement.

## DP-006-C2 — Disguised Ad — Morphological Similarity

Operational reading: Evaluate the source formula over directly observed evidence; preserve every Boolean operator, quantifier, comparison, unit, scope, and threshold.

Pseudocode:
- `CONTEXT GATE: identify the rule's specific object/context and mechanism anchor before evaluation.`
- `IF only a generic UI object, generic price, generic CTA, or mere absence is observed: provide no support.`
- `IF a required atom is unavailable: provide no support, preserve the base judgment, and record the evidence as missing.`
- `IF a required atom depends on a UI transition: snapshot the scoped DOM, activate the most direct in-scope control once, wait for rendered settlement, and snapshot again.`
- `IF a visible standard next step is required by the rule: follow one such step and capture the resulting scoped DOM; an unverified click is missing evidence, not a negative.`
- `INPUT evidence_ledger, with state IDs and modality availability.`
- `CHECK the pattern-specific domain/context gate.`
- `IF the core context is contradicted: RETURN NO_MATCH.`
- `PARSE the original formula while preserving every AND, OR, NOT, quantifier, and grouping.`
- `EVALUATE each atomic operand only from directly observed evidence.`
- `EVALUATE implication only as an observed premise-and-consequence proof obligation; never use vacuous truth.`
- `IF the formula result is FALSE: RETURN FALSE.`
- `IF direct_counterevidence is observed: RETURN FALSE.`
- `RETURN TRUE.`

Required observable atoms:
- Every atomic clause in the original formula is identified and evaluated in the stated scope.

Direct counterevidence:
- A directly observed atom contradicts a mandatory requirement.

## DP-006-C3 — Disguised Ad — Semantic Mimicry of Native Action Labels

Operational reading: Evaluate the source formula over directly observed evidence; preserve every Boolean operator, quantifier, comparison, unit, scope, and threshold.

Pseudocode:
- `CONTEXT GATE: identify the rule's specific object/context and mechanism anchor before evaluation.`
- `IF only a generic UI object, generic price, generic CTA, or mere absence is observed: provide no support.`
- `IF a required atom is unavailable: provide no support, preserve the base judgment, and record the evidence as missing.`
- `IF a required atom depends on a UI transition: snapshot the scoped DOM, activate the most direct in-scope control once, wait for rendered settlement, and snapshot again.`
- `IF a visible standard next step is required by the rule: follow one such step and capture the resulting scoped DOM; an unverified click is missing evidence, not a negative.`
- `INPUT evidence_ledger, with state IDs and modality availability.`
- `CHECK the pattern-specific domain/context gate.`
- `IF the core context is contradicted: RETURN NO_MATCH.`
- `PARSE the original formula while preserving every AND, OR, NOT, quantifier, and grouping.`
- `EVALUATE each atomic operand only from directly observed evidence.`
- `EVALUATE implication only as an observed premise-and-consequence proof obligation; never use vacuous truth.`
- `IF the formula result is FALSE: RETURN FALSE.`
- `IF direct_counterevidence is observed: RETURN FALSE.`
- `RETURN TRUE.`

Required observable atoms:
- Every atomic clause in the original formula is identified and evaluated in the stated scope.

Direct counterevidence:
- A directly observed atom contradicts a mandatory requirement.

## DP-007-C1 — Sneak Into Basket — Unprompted State Mutation

Operational reading: An item appears in the cart without a matching user event

Pseudocode:
- `CONTEXT GATE: identify the rule's specific object/context and mechanism anchor before evaluation.`
- `IF only a generic UI object, generic price, generic CTA, or mere absence is observed: provide no support.`
- `IF a required atom is unavailable: provide no support, preserve the base judgment, and record the evidence as missing.`
- `IF a required atom depends on a UI transition: snapshot the scoped DOM, activate the most direct in-scope control once, wait for rendered settlement, and snapshot again.`
- `IF a visible standard next step is required by the rule: follow one such step and capture the resulting scoped DOM; an unverified click is missing evidence, not a negative.`
- `INPUT evidence_ledger, with state IDs and modality availability.`
- `CHECK the pattern-specific domain/context gate.`
- `IF the core context is contradicted: RETURN NO_MATCH.`
- `PARSE the original formula while preserving every AND, OR, NOT, quantifier, and grouping.`
- `EVALUATE each atomic operand only from directly observed evidence.`
- `EVALUATE implication only as an observed premise-and-consequence proof obligation; never use vacuous truth.`
- `IF the formula result is FALSE: RETURN FALSE.`
- `IF direct_counterevidence is observed: RETURN FALSE.`
- `RETURN TRUE.`

Required observable atoms:
- The item is present in the cart/final checkout state.
- The item is absent from the set of explicitly selected items.
- The aligned event trace shows no user event that adds or selects that item.

Direct counterevidence:
- A matching explicit add/select event is observed for the item.

## DP-007-C2 — Sneak Into Basket — Visual Indistinguishability of Surcharged Items

Operational reading: An identified injected item visually resembles legitimate cart items

Pseudocode:
- `CONTEXT GATE: identify the rule's specific object/context and mechanism anchor before evaluation.`
- `IF only a generic UI object, generic price, generic CTA, or mere absence is observed: provide no support.`
- `IF a required atom is unavailable: provide no support, preserve the base judgment, and record the evidence as missing.`
- `IF a required atom depends on a UI transition: snapshot the scoped DOM, activate the most direct in-scope control once, wait for rendered settlement, and snapshot again.`
- `IF a visible standard next step is required by the rule: follow one such step and capture the resulting scoped DOM; an unverified click is missing evidence, not a negative.`
- `INPUT evidence_ledger, with state IDs and modality availability.`
- `CHECK the pattern-specific domain/context gate.`
- `IF the core context is contradicted: RETURN NO_MATCH.`
- `PARSE the original formula while preserving every AND, OR, NOT, quantifier, and grouping.`
- `EVALUATE each atomic operand only from directly observed evidence.`
- `EVALUATE implication only as an observed premise-and-consequence proof obligation; never use vacuous truth.`
- `IF the formula result is FALSE: RETURN FALSE.`
- `IF direct_counterevidence is observed: RETURN FALSE.`
- `RETURN TRUE.`

Required observable atoms:
- An injected/surcharged item has already been established by the relevant cart/event evidence.
- At least one legitimate comparison item is identified in the same state.
- The visual distance is below the stated camouflage threshold under the same feature representation.

Direct counterevidence:
- The injected item is visually distinguished or the threshold is not met.

## DP-007-C3 — Sneak Into Basket — Semantic Obscuration of Injected Line Items

Operational reading: An unconsented injected item is semantically presented as optional

Pseudocode:
- `CONTEXT GATE: identify the rule's specific object/context and mechanism anchor before evaluation.`
- `IF only a generic UI object, generic price, generic CTA, or mere absence is observed: provide no support.`
- `IF a required atom is unavailable: provide no support, preserve the base judgment, and record the evidence as missing.`
- `IF a required atom depends on a UI transition: snapshot the scoped DOM, activate the most direct in-scope control once, wait for rendered settlement, and snapshot again.`
- `IF a visible standard next step is required by the rule: follow one such step and capture the resulting scoped DOM; an unverified click is missing evidence, not a negative.`
- `INPUT evidence_ledger, with state IDs and modality availability.`
- `CHECK the pattern-specific domain/context gate.`
- `IF the core context is contradicted: RETURN NO_MATCH.`
- `PARSE the original formula while preserving every AND, OR, NOT, quantifier, and grouping.`
- `EVALUATE each atomic operand only from directly observed evidence.`
- `EVALUATE implication only as an observed premise-and-consequence proof obligation; never use vacuous truth.`
- `IF the formula result is FALSE: RETURN FALSE.`
- `IF direct_counterevidence is observed: RETURN FALSE.`
- `RETURN TRUE.`

Required observable atoms:
- The line item is established as injected by the cart/event evidence.
- The item description entails optional add-on status.
- No explicit user consent event for that item is observed.

Direct counterevidence:
- An explicit user opt-in for the item is observed.

## DP-010-C1 — Drip Pricing, Hidden Costs, or Partitioned Pricing — Sequential Price Inflation

Operational reading: Evaluate the source formula over directly observed evidence; preserve every Boolean operator, quantifier, comparison, unit, scope, and threshold.

Pseudocode:
- `CONTEXT GATE: identify the rule's specific object/context and mechanism anchor before evaluation.`
- `IF only a generic UI object, generic price, generic CTA, or mere absence is observed: provide no support.`
- `IF a required atom is unavailable: provide no support, preserve the base judgment, and record the evidence as missing.`
- `IF a required atom depends on a UI transition: snapshot the scoped DOM, activate the most direct in-scope control once, wait for rendered settlement, and snapshot again.`
- `IF a visible standard next step is required by the rule: follow one such step and capture the resulting scoped DOM; an unverified click is missing evidence, not a negative.`
- `INPUT evidence_ledger, with state IDs and modality availability.`
- `CHECK the pattern-specific domain/context gate.`
- `IF the core context is contradicted: RETURN NO_MATCH.`
- `PARSE the original formula while preserving every AND, OR, NOT, quantifier, and grouping.`
- `EVALUATE each atomic operand only from directly observed evidence.`
- `EVALUATE implication only as an observed premise-and-consequence proof obligation; never use vacuous truth.`
- `IF the formula result is FALSE: RETURN FALSE.`
- `IF direct_counterevidence is observed: RETURN FALSE.`
- `RETURN TRUE.`

Required observable atoms:
- Every atomic clause in the original formula is identified and evaluated in the stated scope.

Direct counterevidence:
- A directly observed atom contradicts a mandatory requirement.

## DP-010-C2 — Drip Pricing, Hidden Costs, or Partitioned Pricing — Visual Disparity of Cost Partitioning

Operational reading: Evaluate the source formula over directly observed evidence; preserve every Boolean operator, quantifier, comparison, unit, scope, and threshold.

Pseudocode:
- `CONTEXT GATE: identify the rule's specific object/context and mechanism anchor before evaluation.`
- `IF only a generic UI object, generic price, generic CTA, or mere absence is observed: provide no support.`
- `IF a required atom is unavailable: provide no support, preserve the base judgment, and record the evidence as missing.`
- `IF a required atom depends on a UI transition: snapshot the scoped DOM, activate the most direct in-scope control once, wait for rendered settlement, and snapshot again.`
- `IF a visible standard next step is required by the rule: follow one such step and capture the resulting scoped DOM; an unverified click is missing evidence, not a negative.`
- `INPUT evidence_ledger, with state IDs and modality availability.`
- `CHECK the pattern-specific domain/context gate.`
- `IF the core context is contradicted: RETURN NO_MATCH.`
- `PARSE the original formula while preserving every AND, OR, NOT, quantifier, and grouping.`
- `EVALUATE each atomic operand only from directly observed evidence.`
- `EVALUATE implication only as an observed premise-and-consequence proof obligation; never use vacuous truth.`
- `IF the formula result is FALSE: RETURN FALSE.`
- `IF direct_counterevidence is observed: RETURN FALSE.`
- `RETURN TRUE.`

Required observable atoms:
- Every atomic clause in the original formula is identified and evaluated in the stated scope.

Direct counterevidence:
- A directly observed atom contradicts a mandatory requirement.

## DP-010-C3 — Drip Pricing, Hidden Costs, or Partitioned Pricing — Semantic Concealment of Mandatory Fee Disclosure

Operational reading: Evaluate the source formula over directly observed evidence; preserve every Boolean operator, quantifier, comparison, unit, scope, and threshold.

Pseudocode:
- `CONTEXT GATE: identify the rule's specific object/context and mechanism anchor before evaluation.`
- `IF only a generic UI object, generic price, generic CTA, or mere absence is observed: provide no support.`
- `IF a required atom is unavailable: provide no support, preserve the base judgment, and record the evidence as missing.`
- `IF a required atom depends on a UI transition: snapshot the scoped DOM, activate the most direct in-scope control once, wait for rendered settlement, and snapshot again.`
- `IF a visible standard next step is required by the rule: follow one such step and capture the resulting scoped DOM; an unverified click is missing evidence, not a negative.`
- `INPUT evidence_ledger, with state IDs and modality availability.`
- `CHECK the pattern-specific domain/context gate.`
- `IF the core context is contradicted: RETURN NO_MATCH.`
- `PARSE the original formula while preserving every AND, OR, NOT, quantifier, and grouping.`
- `EVALUATE each atomic operand only from directly observed evidence.`
- `EVALUATE implication only as an observed premise-and-consequence proof obligation; never use vacuous truth.`
- `IF the formula result is FALSE: RETURN FALSE.`
- `IF direct_counterevidence is observed: RETURN FALSE.`
- `RETURN TRUE.`

Required observable atoms:
- Every atomic clause in the original formula is identified and evaluated in the stated scope.

Direct counterevidence:
- A directly observed atom contradicts a mandatory requirement.

## DP-016-C1 — Bundling — Inseparable Transactional Nodes

Operational reading: Primary purchase necessarily includes a supplementary item

Pseudocode:
- `CONTEXT GATE: identify the rule's specific object/context and mechanism anchor before evaluation.`
- `IF only a generic UI object, generic price, generic CTA, or mere absence is observed: provide no support.`
- `IF a required atom is unavailable: provide no support, preserve the base judgment, and record the evidence as missing.`
- `IF a required atom depends on a UI transition: snapshot the scoped DOM, activate the most direct in-scope control once, wait for rendered settlement, and snapshot again.`
- `IF a visible standard next step is required by the rule: follow one such step and capture the resulting scoped DOM; an unverified click is missing evidence, not a negative.`
- `INPUT evidence_ledger, with state IDs and modality availability.`
- `CHECK the pattern-specific domain/context gate.`
- `IF the core context is contradicted: RETURN NO_MATCH.`
- `PARSE the original formula while preserving every AND, OR, NOT, quantifier, and grouping.`
- `EVALUATE each atomic operand only from directly observed evidence.`
- `EVALUATE implication only as an observed premise-and-consequence proof obligation; never use vacuous truth.`
- `IF the formula result is FALSE: RETURN FALSE.`
- `IF direct_counterevidence is observed: RETURN FALSE.`
- `RETURN TRUE.`

Required observable atoms:
- The primary purchase event is observed from the relevant starting state.
- The resulting checkout state contains both primary and supplementary items.
- No primary-only purchase event/path is observed in the inspected flow.

Direct counterevidence:
- A primary-only purchase path is directly available.

## DP-016-C2 — Bundling — Visual Obscuration of Individual Component Pricing

Operational reading: Component prices occupy less than the stated share of the bundle presentation

Pseudocode:
- `CONTEXT GATE: identify the rule's specific object/context and mechanism anchor before evaluation.`
- `IF only a generic UI object, generic price, generic CTA, or mere absence is observed: provide no support.`
- `IF a required atom is unavailable: provide no support, preserve the base judgment, and record the evidence as missing.`
- `IF a required atom depends on a UI transition: snapshot the scoped DOM, activate the most direct in-scope control once, wait for rendered settlement, and snapshot again.`
- `IF a visible standard next step is required by the rule: follow one such step and capture the resulting scoped DOM; an unverified click is missing evidence, not a negative.`
- `INPUT evidence_ledger, with state IDs and modality availability.`
- `CHECK the pattern-specific domain/context gate.`
- `IF the core context is contradicted: RETURN NO_MATCH.`
- `PARSE the original formula while preserving every AND, OR, NOT, quantifier, and grouping.`
- `EVALUATE each atomic operand only from directly observed evidence.`
- `EVALUATE implication only as an observed premise-and-consequence proof obligation; never use vacuous truth.`
- `IF the formula result is FALSE: RETURN FALSE.`
- `IF direct_counterevidence is observed: RETURN FALSE.`
- `RETURN TRUE.`

Required observable atoms:
- The bundle container and all relevant component-price nodes are identified in the same state.
- The component-price area and bundle area use the same coordinate/viewport basis.
- The stated area ratio is below the threshold.

Direct counterevidence:
- Component prices are visibly and proportionally presented above the threshold.

## DP-016-C3 — Bundling — Semantic Suppression of Individual Item Descriptions

Operational reading: Bundled descriptions are less informative than comparable standalone descriptions

Pseudocode:
- `CONTEXT GATE: identify the rule's specific object/context and mechanism anchor before evaluation.`
- `IF only a generic UI object, generic price, generic CTA, or mere absence is observed: provide no support.`
- `IF a required atom is unavailable: provide no support, preserve the base judgment, and record the evidence as missing.`
- `IF a required atom depends on a UI transition: snapshot the scoped DOM, activate the most direct in-scope control once, wait for rendered settlement, and snapshot again.`
- `IF a visible standard next step is required by the rule: follow one such step and capture the resulting scoped DOM; an unverified click is missing evidence, not a negative.`
- `INPUT evidence_ledger, with state IDs and modality availability.`
- `CHECK the pattern-specific domain/context gate.`
- `IF the core context is contradicted: RETURN NO_MATCH.`
- `PARSE the original formula while preserving every AND, OR, NOT, quantifier, and grouping.`
- `EVALUATE each atomic operand only from directly observed evidence.`
- `EVALUATE implication only as an observed premise-and-consequence proof obligation; never use vacuous truth.`
- `IF the formula result is FALSE: RETURN FALSE.`
- `IF direct_counterevidence is observed: RETURN FALSE.`
- `RETURN TRUE.`

Required observable atoms:
- Bundled and standalone descriptions for equivalent items are identified.
- The same entropy/description representation is available for both sets.
- The ratio is below the stated threshold.

Direct counterevidence:
- Bundled descriptions are comparably informative under the stated representation.

## DP-026-C1 — Hidden Information — Structural Burial in High-Density Text

Operational reading: Evaluate the source formula over directly observed evidence; preserve every Boolean operator, quantifier, comparison, unit, scope, and threshold.

Pseudocode:
- `CONTEXT GATE: identify the rule's specific object/context and mechanism anchor before evaluation.`
- `IF only a generic UI object, generic price, generic CTA, or mere absence is observed: provide no support.`
- `IF a required atom is unavailable: provide no support, preserve the base judgment, and record the evidence as missing.`
- `IF a required atom depends on a UI transition: snapshot the scoped DOM, activate the most direct in-scope control once, wait for rendered settlement, and snapshot again.`
- `IF a visible standard next step is required by the rule: follow one such step and capture the resulting scoped DOM; an unverified click is missing evidence, not a negative.`
- `INPUT evidence_ledger, with state IDs and modality availability.`
- `CHECK the pattern-specific domain/context gate.`
- `IF the core context is contradicted: RETURN NO_MATCH.`
- `PARSE the original formula while preserving every AND, OR, NOT, quantifier, and grouping.`
- `EVALUATE each atomic operand only from directly observed evidence.`
- `EVALUATE implication only as an observed premise-and-consequence proof obligation; never use vacuous truth.`
- `IF the formula result is FALSE: RETURN FALSE.`
- `IF direct_counterevidence is observed: RETURN FALSE.`
- `RETURN TRUE.`

Required observable atoms:
- Every atomic clause in the original formula is identified and evaluated in the stated scope.

Direct counterevidence:
- A directly observed atom contradicts a mandatory requirement.

## DP-026-C2 — Hidden Information — Typographical and Chromatic Camouflage

Operational reading: Evaluate the source formula over directly observed evidence; preserve every Boolean operator, quantifier, comparison, unit, scope, and threshold.

Pseudocode:
- `CONTEXT GATE: identify the rule's specific object/context and mechanism anchor before evaluation.`
- `IF only a generic UI object, generic price, generic CTA, or mere absence is observed: provide no support.`
- `IF a required atom is unavailable: provide no support, preserve the base judgment, and record the evidence as missing.`
- `IF a required atom depends on a UI transition: snapshot the scoped DOM, activate the most direct in-scope control once, wait for rendered settlement, and snapshot again.`
- `IF a visible standard next step is required by the rule: follow one such step and capture the resulting scoped DOM; an unverified click is missing evidence, not a negative.`
- `INPUT evidence_ledger, with state IDs and modality availability.`
- `CHECK the pattern-specific domain/context gate.`
- `IF the core context is contradicted: RETURN NO_MATCH.`
- `PARSE the original formula while preserving every AND, OR, NOT, quantifier, and grouping.`
- `EVALUATE each atomic operand only from directly observed evidence.`
- `EVALUATE implication only as an observed premise-and-consequence proof obligation; never use vacuous truth.`
- `IF the formula result is FALSE: RETURN FALSE.`
- `IF direct_counterevidence is observed: RETURN FALSE.`
- `RETURN TRUE.`

Required observable atoms:
- Every atomic clause in the original formula is identified and evaluated in the stated scope.

Direct counterevidence:
- A directly observed atom contradicts a mandatory requirement.

## DP-026-C3 — Hidden Information — Semantic Concealment of Adverse Terms

Operational reading: Evaluate the source formula over directly observed evidence; preserve every Boolean operator, quantifier, comparison, unit, scope, and threshold.

Pseudocode:
- `CONTEXT GATE: identify the rule's specific object/context and mechanism anchor before evaluation.`
- `IF only a generic UI object, generic price, generic CTA, or mere absence is observed: provide no support.`
- `IF a required atom is unavailable: provide no support, preserve the base judgment, and record the evidence as missing.`
- `IF a required atom depends on a UI transition: snapshot the scoped DOM, activate the most direct in-scope control once, wait for rendered settlement, and snapshot again.`
- `IF a visible standard next step is required by the rule: follow one such step and capture the resulting scoped DOM; an unverified click is missing evidence, not a negative.`
- `INPUT evidence_ledger, with state IDs and modality availability.`
- `CHECK the pattern-specific domain/context gate.`
- `IF the core context is contradicted: RETURN NO_MATCH.`
- `PARSE the original formula while preserving every AND, OR, NOT, quantifier, and grouping.`
- `EVALUATE each atomic operand only from directly observed evidence.`
- `EVALUATE implication only as an observed premise-and-consequence proof obligation; never use vacuous truth.`
- `IF the formula result is FALSE: RETURN FALSE.`
- `IF direct_counterevidence is observed: RETURN FALSE.`
- `RETURN TRUE.`

Required observable atoms:
- Every atomic clause in the original formula is identified and evaluated in the stated scope.

Direct counterevidence:
- A directly observed atom contradicts a mandatory requirement.

## DP-034-C1 — Reduced Friction — Absence of Confirmation Interstitial

Operational reading: Evaluate the source formula over directly observed evidence; preserve every Boolean operator, quantifier, comparison, unit, scope, and threshold.

Pseudocode:
- `CONTEXT GATE: identify the rule's specific object/context and mechanism anchor before evaluation.`
- `IF only a generic UI object, generic price, generic CTA, or mere absence is observed: provide no support.`
- `IF a required atom is unavailable: provide no support, preserve the base judgment, and record the evidence as missing.`
- `IF a required atom depends on a UI transition: snapshot the scoped DOM, activate the most direct in-scope control once, wait for rendered settlement, and snapshot again.`
- `IF a visible standard next step is required by the rule: follow one such step and capture the resulting scoped DOM; an unverified click is missing evidence, not a negative.`
- `INPUT evidence_ledger, with state IDs and modality availability.`
- `CHECK the pattern-specific domain/context gate.`
- `IF the core context is contradicted: RETURN NO_MATCH.`
- `PARSE the original formula while preserving every AND, OR, NOT, quantifier, and grouping.`
- `EVALUATE each atomic operand only from directly observed evidence.`
- `EVALUATE implication only as an observed premise-and-consequence proof obligation; never use vacuous truth.`
- `IF the formula result is FALSE: RETURN FALSE.`
- `IF direct_counterevidence is observed: RETURN FALSE.`
- `RETURN TRUE.`

Required observable atoms:
- Every atomic clause in the original formula is identified and evaluated in the stated scope.

Direct counterevidence:
- A directly observed atom contradicts a mandatory requirement.

## DP-034-C2 — Reduced Friction — Visual Proximity of Destructive Actions to Neutral UI

Operational reading: Evaluate the source formula over directly observed evidence; preserve every Boolean operator, quantifier, comparison, unit, scope, and threshold.

Pseudocode:
- `CONTEXT GATE: identify the rule's specific object/context and mechanism anchor before evaluation.`
- `IF only a generic UI object, generic price, generic CTA, or mere absence is observed: provide no support.`
- `IF a required atom is unavailable: provide no support, preserve the base judgment, and record the evidence as missing.`
- `IF a required atom depends on a UI transition: snapshot the scoped DOM, activate the most direct in-scope control once, wait for rendered settlement, and snapshot again.`
- `IF a visible standard next step is required by the rule: follow one such step and capture the resulting scoped DOM; an unverified click is missing evidence, not a negative.`
- `INPUT evidence_ledger, with state IDs and modality availability.`
- `CHECK the pattern-specific domain/context gate.`
- `IF the core context is contradicted: RETURN NO_MATCH.`
- `PARSE the original formula while preserving every AND, OR, NOT, quantifier, and grouping.`
- `EVALUATE each atomic operand only from directly observed evidence.`
- `EVALUATE implication only as an observed premise-and-consequence proof obligation; never use vacuous truth.`
- `IF the formula result is FALSE: RETURN FALSE.`
- `IF direct_counterevidence is observed: RETURN FALSE.`
- `RETURN TRUE.`

Required observable atoms:
- Every atomic clause in the original formula is identified and evaluated in the stated scope.

Direct counterevidence:
- A directly observed atom contradicts a mandatory requirement.

## DP-034-C3 — Reduced Friction — Semantic Absence of Confirmation Language

Operational reading: Evaluate the source formula over directly observed evidence; preserve every Boolean operator, quantifier, comparison, unit, scope, and threshold.

Pseudocode:
- `CONTEXT GATE: identify the rule's specific object/context and mechanism anchor before evaluation.`
- `IF only a generic UI object, generic price, generic CTA, or mere absence is observed: provide no support.`
- `IF a required atom is unavailable: provide no support, preserve the base judgment, and record the evidence as missing.`
- `IF a required atom depends on a UI transition: snapshot the scoped DOM, activate the most direct in-scope control once, wait for rendered settlement, and snapshot again.`
- `IF a visible standard next step is required by the rule: follow one such step and capture the resulting scoped DOM; an unverified click is missing evidence, not a negative.`
- `INPUT evidence_ledger, with state IDs and modality availability.`
- `CHECK the pattern-specific domain/context gate.`
- `IF the core context is contradicted: RETURN NO_MATCH.`
- `PARSE the original formula while preserving every AND, OR, NOT, quantifier, and grouping.`
- `EVALUATE each atomic operand only from directly observed evidence.`
- `EVALUATE implication only as an observed premise-and-consequence proof obligation; never use vacuous truth.`
- `IF the formula result is FALSE: RETURN FALSE.`
- `IF direct_counterevidence is observed: RETURN FALSE.`
- `RETURN TRUE.`

Required observable atoms:
- Every atomic clause in the original formula is identified and evaluated in the stated scope.

Direct counterevidence:
- A directly observed atom contradicts a mandatory requirement.

## DP-036-C1 — Forced Continuity — Time-Triggered Silent State Mutation

Operational reading: Evaluate the source formula over directly observed evidence; preserve every Boolean operator, quantifier, comparison, unit, scope, and threshold.

Pseudocode:
- `CONTEXT GATE: identify the rule's specific object/context and mechanism anchor before evaluation.`
- `IF only a generic UI object, generic price, generic CTA, or mere absence is observed: provide no support.`
- `IF a required atom is unavailable: provide no support, preserve the base judgment, and record the evidence as missing.`
- `IF a required atom depends on a UI transition: snapshot the scoped DOM, activate the most direct in-scope control once, wait for rendered settlement, and snapshot again.`
- `IF a visible standard next step is required by the rule: follow one such step and capture the resulting scoped DOM; an unverified click is missing evidence, not a negative.`
- `INPUT evidence_ledger, with state IDs and modality availability.`
- `CHECK the pattern-specific domain/context gate.`
- `IF the core context is contradicted: RETURN NO_MATCH.`
- `PARSE the original formula while preserving every AND, OR, NOT, quantifier, and grouping.`
- `EVALUATE each atomic operand only from directly observed evidence.`
- `EVALUATE implication only as an observed premise-and-consequence proof obligation; never use vacuous truth.`
- `IF the formula result is FALSE: RETURN FALSE.`
- `IF direct_counterevidence is observed: RETURN FALSE.`
- `RETURN TRUE.`

Required observable atoms:
- Every atomic clause in the original formula is identified and evaluated in the stated scope.

Direct counterevidence:
- A directly observed atom contradicts a mandatory requirement.

## DP-036-C2 — Forced Continuity — Absence of Temporal Feedforward

Operational reading: Evaluate the source formula over directly observed evidence; preserve every Boolean operator, quantifier, comparison, unit, scope, and threshold.

Pseudocode:
- `CONTEXT GATE: identify the rule's specific object/context and mechanism anchor before evaluation.`
- `IF only a generic UI object, generic price, generic CTA, or mere absence is observed: provide no support.`
- `IF a required atom is unavailable: provide no support, preserve the base judgment, and record the evidence as missing.`
- `IF a required atom depends on a UI transition: snapshot the scoped DOM, activate the most direct in-scope control once, wait for rendered settlement, and snapshot again.`
- `IF a visible standard next step is required by the rule: follow one such step and capture the resulting scoped DOM; an unverified click is missing evidence, not a negative.`
- `INPUT evidence_ledger, with state IDs and modality availability.`
- `CHECK the pattern-specific domain/context gate.`
- `IF the core context is contradicted: RETURN NO_MATCH.`
- `PARSE the original formula while preserving every AND, OR, NOT, quantifier, and grouping.`
- `EVALUATE each atomic operand only from directly observed evidence.`
- `EVALUATE implication only as an observed premise-and-consequence proof obligation; never use vacuous truth.`
- `IF the formula result is FALSE: RETURN FALSE.`
- `IF direct_counterevidence is observed: RETURN FALSE.`
- `RETURN TRUE.`

Required observable atoms:
- Every atomic clause in the original formula is identified and evaluated in the stated scope.

Direct counterevidence:
- A directly observed atom contradicts a mandatory requirement.

## DP-036-C3 — Forced Continuity — Semantic Asymmetry Between Subscription and Cancellation Language

Operational reading: Evaluate the source formula over directly observed evidence; preserve every Boolean operator, quantifier, comparison, unit, scope, and threshold.

Pseudocode:
- `CONTEXT GATE: identify the rule's specific object/context and mechanism anchor before evaluation.`
- `IF only a generic UI object, generic price, generic CTA, or mere absence is observed: provide no support.`
- `IF a required atom is unavailable: provide no support, preserve the base judgment, and record the evidence as missing.`
- `IF a required atom depends on a UI transition: snapshot the scoped DOM, activate the most direct in-scope control once, wait for rendered settlement, and snapshot again.`
- `IF a visible standard next step is required by the rule: follow one such step and capture the resulting scoped DOM; an unverified click is missing evidence, not a negative.`
- `INPUT evidence_ledger, with state IDs and modality availability.`
- `CHECK the pattern-specific domain/context gate.`
- `IF the core context is contradicted: RETURN NO_MATCH.`
- `PARSE the original formula while preserving every AND, OR, NOT, quantifier, and grouping.`
- `EVALUATE each atomic operand only from directly observed evidence.`
- `EVALUATE implication only as an observed premise-and-consequence proof obligation; never use vacuous truth.`
- `IF the formula result is FALSE: RETURN FALSE.`
- `IF direct_counterevidence is observed: RETURN FALSE.`
- `RETURN TRUE.`

Required observable atoms:
- Every atomic clause in the original formula is identified and evaluated in the stated scope.

Direct counterevidence:
- A directly observed atom contradicts a mandatory requirement.

## DP-038-C1 — Privacy Zuckering — Bundled Consent and Granularity Violation

Operational reading: Evaluate the source formula over directly observed evidence; preserve every Boolean operator, quantifier, comparison, unit, scope, and threshold.

Pseudocode:
- `CONTEXT GATE: identify the rule's specific object/context and mechanism anchor before evaluation.`
- `IF only a generic UI object, generic price, generic CTA, or mere absence is observed: provide no support.`
- `IF a required atom is unavailable: provide no support, preserve the base judgment, and record the evidence as missing.`
- `IF a required atom depends on a UI transition: snapshot the scoped DOM, activate the most direct in-scope control once, wait for rendered settlement, and snapshot again.`
- `IF a visible standard next step is required by the rule: follow one such step and capture the resulting scoped DOM; an unverified click is missing evidence, not a negative.`
- `INPUT evidence_ledger, with state IDs and modality availability.`
- `CHECK the pattern-specific domain/context gate.`
- `IF the core context is contradicted: RETURN NO_MATCH.`
- `PARSE the original formula while preserving every AND, OR, NOT, quantifier, and grouping.`
- `EVALUATE each atomic operand only from directly observed evidence.`
- `EVALUATE implication only as an observed premise-and-consequence proof obligation; never use vacuous truth.`
- `IF the formula result is FALSE: RETURN FALSE.`
- `IF direct_counterevidence is observed: RETURN FALSE.`
- `RETURN TRUE.`

Required observable atoms:
- Every atomic clause in the original formula is identified and evaluated in the stated scope.

Direct counterevidence:
- A directly observed atom contradicts a mandatory requirement.

## DP-038-C2 — Privacy Zuckering — Visual Asymmetry Between Privacy-Invasive and Privacy-Preserving Options

Operational reading: Evaluate the source formula over directly observed evidence; preserve every Boolean operator, quantifier, comparison, unit, scope, and threshold.

Pseudocode:
- `CONTEXT GATE: identify the rule's specific object/context and mechanism anchor before evaluation.`
- `IF only a generic UI object, generic price, generic CTA, or mere absence is observed: provide no support.`
- `IF a required atom is unavailable: provide no support, preserve the base judgment, and record the evidence as missing.`
- `IF a required atom depends on a UI transition: snapshot the scoped DOM, activate the most direct in-scope control once, wait for rendered settlement, and snapshot again.`
- `IF a visible standard next step is required by the rule: follow one such step and capture the resulting scoped DOM; an unverified click is missing evidence, not a negative.`
- `INPUT evidence_ledger, with state IDs and modality availability.`
- `CHECK the pattern-specific domain/context gate.`
- `IF the core context is contradicted: RETURN NO_MATCH.`
- `PARSE the original formula while preserving every AND, OR, NOT, quantifier, and grouping.`
- `EVALUATE each atomic operand only from directly observed evidence.`
- `EVALUATE implication only as an observed premise-and-consequence proof obligation; never use vacuous truth.`
- `IF the formula result is FALSE: RETURN FALSE.`
- `IF direct_counterevidence is observed: RETURN FALSE.`
- `RETURN TRUE.`

Required observable atoms:
- Every atomic clause in the original formula is identified and evaluated in the stated scope.

Direct counterevidence:
- A directly observed atom contradicts a mandatory requirement.

## DP-038-C3 — Privacy Zuckering — Semantic Ambiguity of Third-Party Entities

Operational reading: Evaluate the source formula over directly observed evidence; preserve every Boolean operator, quantifier, comparison, unit, scope, and threshold.

Pseudocode:
- `CONTEXT GATE: identify the rule's specific object/context and mechanism anchor before evaluation.`
- `IF only a generic UI object, generic price, generic CTA, or mere absence is observed: provide no support.`
- `IF a required atom is unavailable: provide no support, preserve the base judgment, and record the evidence as missing.`
- `IF a required atom depends on a UI transition: snapshot the scoped DOM, activate the most direct in-scope control once, wait for rendered settlement, and snapshot again.`
- `IF a visible standard next step is required by the rule: follow one such step and capture the resulting scoped DOM; an unverified click is missing evidence, not a negative.`
- `INPUT evidence_ledger, with state IDs and modality availability.`
- `CHECK the pattern-specific domain/context gate.`
- `IF the core context is contradicted: RETURN NO_MATCH.`
- `PARSE the original formula while preserving every AND, OR, NOT, quantifier, and grouping.`
- `EVALUATE each atomic operand only from directly observed evidence.`
- `EVALUATE implication only as an observed premise-and-consequence proof obligation; never use vacuous truth.`
- `IF the formula result is FALSE: RETURN FALSE.`
- `IF direct_counterevidence is observed: RETURN FALSE.`
- `RETURN TRUE.`

Required observable atoms:
- Every atomic clause in the original formula is identified and evaluated in the stated scope.

Direct counterevidence:
- A directly observed atom contradicts a mandatory requirement.

## DP-039-C1 — Friend Spam — Feedforward Intent vs. Payload Execution

Operational reading: Read-only contact intent leads to observed outbound messaging

Pseudocode:
- `CONTEXT GATE: identify the rule's specific object/context and mechanism anchor before evaluation.`
- `IF only a generic UI object, generic price, generic CTA, or mere absence is observed: provide no support.`
- `IF a required atom is unavailable: provide no support, preserve the base judgment, and record the evidence as missing.`
- `IF a required atom depends on a UI transition: snapshot the scoped DOM, activate the most direct in-scope control once, wait for rendered settlement, and snapshot again.`
- `IF a visible standard next step is required by the rule: follow one such step and capture the resulting scoped DOM; an unverified click is missing evidence, not a negative.`
- `INPUT evidence_ledger, with state IDs and modality availability.`
- `CHECK the pattern-specific domain/context gate.`
- `IF the core context is contradicted: RETURN NO_MATCH.`
- `PARSE the original formula while preserving every AND, OR, NOT, quantifier, and grouping.`
- `EVALUATE each atomic operand only from directly observed evidence.`
- `EVALUATE implication only as an observed premise-and-consequence proof obligation; never use vacuous truth.`
- `IF the formula result is FALSE: RETURN FALSE.`
- `IF direct_counterevidence is observed: RETURN FALSE.`
- `RETURN TRUE.`

Required observable atoms:
- The prompt's intended action is read-only matching/searching.
- An outbound message/broadcast action is directly observed after the access event.
- The transition from access to outbound action is evidenced in the aligned state/event record.

Direct counterevidence:
- Only read-only matching occurs and no outbound message action is observed.

## DP-039-C2 — Friend Spam — Absence of Granular Selection

Operational reading: Messages are sent to the contact set without granular selection

Pseudocode:
- `CONTEXT GATE: identify the rule's specific object/context and mechanism anchor before evaluation.`
- `IF only a generic UI object, generic price, generic CTA, or mere absence is observed: provide no support.`
- `IF a required atom is unavailable: provide no support, preserve the base judgment, and record the evidence as missing.`
- `IF a required atom depends on a UI transition: snapshot the scoped DOM, activate the most direct in-scope control once, wait for rendered settlement, and snapshot again.`
- `IF a visible standard next step is required by the rule: follow one such step and capture the resulting scoped DOM; an unverified click is missing evidence, not a negative.`
- `INPUT evidence_ledger, with state IDs and modality availability.`
- `CHECK the pattern-specific domain/context gate.`
- `IF the core context is contradicted: RETURN NO_MATCH.`
- `PARSE the original formula while preserving every AND, OR, NOT, quantifier, and grouping.`
- `EVALUATE each atomic operand only from directly observed evidence.`
- `EVALUATE implication only as an observed premise-and-consequence proof obligation; never use vacuous truth.`
- `IF the formula result is FALSE: RETURN FALSE.`
- `IF direct_counterevidence is observed: RETURN FALSE.`
- `RETURN TRUE.`

Required observable atoms:
- The selection state shows zero explicitly selected contacts.
- The dispatched-message set is observed and approximately covers the extracted contact set.
- The dispatch occurs without an affirmative granular-selection event.

Direct counterevidence:
- The user selects recipients or the dispatch set is limited to selected recipients.

## DP-039-C3 — Friend Spam — Sender Identity Spoofing

Operational reading: Corporate outbound message uses the user's identity as sender alias

Pseudocode:
- `CONTEXT GATE: identify the rule's specific object/context and mechanism anchor before evaluation.`
- `IF only a generic UI object, generic price, generic CTA, or mere absence is observed: provide no support.`
- `IF a required atom is unavailable: provide no support, preserve the base judgment, and record the evidence as missing.`
- `IF a required atom depends on a UI transition: snapshot the scoped DOM, activate the most direct in-scope control once, wait for rendered settlement, and snapshot again.`
- `IF a visible standard next step is required by the rule: follow one such step and capture the resulting scoped DOM; an unverified click is missing evidence, not a negative.`
- `INPUT evidence_ledger, with state IDs and modality availability.`
- `CHECK the pattern-specific domain/context gate.`
- `IF the core context is contradicted: RETURN NO_MATCH.`
- `PARSE the original formula while preserving every AND, OR, NOT, quantifier, and grouping.`
- `EVALUATE each atomic operand only from directly observed evidence.`
- `EVALUATE implication only as an observed premise-and-consequence proof obligation; never use vacuous truth.`
- `IF the formula result is FALSE: RETURN FALSE.`
- `IF direct_counterevidence is observed: RETURN FALSE.`
- `RETURN TRUE.`

Required observable atoms:
- The outbound message's sender alias matches the user's identity vector.
- The message author is identified as the corporate entity.

Direct counterevidence:
- The sender alias accurately identifies the corporate sender.

## DP-040-C1 — Address Book Leeching — Utility-Permission Decoupling

Operational reading: Evaluate the source formula over directly observed evidence; preserve every Boolean operator, quantifier, comparison, unit, scope, and threshold.

Pseudocode:
- `CONTEXT GATE: identify the rule's specific object/context and mechanism anchor before evaluation.`
- `IF only a generic UI object, generic price, generic CTA, or mere absence is observed: provide no support.`
- `IF a required atom is unavailable: provide no support, preserve the base judgment, and record the evidence as missing.`
- `IF a required atom depends on a UI transition: snapshot the scoped DOM, activate the most direct in-scope control once, wait for rendered settlement, and snapshot again.`
- `IF a visible standard next step is required by the rule: follow one such step and capture the resulting scoped DOM; an unverified click is missing evidence, not a negative.`
- `INPUT evidence_ledger, with state IDs and modality availability.`
- `CHECK the pattern-specific domain/context gate.`
- `IF the core context is contradicted: RETURN NO_MATCH.`
- `PARSE the original formula while preserving every AND, OR, NOT, quantifier, and grouping.`
- `EVALUATE each atomic operand only from directly observed evidence.`
- `EVALUATE implication only as an observed premise-and-consequence proof obligation; never use vacuous truth.`
- `IF the formula result is FALSE: RETURN FALSE.`
- `IF direct_counterevidence is observed: RETURN FALSE.`
- `RETURN TRUE.`

Required observable atoms:
- Every atomic clause in the original formula is identified and evaluated in the stated scope.

Direct counterevidence:
- A directly observed atom contradicts a mandatory requirement.

## DP-040-C2 — Address Book Leeching — Visual Prominence of the Invite-All Affordance

Operational reading: Evaluate the source formula over directly observed evidence; preserve every Boolean operator, quantifier, comparison, unit, scope, and threshold.

Pseudocode:
- `CONTEXT GATE: identify the rule's specific object/context and mechanism anchor before evaluation.`
- `IF only a generic UI object, generic price, generic CTA, or mere absence is observed: provide no support.`
- `IF a required atom is unavailable: provide no support, preserve the base judgment, and record the evidence as missing.`
- `IF a required atom depends on a UI transition: snapshot the scoped DOM, activate the most direct in-scope control once, wait for rendered settlement, and snapshot again.`
- `IF a visible standard next step is required by the rule: follow one such step and capture the resulting scoped DOM; an unverified click is missing evidence, not a negative.`
- `INPUT evidence_ledger, with state IDs and modality availability.`
- `CHECK the pattern-specific domain/context gate.`
- `IF the core context is contradicted: RETURN NO_MATCH.`
- `PARSE the original formula while preserving every AND, OR, NOT, quantifier, and grouping.`
- `EVALUATE each atomic operand only from directly observed evidence.`
- `EVALUATE implication only as an observed premise-and-consequence proof obligation; never use vacuous truth.`
- `IF the formula result is FALSE: RETURN FALSE.`
- `IF direct_counterevidence is observed: RETURN FALSE.`
- `RETURN TRUE.`

Required observable atoms:
- Every atomic clause in the original formula is identified and evaluated in the stated scope.

Direct counterevidence:
- A directly observed atom contradicts a mandatory requirement.

## DP-040-C3 — Address Book Leeching — Semantic Framing of Contact-Sharing Consent

Operational reading: Evaluate the source formula over directly observed evidence; preserve every Boolean operator, quantifier, comparison, unit, scope, and threshold.

Pseudocode:
- `CONTEXT GATE: identify the rule's specific object/context and mechanism anchor before evaluation.`
- `IF only a generic UI object, generic price, generic CTA, or mere absence is observed: provide no support.`
- `IF a required atom is unavailable: provide no support, preserve the base judgment, and record the evidence as missing.`
- `IF a required atom depends on a UI transition: snapshot the scoped DOM, activate the most direct in-scope control once, wait for rendered settlement, and snapshot again.`
- `IF a visible standard next step is required by the rule: follow one such step and capture the resulting scoped DOM; an unverified click is missing evidence, not a negative.`
- `INPUT evidence_ledger, with state IDs and modality availability.`
- `CHECK the pattern-specific domain/context gate.`
- `IF the core context is contradicted: RETURN NO_MATCH.`
- `PARSE the original formula while preserving every AND, OR, NOT, quantifier, and grouping.`
- `EVALUATE each atomic operand only from directly observed evidence.`
- `EVALUATE implication only as an observed premise-and-consequence proof obligation; never use vacuous truth.`
- `IF the formula result is FALSE: RETURN FALSE.`
- `IF direct_counterevidence is observed: RETURN FALSE.`
- `RETURN TRUE.`

Required observable atoms:
- Every atomic clause in the original formula is identified and evaluated in the stated scope.

Direct counterevidence:
- A directly observed atom contradicts a mandatory requirement.

## DP-050-C1 — Automatic Accept Third Party Term — Bundled Transitive Consent

Operational reading: Evaluate the source formula over directly observed evidence; preserve every Boolean operator, quantifier, comparison, unit, scope, and threshold.

Pseudocode:
- `CONTEXT GATE: identify the rule's specific object/context and mechanism anchor before evaluation.`
- `IF only a generic UI object, generic price, generic CTA, or mere absence is observed: provide no support.`
- `IF a required atom is unavailable: provide no support, preserve the base judgment, and record the evidence as missing.`
- `IF a required atom depends on a UI transition: snapshot the scoped DOM, activate the most direct in-scope control once, wait for rendered settlement, and snapshot again.`
- `IF a visible standard next step is required by the rule: follow one such step and capture the resulting scoped DOM; an unverified click is missing evidence, not a negative.`
- `INPUT evidence_ledger, with state IDs and modality availability.`
- `CHECK the pattern-specific domain/context gate.`
- `IF the core context is contradicted: RETURN NO_MATCH.`
- `PARSE the original formula while preserving every AND, OR, NOT, quantifier, and grouping.`
- `EVALUATE each atomic operand only from directly observed evidence.`
- `EVALUATE implication only as an observed premise-and-consequence proof obligation; never use vacuous truth.`
- `IF the formula result is FALSE: RETURN FALSE.`
- `IF direct_counterevidence is observed: RETURN FALSE.`
- `RETURN TRUE.`

Required observable atoms:
- Every atomic clause in the original formula is identified and evaluated in the stated scope.

Direct counterevidence:
- A directly observed atom contradicts a mandatory requirement.

## DP-050-C2 — Automatic Accept Third Party Term — Opaque Entity Resolution

Operational reading: Evaluate the source formula over directly observed evidence; preserve every Boolean operator, quantifier, comparison, unit, scope, and threshold.

Pseudocode:
- `CONTEXT GATE: identify the rule's specific object/context and mechanism anchor before evaluation.`
- `IF only a generic UI object, generic price, generic CTA, or mere absence is observed: provide no support.`
- `IF a required atom is unavailable: provide no support, preserve the base judgment, and record the evidence as missing.`
- `IF a required atom depends on a UI transition: snapshot the scoped DOM, activate the most direct in-scope control once, wait for rendered settlement, and snapshot again.`
- `IF a visible standard next step is required by the rule: follow one such step and capture the resulting scoped DOM; an unverified click is missing evidence, not a negative.`
- `INPUT evidence_ledger, with state IDs and modality availability.`
- `CHECK the pattern-specific domain/context gate.`
- `IF the core context is contradicted: RETURN NO_MATCH.`
- `PARSE the original formula while preserving every AND, OR, NOT, quantifier, and grouping.`
- `EVALUATE each atomic operand only from directly observed evidence.`
- `EVALUATE implication only as an observed premise-and-consequence proof obligation; never use vacuous truth.`
- `IF the formula result is FALSE: RETURN FALSE.`
- `IF direct_counterevidence is observed: RETURN FALSE.`
- `RETURN TRUE.`

Required observable atoms:
- Every atomic clause in the original formula is identified and evaluated in the stated scope.

Direct counterevidence:
- A directly observed atom contradicts a mandatory requirement.

## DP-050-C3 — Automatic Accept Third Party Term — Semantic Concealment of Third-Party Agreement Language

Operational reading: Evaluate the source formula over directly observed evidence; preserve every Boolean operator, quantifier, comparison, unit, scope, and threshold.

Pseudocode:
- `CONTEXT GATE: identify the rule's specific object/context and mechanism anchor before evaluation.`
- `IF only a generic UI object, generic price, generic CTA, or mere absence is observed: provide no support.`
- `IF a required atom is unavailable: provide no support, preserve the base judgment, and record the evidence as missing.`
- `IF a required atom depends on a UI transition: snapshot the scoped DOM, activate the most direct in-scope control once, wait for rendered settlement, and snapshot again.`
- `IF a visible standard next step is required by the rule: follow one such step and capture the resulting scoped DOM; an unverified click is missing evidence, not a negative.`
- `INPUT evidence_ledger, with state IDs and modality availability.`
- `CHECK the pattern-specific domain/context gate.`
- `IF the core context is contradicted: RETURN NO_MATCH.`
- `PARSE the original formula while preserving every AND, OR, NOT, quantifier, and grouping.`
- `EVALUATE each atomic operand only from directly observed evidence.`
- `EVALUATE implication only as an observed premise-and-consequence proof obligation; never use vacuous truth.`
- `IF the formula result is FALSE: RETURN FALSE.`
- `IF direct_counterevidence is observed: RETURN FALSE.`
- `RETURN TRUE.`

Required observable atoms:
- Every atomic clause in the original formula is identified and evaluated in the stated scope.

Direct counterevidence:
- A directly observed atom contradicts a mandatory requirement.

## DP-062-C1 — Pre-Delivered Content — Unconsented Local Storage Consumption

Operational reading: Premium content is locally present without consent

Pseudocode:
- `CONTEXT GATE: identify the rule's specific object/context and mechanism anchor before evaluation.`
- `IF only a generic UI object, generic price, generic CTA, or mere absence is observed: provide no support.`
- `IF a required atom is unavailable: provide no support, preserve the base judgment, and record the evidence as missing.`
- `IF a required atom depends on a UI transition: snapshot the scoped DOM, activate the most direct in-scope control once, wait for rendered settlement, and snapshot again.`
- `IF a visible standard next step is required by the rule: follow one such step and capture the resulting scoped DOM; an unverified click is missing evidence, not a negative.`
- `INPUT evidence_ledger, with state IDs and modality availability.`
- `CHECK the pattern-specific domain/context gate.`
- `IF the core context is contradicted: RETURN NO_MATCH.`
- `PARSE the original formula while preserving every AND, OR, NOT, quantifier, and grouping.`
- `EVALUATE each atomic operand only from directly observed evidence.`
- `EVALUATE implication only as an observed premise-and-consequence proof obligation; never use vacuous truth.`
- `IF the formula result is FALSE: RETURN FALSE.`
- `IF direct_counterevidence is observed: RETURN FALSE.`
- `RETURN TRUE.`

Required observable atoms:
- The premium content is established as present in local storage.
- No explicit consent/request to install that content is observed.
- The content size is materially greater than zero.

Direct counterevidence:
- The content was explicitly requested or is not locally present.

## DP-062-C2 — Pre-Delivered Content — Visual Density of Locked-Content Badges

Operational reading: Locked-content nodes dominate accessible nodes

Pseudocode:
- `CONTEXT GATE: identify the rule's specific object/context and mechanism anchor before evaluation.`
- `IF only a generic UI object, generic price, generic CTA, or mere absence is observed: provide no support.`
- `IF a required atom is unavailable: provide no support, preserve the base judgment, and record the evidence as missing.`
- `IF a required atom depends on a UI transition: snapshot the scoped DOM, activate the most direct in-scope control once, wait for rendered settlement, and snapshot again.`
- `IF a visible standard next step is required by the rule: follow one such step and capture the resulting scoped DOM; an unverified click is missing evidence, not a negative.`
- `INPUT evidence_ledger, with state IDs and modality availability.`
- `CHECK the pattern-specific domain/context gate.`
- `IF the core context is contradicted: RETURN NO_MATCH.`
- `PARSE the original formula while preserving every AND, OR, NOT, quantifier, and grouping.`
- `EVALUATE each atomic operand only from directly observed evidence.`
- `EVALUATE implication only as an observed premise-and-consequence proof obligation; never use vacuous truth.`
- `IF the formula result is FALSE: RETURN FALSE.`
- `IF direct_counterevidence is observed: RETURN FALSE.`
- `RETURN TRUE.`

Required observable atoms:
- Locked and accessible content nodes are countable in the same inspected scope.
- The accessible-node denominator is nonzero.
- The stated locked/accessibility ratio exceeds the threshold.

Direct counterevidence:
- The ratio does not exceed the threshold.

## DP-062-C3 — Pre-Delivered Content — Semantic Framing of Local Assets as Purchase Opportunities

Operational reading: Locally present assets are framed as purchasable

Pseudocode:
- `CONTEXT GATE: identify the rule's specific object/context and mechanism anchor before evaluation.`
- `IF only a generic UI object, generic price, generic CTA, or mere absence is observed: provide no support.`
- `IF a required atom is unavailable: provide no support, preserve the base judgment, and record the evidence as missing.`
- `IF a required atom depends on a UI transition: snapshot the scoped DOM, activate the most direct in-scope control once, wait for rendered settlement, and snapshot again.`
- `IF a visible standard next step is required by the rule: follow one such step and capture the resulting scoped DOM; an unverified click is missing evidence, not a negative.`
- `INPUT evidence_ledger, with state IDs and modality availability.`
- `CHECK the pattern-specific domain/context gate.`
- `IF the core context is contradicted: RETURN NO_MATCH.`
- `PARSE the original formula while preserving every AND, OR, NOT, quantifier, and grouping.`
- `EVALUATE each atomic operand only from directly observed evidence.`
- `EVALUATE implication only as an observed premise-and-consequence proof obligation; never use vacuous truth.`
- `IF the formula result is FALSE: RETURN FALSE.`
- `IF direct_counterevidence is observed: RETURN FALSE.`
- `RETURN TRUE.`

Required observable atoms:
- The asset is established as locally present.
- The asset text frames the asset as purchasable/unlockable/downloadable.

Direct counterevidence:
- The asset is remote/not present locally or is described as already included.

## DP-023-C1 — Fear Of Missing Out (FOMO) — Artificial Temporal Scarcity

Operational reading: Temporal scarcity is synthetic only when the displayed duration resets across independently observed fresh sessions rather than following a stable external deadline.

Pseudocode:
- `CONTEXT GATE: identify the rule's specific object/context and mechanism anchor before evaluation.`
- `IF only a generic UI object, generic price, generic CTA, or mere absence is observed: provide no support.`
- `IF a required atom is unavailable: provide no support, preserve the base judgment, and record the evidence as missing.`
- `IF a required atom depends on a UI transition: snapshot the scoped DOM, activate the most direct in-scope control once, wait for rendered settlement, and snapshot again.`
- `IF a visible standard next step is required by the rule: follow one such step and capture the resulting scoped DOM; an unverified click is missing evidence, not a negative.`
- `INPUT evidence_ledger, with state IDs and modality availability.`
- `CHECK the pattern-specific domain/context gate.`
- `IF the core context is contradicted: RETURN NO_MATCH.`
- `PARSE the original formula while preserving every AND, OR, NOT, quantifier, and grouping.`
- `EVALUATE each atomic operand only from directly observed evidence.`
- `EVALUATE implication only as an observed premise-and-consequence proof obligation; never use vacuous truth.`
- `IF the formula result is FALSE: RETURN FALSE.`
- `IF direct_counterevidence is observed: RETURN FALSE.`
- `RETURN TRUE.`

Required observable atoms:
- The same timer or scarcity value is observed in two independently initialized sessions.
- Elapsed monotonic time and the two session-load states are recorded or directly comparable.
- The displayed duration resets or is rebound to the new session load.

Direct counterevidence:
- The value follows one stable external deadline across the observed sessions.

## DP-023-C2 — Fear Of Missing Out (FOMO) — Visual Pulsation Frequency of Urgency Indicators

Operational reading: Evaluate the source formula over directly observed evidence; preserve every Boolean operator, quantifier, comparison, unit, scope, and threshold.

Pseudocode:
- `CONTEXT GATE: identify the rule's specific object/context and mechanism anchor before evaluation.`
- `IF only a generic UI object, generic price, generic CTA, or mere absence is observed: provide no support.`
- `IF a required atom is unavailable: provide no support, preserve the base judgment, and record the evidence as missing.`
- `IF a required atom depends on a UI transition: snapshot the scoped DOM, activate the most direct in-scope control once, wait for rendered settlement, and snapshot again.`
- `IF a visible standard next step is required by the rule: follow one such step and capture the resulting scoped DOM; an unverified click is missing evidence, not a negative.`
- `INPUT evidence_ledger, with state IDs and modality availability.`
- `CHECK the pattern-specific domain/context gate.`
- `IF the core context is contradicted: RETURN NO_MATCH.`
- `PARSE the original formula while preserving every AND, OR, NOT, quantifier, and grouping.`
- `EVALUATE each atomic operand only from directly observed evidence.`
- `EVALUATE implication only as an observed premise-and-consequence proof obligation; never use vacuous truth.`
- `IF the formula result is FALSE: RETURN FALSE.`
- `IF direct_counterevidence is observed: RETURN FALSE.`
- `RETURN TRUE.`

Required observable atoms:
- Every atomic clause in the original formula is identified and evaluated in the stated scope.

Direct counterevidence:
- A directly observed atom contradicts a mandatory requirement.

## DP-023-C3 — Fear Of Missing Out (FOMO) — Semantic Density of Scarcity and Urgency Lexemes

Operational reading: Evaluate the source formula over directly observed evidence; preserve every Boolean operator, quantifier, comparison, unit, scope, and threshold.

Pseudocode:
- `CONTEXT GATE: identify the rule's specific object/context and mechanism anchor before evaluation.`
- `IF only a generic UI object, generic price, generic CTA, or mere absence is observed: provide no support.`
- `IF a required atom is unavailable: provide no support, preserve the base judgment, and record the evidence as missing.`
- `IF a required atom depends on a UI transition: snapshot the scoped DOM, activate the most direct in-scope control once, wait for rendered settlement, and snapshot again.`
- `IF a visible standard next step is required by the rule: follow one such step and capture the resulting scoped DOM; an unverified click is missing evidence, not a negative.`
- `INPUT evidence_ledger, with state IDs and modality availability.`
- `CHECK the pattern-specific domain/context gate.`
- `IF the core context is contradicted: RETURN NO_MATCH.`
- `PARSE the original formula while preserving every AND, OR, NOT, quantifier, and grouping.`
- `EVALUATE each atomic operand only from directly observed evidence.`
- `EVALUATE implication only as an observed premise-and-consequence proof obligation; never use vacuous truth.`
- `IF the formula result is FALSE: RETURN FALSE.`
- `IF direct_counterevidence is observed: RETURN FALSE.`
- `RETURN TRUE.`

Required observable atoms:
- Every atomic clause in the original formula is identified and evaluated in the stated scope.

Direct counterevidence:
- A directly observed atom contradicts a mandatory requirement.

## DP-052-C1 — High Demand — Metric Fabrication

Operational reading: Evaluate the source formula over directly observed evidence; preserve every Boolean operator, quantifier, comparison, unit, scope, and threshold.

Pseudocode:
- `CONTEXT GATE: identify the rule's specific object/context and mechanism anchor before evaluation.`
- `IF only a generic UI object, generic price, generic CTA, or mere absence is observed: provide no support.`
- `IF a required atom is unavailable: provide no support, preserve the base judgment, and record the evidence as missing.`
- `IF a required atom depends on a UI transition: snapshot the scoped DOM, activate the most direct in-scope control once, wait for rendered settlement, and snapshot again.`
- `IF a visible standard next step is required by the rule: follow one such step and capture the resulting scoped DOM; an unverified click is missing evidence, not a negative.`
- `INPUT evidence_ledger, with state IDs and modality availability.`
- `CHECK the pattern-specific domain/context gate.`
- `IF the core context is contradicted: RETURN NO_MATCH.`
- `PARSE the original formula while preserving every AND, OR, NOT, quantifier, and grouping.`
- `EVALUATE each atomic operand only from directly observed evidence.`
- `EVALUATE implication only as an observed premise-and-consequence proof obligation; never use vacuous truth.`
- `IF the formula result is FALSE: RETURN FALSE.`
- `IF direct_counterevidence is observed: RETURN FALSE.`
- `RETURN TRUE.`

Required observable atoms:
- Every atomic clause in the original formula is identified and evaluated in the stated scope.

Direct counterevidence:
- A directly observed atom contradicts a mandatory requirement.

## DP-052-C2 — High Demand — Visual Dynamics of Social-Proof Badges

Operational reading: Evaluate the source formula over directly observed evidence; preserve every Boolean operator, quantifier, comparison, unit, scope, and threshold.

Pseudocode:
- `CONTEXT GATE: identify the rule's specific object/context and mechanism anchor before evaluation.`
- `IF only a generic UI object, generic price, generic CTA, or mere absence is observed: provide no support.`
- `IF a required atom is unavailable: provide no support, preserve the base judgment, and record the evidence as missing.`
- `IF a required atom depends on a UI transition: snapshot the scoped DOM, activate the most direct in-scope control once, wait for rendered settlement, and snapshot again.`
- `IF a visible standard next step is required by the rule: follow one such step and capture the resulting scoped DOM; an unverified click is missing evidence, not a negative.`
- `INPUT evidence_ledger, with state IDs and modality availability.`
- `CHECK the pattern-specific domain/context gate.`
- `IF the core context is contradicted: RETURN NO_MATCH.`
- `PARSE the original formula while preserving every AND, OR, NOT, quantifier, and grouping.`
- `EVALUATE each atomic operand only from directly observed evidence.`
- `EVALUATE implication only as an observed premise-and-consequence proof obligation; never use vacuous truth.`
- `IF the formula result is FALSE: RETURN FALSE.`
- `IF direct_counterevidence is observed: RETURN FALSE.`
- `RETURN TRUE.`

Required observable atoms:
- Every atomic clause in the original formula is identified and evaluated in the stated scope.

Direct counterevidence:
- A directly observed atom contradicts a mandatory requirement.

## DP-052-C3 — High Demand — Semantic Verifiability of Social-Proof Quantifiers

Operational reading: Evaluate the source formula over directly observed evidence; preserve every Boolean operator, quantifier, comparison, unit, scope, and threshold.

Pseudocode:
- `CONTEXT GATE: identify the rule's specific object/context and mechanism anchor before evaluation.`
- `IF only a generic UI object, generic price, generic CTA, or mere absence is observed: provide no support.`
- `IF a required atom is unavailable: provide no support, preserve the base judgment, and record the evidence as missing.`
- `IF a required atom depends on a UI transition: snapshot the scoped DOM, activate the most direct in-scope control once, wait for rendered settlement, and snapshot again.`
- `IF a visible standard next step is required by the rule: follow one such step and capture the resulting scoped DOM; an unverified click is missing evidence, not a negative.`
- `INPUT evidence_ledger, with state IDs and modality availability.`
- `CHECK the pattern-specific domain/context gate.`
- `IF the core context is contradicted: RETURN NO_MATCH.`
- `PARSE the original formula while preserving every AND, OR, NOT, quantifier, and grouping.`
- `EVALUATE each atomic operand only from directly observed evidence.`
- `EVALUATE implication only as an observed premise-and-consequence proof obligation; never use vacuous truth.`
- `IF the formula result is FALSE: RETURN FALSE.`
- `IF direct_counterevidence is observed: RETURN FALSE.`
- `RETURN TRUE.`

Required observable atoms:
- Every atomic clause in the original formula is identified and evaluated in the stated scope.

Direct counterevidence:
- A directly observed atom contradicts a mandatory requirement.

## DP-053-C1 — Low Stock — Inventory Fabrication

Operational reading: Evaluate the source formula over directly observed evidence; preserve every Boolean operator, quantifier, comparison, unit, scope, and threshold.

Pseudocode:
- `CONTEXT GATE: identify the rule's specific object/context and mechanism anchor before evaluation.`
- `IF only a generic UI object, generic price, generic CTA, or mere absence is observed: provide no support.`
- `IF a required atom is unavailable: provide no support, preserve the base judgment, and record the evidence as missing.`
- `IF a required atom depends on a UI transition: snapshot the scoped DOM, activate the most direct in-scope control once, wait for rendered settlement, and snapshot again.`
- `IF a visible standard next step is required by the rule: follow one such step and capture the resulting scoped DOM; an unverified click is missing evidence, not a negative.`
- `INPUT evidence_ledger, with state IDs and modality availability.`
- `CHECK the pattern-specific domain/context gate.`
- `IF the core context is contradicted: RETURN NO_MATCH.`
- `PARSE the original formula while preserving every AND, OR, NOT, quantifier, and grouping.`
- `EVALUATE each atomic operand only from directly observed evidence.`
- `EVALUATE implication only as an observed premise-and-consequence proof obligation; never use vacuous truth.`
- `IF the formula result is FALSE: RETURN FALSE.`
- `IF direct_counterevidence is observed: RETURN FALSE.`
- `RETURN TRUE.`

Required observable atoms:
- Every atomic clause in the original formula is identified and evaluated in the stated scope.

Direct counterevidence:
- A directly observed atom contradicts a mandatory requirement.

## DP-053-C2 — Low Stock — Visual Alarm Salience of Scarcity Indicators

Operational reading: Evaluate the source formula over directly observed evidence; preserve every Boolean operator, quantifier, comparison, unit, scope, and threshold.

Pseudocode:
- `CONTEXT GATE: identify the rule's specific object/context and mechanism anchor before evaluation.`
- `IF only a generic UI object, generic price, generic CTA, or mere absence is observed: provide no support.`
- `IF a required atom is unavailable: provide no support, preserve the base judgment, and record the evidence as missing.`
- `IF a required atom depends on a UI transition: snapshot the scoped DOM, activate the most direct in-scope control once, wait for rendered settlement, and snapshot again.`
- `IF a visible standard next step is required by the rule: follow one such step and capture the resulting scoped DOM; an unverified click is missing evidence, not a negative.`
- `INPUT evidence_ledger, with state IDs and modality availability.`
- `CHECK the pattern-specific domain/context gate.`
- `IF the core context is contradicted: RETURN NO_MATCH.`
- `PARSE the original formula while preserving every AND, OR, NOT, quantifier, and grouping.`
- `EVALUATE each atomic operand only from directly observed evidence.`
- `EVALUATE implication only as an observed premise-and-consequence proof obligation; never use vacuous truth.`
- `IF the formula result is FALSE: RETURN FALSE.`
- `IF direct_counterevidence is observed: RETURN FALSE.`
- `RETURN TRUE.`

Required observable atoms:
- Every atomic clause in the original formula is identified and evaluated in the stated scope.

Direct counterevidence:
- A directly observed atom contradicts a mandatory requirement.

## DP-053-C3 — Low Stock — Semantic Verifiability of Stock-Level Quantifiers

Operational reading: Evaluate the source formula over directly observed evidence; preserve every Boolean operator, quantifier, comparison, unit, scope, and threshold.

Pseudocode:
- `CONTEXT GATE: identify the rule's specific object/context and mechanism anchor before evaluation.`
- `IF only a generic UI object, generic price, generic CTA, or mere absence is observed: provide no support.`
- `IF a required atom is unavailable: provide no support, preserve the base judgment, and record the evidence as missing.`
- `IF a required atom depends on a UI transition: snapshot the scoped DOM, activate the most direct in-scope control once, wait for rendered settlement, and snapshot again.`
- `IF a visible standard next step is required by the rule: follow one such step and capture the resulting scoped DOM; an unverified click is missing evidence, not a negative.`
- `INPUT evidence_ledger, with state IDs and modality availability.`
- `CHECK the pattern-specific domain/context gate.`
- `IF the core context is contradicted: RETURN NO_MATCH.`
- `PARSE the original formula while preserving every AND, OR, NOT, quantifier, and grouping.`
- `EVALUATE each atomic operand only from directly observed evidence.`
- `EVALUATE implication only as an observed premise-and-consequence proof obligation; never use vacuous truth.`
- `IF the formula result is FALSE: RETURN FALSE.`
- `IF direct_counterevidence is observed: RETURN FALSE.`
- `RETURN TRUE.`

Required observable atoms:
- Every atomic clause in the original formula is identified and evaluated in the stated scope.

Direct counterevidence:
- A directly observed atom contradicts a mandatory requirement.

## DP-056-C1 — Activity Messages — Asynchronous Event Fabrication

Operational reading: Evaluate the source formula over directly observed evidence; preserve every Boolean operator, quantifier, comparison, unit, scope, and threshold.

Pseudocode:
- `CONTEXT GATE: identify the rule's specific object/context and mechanism anchor before evaluation.`
- `IF only a generic UI object, generic price, generic CTA, or mere absence is observed: provide no support.`
- `IF a required atom is unavailable: provide no support, preserve the base judgment, and record the evidence as missing.`
- `IF a required atom depends on a UI transition: snapshot the scoped DOM, activate the most direct in-scope control once, wait for rendered settlement, and snapshot again.`
- `IF a visible standard next step is required by the rule: follow one such step and capture the resulting scoped DOM; an unverified click is missing evidence, not a negative.`
- `INPUT evidence_ledger, with state IDs and modality availability.`
- `CHECK the pattern-specific domain/context gate.`
- `IF the core context is contradicted: RETURN NO_MATCH.`
- `PARSE the original formula while preserving every AND, OR, NOT, quantifier, and grouping.`
- `EVALUATE each atomic operand only from directly observed evidence.`
- `EVALUATE implication only as an observed premise-and-consequence proof obligation; never use vacuous truth.`
- `IF the formula result is FALSE: RETURN FALSE.`
- `IF direct_counterevidence is observed: RETURN FALSE.`
- `RETURN TRUE.`

Required observable atoms:
- Every atomic clause in the original formula is identified and evaluated in the stated scope.

Direct counterevidence:
- A directly observed atom contradicts a mandatory requirement.

## DP-056-C2 — Activity Messages — Cognitive Interruption

Operational reading: Evaluate the source formula over directly observed evidence; preserve every Boolean operator, quantifier, comparison, unit, scope, and threshold.

Pseudocode:
- `CONTEXT GATE: identify the rule's specific object/context and mechanism anchor before evaluation.`
- `IF only a generic UI object, generic price, generic CTA, or mere absence is observed: provide no support.`
- `IF a required atom is unavailable: provide no support, preserve the base judgment, and record the evidence as missing.`
- `IF a required atom depends on a UI transition: snapshot the scoped DOM, activate the most direct in-scope control once, wait for rendered settlement, and snapshot again.`
- `IF a visible standard next step is required by the rule: follow one such step and capture the resulting scoped DOM; an unverified click is missing evidence, not a negative.`
- `INPUT evidence_ledger, with state IDs and modality availability.`
- `CHECK the pattern-specific domain/context gate.`
- `IF the core context is contradicted: RETURN NO_MATCH.`
- `PARSE the original formula while preserving every AND, OR, NOT, quantifier, and grouping.`
- `EVALUATE each atomic operand only from directly observed evidence.`
- `EVALUATE implication only as an observed premise-and-consequence proof obligation; never use vacuous truth.`
- `IF the formula result is FALSE: RETURN FALSE.`
- `IF direct_counterevidence is observed: RETURN FALSE.`
- `RETURN TRUE.`

Required observable atoms:
- Every atomic clause in the original formula is identified and evaluated in the stated scope.

Direct counterevidence:
- A directly observed atom contradicts a mandatory requirement.

## DP-056-C3 — Activity Messages — Semantic Specificity of Activity-Notification Content

Operational reading: Evaluate the source formula over directly observed evidence; preserve every Boolean operator, quantifier, comparison, unit, scope, and threshold.

Pseudocode:
- `CONTEXT GATE: identify the rule's specific object/context and mechanism anchor before evaluation.`
- `IF only a generic UI object, generic price, generic CTA, or mere absence is observed: provide no support.`
- `IF a required atom is unavailable: provide no support, preserve the base judgment, and record the evidence as missing.`
- `IF a required atom depends on a UI transition: snapshot the scoped DOM, activate the most direct in-scope control once, wait for rendered settlement, and snapshot again.`
- `IF a visible standard next step is required by the rule: follow one such step and capture the resulting scoped DOM; an unverified click is missing evidence, not a negative.`
- `INPUT evidence_ledger, with state IDs and modality availability.`
- `CHECK the pattern-specific domain/context gate.`
- `IF the core context is contradicted: RETURN NO_MATCH.`
- `PARSE the original formula while preserving every AND, OR, NOT, quantifier, and grouping.`
- `EVALUATE each atomic operand only from directly observed evidence.`
- `EVALUATE implication only as an observed premise-and-consequence proof obligation; never use vacuous truth.`
- `IF the formula result is FALSE: RETURN FALSE.`
- `IF direct_counterevidence is observed: RETURN FALSE.`
- `RETURN TRUE.`

Required observable atoms:
- Every atomic clause in the original formula is identified and evaluated in the stated scope.

Direct counterevidence:
- A directly observed atom contradicts a mandatory requirement.

## DP-057-C1 — Countdown Timer — Fresh-Load Reset of Expiration

Operational reading: A countdown is synthetic only when its expiry is tied to local session load and resets or rebinds across independently observed fresh loads.

Pseudocode:
- `CONTEXT GATE: identify the rule's specific object/context and mechanism anchor before evaluation.`
- `IF only a generic UI object, generic price, generic CTA, or mere absence is observed: provide no support.`
- `IF a required atom is unavailable: provide no support, preserve the base judgment, and record the evidence as missing.`
- `IF a required atom depends on a UI transition: snapshot the scoped DOM, activate the most direct in-scope control once, wait for rendered settlement, and snapshot again.`
- `IF a visible standard next step is required by the rule: follow one such step and capture the resulting scoped DOM; an unverified click is missing evidence, not a negative.`
- `INPUT evidence_ledger, with state IDs and modality availability.`
- `CHECK the pattern-specific domain/context gate.`
- `IF the core context is contradicted: RETURN NO_MATCH.`
- `PARSE the original formula while preserving every AND, OR, NOT, quantifier, and grouping.`
- `EVALUATE each atomic operand only from directly observed evidence.`
- `EVALUATE implication only as an observed premise-and-consequence proof obligation; never use vacuous truth.`
- `IF the formula result is FALSE: RETURN FALSE.`
- `IF direct_counterevidence is observed: RETURN FALSE.`
- `RETURN TRUE.`

Required observable atoms:
- A countdown node, session-load timestamp, and displayed expiry are observed in the same capture.
- The expiry is directly observed to be derived from the local session load and displayed duration.
- A fresh-load comparison directly observes reset or rebinding of the displayed expiry.

Direct counterevidence:
- The same externally anchored deadline persists across fresh loads.

## DP-057-C2 — Countdown Timer — Cognitive Compression

Operational reading: Evaluate the source formula over directly observed evidence; preserve every Boolean operator, quantifier, comparison, unit, scope, and threshold.

Pseudocode:
- `CONTEXT GATE: identify the rule's specific object/context and mechanism anchor before evaluation.`
- `IF only a generic UI object, generic price, generic CTA, or mere absence is observed: provide no support.`
- `IF a required atom is unavailable: provide no support, preserve the base judgment, and record the evidence as missing.`
- `IF a required atom depends on a UI transition: snapshot the scoped DOM, activate the most direct in-scope control once, wait for rendered settlement, and snapshot again.`
- `IF a visible standard next step is required by the rule: follow one such step and capture the resulting scoped DOM; an unverified click is missing evidence, not a negative.`
- `INPUT evidence_ledger, with state IDs and modality availability.`
- `CHECK the pattern-specific domain/context gate.`
- `IF the core context is contradicted: RETURN NO_MATCH.`
- `PARSE the original formula while preserving every AND, OR, NOT, quantifier, and grouping.`
- `EVALUATE each atomic operand only from directly observed evidence.`
- `EVALUATE implication only as an observed premise-and-consequence proof obligation; never use vacuous truth.`
- `IF the formula result is FALSE: RETURN FALSE.`
- `IF direct_counterevidence is observed: RETURN FALSE.`
- `RETURN TRUE.`

Required observable atoms:
- Every atomic clause in the original formula is identified and evaluated in the stated scope.

Direct counterevidence:
- A directly observed atom contradicts a mandatory requirement.

## DP-057-C3 — Countdown Timer — Semantic Urgency Inflation via Temporal Lexemes

Operational reading: Evaluate the source formula over directly observed evidence; preserve every Boolean operator, quantifier, comparison, unit, scope, and threshold.

Pseudocode:
- `CONTEXT GATE: identify the rule's specific object/context and mechanism anchor before evaluation.`
- `IF only a generic UI object, generic price, generic CTA, or mere absence is observed: provide no support.`
- `IF a required atom is unavailable: provide no support, preserve the base judgment, and record the evidence as missing.`
- `IF a required atom depends on a UI transition: snapshot the scoped DOM, activate the most direct in-scope control once, wait for rendered settlement, and snapshot again.`
- `IF a visible standard next step is required by the rule: follow one such step and capture the resulting scoped DOM; an unverified click is missing evidence, not a negative.`
- `INPUT evidence_ledger, with state IDs and modality availability.`
- `CHECK the pattern-specific domain/context gate.`
- `IF the core context is contradicted: RETURN NO_MATCH.`
- `PARSE the original formula while preserving every AND, OR, NOT, quantifier, and grouping.`
- `EVALUATE each atomic operand only from directly observed evidence.`
- `EVALUATE implication only as an observed premise-and-consequence proof obligation; never use vacuous truth.`
- `IF the formula result is FALSE: RETURN FALSE.`
- `IF direct_counterevidence is observed: RETURN FALSE.`
- `RETURN TRUE.`

Required observable atoms:
- Every atomic clause in the original formula is identified and evaluated in the stated scope.

Direct counterevidence:
- A directly observed atom contradicts a mandatory requirement.

## DP-058-C1 — Limited Time Message — Perpetual Extension

Operational reading: Evaluate the source formula over directly observed evidence; preserve every Boolean operator, quantifier, comparison, unit, scope, and threshold.

Pseudocode:
- `CONTEXT GATE: identify the rule's specific object/context and mechanism anchor before evaluation.`
- `IF only a generic UI object, generic price, generic CTA, or mere absence is observed: provide no support.`
- `IF a required atom is unavailable: provide no support, preserve the base judgment, and record the evidence as missing.`
- `IF a required atom depends on a UI transition: snapshot the scoped DOM, activate the most direct in-scope control once, wait for rendered settlement, and snapshot again.`
- `IF a visible standard next step is required by the rule: follow one such step and capture the resulting scoped DOM; an unverified click is missing evidence, not a negative.`
- `INPUT evidence_ledger, with state IDs and modality availability.`
- `CHECK the pattern-specific domain/context gate.`
- `IF the core context is contradicted: RETURN NO_MATCH.`
- `PARSE the original formula while preserving every AND, OR, NOT, quantifier, and grouping.`
- `EVALUATE each atomic operand only from directly observed evidence.`
- `EVALUATE implication only as an observed premise-and-consequence proof obligation; never use vacuous truth.`
- `IF the formula result is FALSE: RETURN FALSE.`
- `IF direct_counterevidence is observed: RETURN FALSE.`
- `RETURN TRUE.`

Required observable atoms:
- Every atomic clause in the original formula is identified and evaluated in the stated scope.

Direct counterevidence:
- A directly observed atom contradicts a mandatory requirement.

## DP-058-C2 — Limited Time Message — Visual Salience of Temporal-Urgency Chromatics

Operational reading: Evaluate the source formula over directly observed evidence; preserve every Boolean operator, quantifier, comparison, unit, scope, and threshold.

Pseudocode:
- `CONTEXT GATE: identify the rule's specific object/context and mechanism anchor before evaluation.`
- `IF only a generic UI object, generic price, generic CTA, or mere absence is observed: provide no support.`
- `IF a required atom is unavailable: provide no support, preserve the base judgment, and record the evidence as missing.`
- `IF a required atom depends on a UI transition: snapshot the scoped DOM, activate the most direct in-scope control once, wait for rendered settlement, and snapshot again.`
- `IF a visible standard next step is required by the rule: follow one such step and capture the resulting scoped DOM; an unverified click is missing evidence, not a negative.`
- `INPUT evidence_ledger, with state IDs and modality availability.`
- `CHECK the pattern-specific domain/context gate.`
- `IF the core context is contradicted: RETURN NO_MATCH.`
- `PARSE the original formula while preserving every AND, OR, NOT, quantifier, and grouping.`
- `EVALUATE each atomic operand only from directly observed evidence.`
- `EVALUATE implication only as an observed premise-and-consequence proof obligation; never use vacuous truth.`
- `IF the formula result is FALSE: RETURN FALSE.`
- `IF direct_counterevidence is observed: RETURN FALSE.`
- `RETURN TRUE.`

Required observable atoms:
- Every atomic clause in the original formula is identified and evaluated in the stated scope.

Direct counterevidence:
- A directly observed atom contradicts a mandatory requirement.

## DP-058-C3 — Limited Time Message — Ambiguous Temporal Bounding

Operational reading: Evaluate the source formula over directly observed evidence; preserve every Boolean operator, quantifier, comparison, unit, scope, and threshold.

Pseudocode:
- `CONTEXT GATE: identify the rule's specific object/context and mechanism anchor before evaluation.`
- `IF only a generic UI object, generic price, generic CTA, or mere absence is observed: provide no support.`
- `IF a required atom is unavailable: provide no support, preserve the base judgment, and record the evidence as missing.`
- `IF a required atom depends on a UI transition: snapshot the scoped DOM, activate the most direct in-scope control once, wait for rendered settlement, and snapshot again.`
- `IF a visible standard next step is required by the rule: follow one such step and capture the resulting scoped DOM; an unverified click is missing evidence, not a negative.`
- `INPUT evidence_ledger, with state IDs and modality availability.`
- `CHECK the pattern-specific domain/context gate.`
- `IF the core context is contradicted: RETURN NO_MATCH.`
- `PARSE the original formula while preserving every AND, OR, NOT, quantifier, and grouping.`
- `EVALUATE each atomic operand only from directly observed evidence.`
- `EVALUATE implication only as an observed premise-and-consequence proof obligation; never use vacuous truth.`
- `IF the formula result is FALSE: RETURN FALSE.`
- `IF direct_counterevidence is observed: RETURN FALSE.`
- `RETURN TRUE.`

Required observable atoms:
- Every atomic clause in the original formula is identified and evaluated in the stated scope.

Direct counterevidence:
- A directly observed atom contradicts a mandatory requirement.

## DP-004-C1 — Price Comparison Prevention — Fiat Decoupling

Operational reading: A price is shown without an available fiat conversion

Pseudocode:
- `CONTEXT GATE: identify the rule's specific object/context and mechanism anchor before evaluation.`
- `IF only a generic UI object, generic price, generic CTA, or mere absence is observed: provide no support.`
- `IF a required atom is unavailable: provide no support, preserve the base judgment, and record the evidence as missing.`
- `IF a required atom depends on a UI transition: snapshot the scoped DOM, activate the most direct in-scope control once, wait for rendered settlement, and snapshot again.`
- `IF a visible standard next step is required by the rule: follow one such step and capture the resulting scoped DOM; an unverified click is missing evidence, not a negative.`
- `INPUT evidence_ledger, with state IDs and modality availability.`
- `CHECK the pattern-specific domain/context gate.`
- `IF the core context is contradicted: RETURN NO_MATCH.`
- `PARSE the original formula while preserving every AND, OR, NOT, quantifier, and grouping.`
- `EVALUATE each atomic operand only from directly observed evidence.`
- `EVALUATE implication only as an observed premise-and-consequence proof obligation; never use vacuous truth.`
- `IF the formula result is FALSE: RETURN FALSE.`
- `IF direct_counterevidence is observed: RETURN FALSE.`
- `RETURN TRUE.`

Required observable atoms:
- A nonempty price token is visible in the complete relevant price scope.
- No fiat conversion function/label is present in that same scope.

Direct counterevidence:
- A direct fiat conversion is available and visible in the relevant scope.

## DP-004-C2 — Price Comparison Prevention — Visual Suppression of Unit-Price Information

Operational reading: Unit price is visually subordinate to the headline price

Pseudocode:
- `CONTEXT GATE: identify the rule's specific object/context and mechanism anchor before evaluation.`
- `IF only a generic UI object, generic price, generic CTA, or mere absence is observed: provide no support.`
- `IF a required atom is unavailable: provide no support, preserve the base judgment, and record the evidence as missing.`
- `IF a required atom depends on a UI transition: snapshot the scoped DOM, activate the most direct in-scope control once, wait for rendered settlement, and snapshot again.`
- `IF a visible standard next step is required by the rule: follow one such step and capture the resulting scoped DOM; an unverified click is missing evidence, not a negative.`
- `INPUT evidence_ledger, with state IDs and modality availability.`
- `CHECK the pattern-specific domain/context gate.`
- `IF the core context is contradicted: RETURN NO_MATCH.`
- `PARSE the original formula while preserving every AND, OR, NOT, quantifier, and grouping.`
- `EVALUATE each atomic operand only from directly observed evidence.`
- `EVALUATE implication only as an observed premise-and-consequence proof obligation; never use vacuous truth.`
- `IF the formula result is FALSE: RETURN FALSE.`
- `IF direct_counterevidence is observed: RETURN FALSE.`
- `RETURN TRUE.`

Required observable atoms:
- Unit-price and headline-price nodes are identified in the same state.
- The same font-size/contrast measurements, or an explicit direct visual comparison with direction, are available for both nodes.
- The stated suppression relation is met; qualitative direction is acceptable only when both operands and direction are explicit.

Direct counterevidence:
- Unit-price information meets the stated relative visibility threshold.

## DP-004-C3 — Price Comparison Prevention — Semantic Omission of Comparison-Relevant Qualifiers

Operational reading: Comparison-relevant qualifiers are absent from the rendered price

Pseudocode:
- `CONTEXT GATE: identify the rule's specific object/context and mechanism anchor before evaluation.`
- `IF only a generic UI object, generic price, generic CTA, or mere absence is observed: provide no support.`
- `IF a required atom is unavailable: provide no support, preserve the base judgment, and record the evidence as missing.`
- `IF a required atom depends on a UI transition: snapshot the scoped DOM, activate the most direct in-scope control once, wait for rendered settlement, and snapshot again.`
- `IF a visible standard next step is required by the rule: follow one such step and capture the resulting scoped DOM; an unverified click is missing evidence, not a negative.`
- `INPUT evidence_ledger, with state IDs and modality availability.`
- `CHECK the pattern-specific domain/context gate.`
- `IF the core context is contradicted: RETURN NO_MATCH.`
- `PARSE the original formula while preserving every AND, OR, NOT, quantifier, and grouping.`
- `EVALUATE each atomic operand only from directly observed evidence.`
- `EVALUATE implication only as an observed premise-and-consequence proof obligation; never use vacuous truth.`
- `IF the formula result is FALSE: RETURN FALSE.`
- `IF direct_counterevidence is observed: RETURN FALSE.`
- `RETURN TRUE.`

Required observable atoms:
- The standard qualifier set for this price/product is known from the evidence.
- The rendered qualifier set is complete and comparable.
- Their intersection is empty.

Direct counterevidence:
- At least one relevant qualifier is rendered.

## DP-011-C1 — Reference Pricing — Discount Magnitude and Reference-Claim Support

Operational reading: A large discount is a candidate signal only when the reference-price claim is unsupported, inflated, or misleadingly framed; arithmetic extremity alone is insufficient.

Pseudocode:
- `CONTEXT GATE: identify the rule's specific object/context and mechanism anchor before evaluation.`
- `IF only a generic UI object, generic price, generic CTA, or mere absence is observed: provide no support.`
- `IF a required atom is unavailable: provide no support, preserve the base judgment, and record the evidence as missing.`
- `IF a required atom depends on a UI transition: snapshot the scoped DOM, activate the most direct in-scope control once, wait for rendered settlement, and snapshot again.`
- `IF a visible standard next step is required by the rule: follow one such step and capture the resulting scoped DOM; an unverified click is missing evidence, not a negative.`
- `INPUT evidence_ledger, with state IDs and modality availability.`
- `CHECK the pattern-specific domain/context gate.`
- `IF the core context is contradicted: RETURN NO_MATCH.`
- `PARSE the original formula while preserving every AND, OR, NOT, quantifier, and grouping.`
- `EVALUATE each atomic operand only from directly observed evidence.`
- `EVALUATE implication only as an observed premise-and-consequence proof obligation; never use vacuous truth.`
- `IF the formula result is FALSE: RETURN FALSE.`
- `IF direct_counterevidence is observed: RETURN FALSE.`
- `RETURN TRUE.`

Required observable atoms:
- Current and reference prices are extracted from the same interface state.
- The percentage discount is calculated using the displayed values and the stated threshold.
- Independent interface evidence shows that the reference price is unsupported, misleadingly framed, or materially unrelated to the comparison being made.

Direct counterevidence:
- The reference price is supported by an explicit valid comparison context.

## DP-011-C2 — Reference Pricing — Visual Salience of Reference-Price Strikethrough

Operational reading: Evaluate the source formula over directly observed evidence; preserve every Boolean operator, quantifier, comparison, unit, scope, and threshold.

Pseudocode:
- `CONTEXT GATE: identify the rule's specific object/context and mechanism anchor before evaluation.`
- `IF only a generic UI object, generic price, generic CTA, or mere absence is observed: provide no support.`
- `IF a required atom is unavailable: provide no support, preserve the base judgment, and record the evidence as missing.`
- `IF a required atom depends on a UI transition: snapshot the scoped DOM, activate the most direct in-scope control once, wait for rendered settlement, and snapshot again.`
- `IF a visible standard next step is required by the rule: follow one such step and capture the resulting scoped DOM; an unverified click is missing evidence, not a negative.`
- `INPUT evidence_ledger, with state IDs and modality availability.`
- `CHECK the pattern-specific domain/context gate.`
- `IF the core context is contradicted: RETURN NO_MATCH.`
- `PARSE the original formula while preserving every AND, OR, NOT, quantifier, and grouping.`
- `EVALUATE each atomic operand only from directly observed evidence.`
- `EVALUATE implication only as an observed premise-and-consequence proof obligation; never use vacuous truth.`
- `IF the formula result is FALSE: RETURN FALSE.`
- `IF direct_counterevidence is observed: RETURN FALSE.`
- `RETURN TRUE.`

Required observable atoms:
- Every atomic clause in the original formula is identified and evaluated in the stated scope.

Direct counterevidence:
- A directly observed atom contradicts a mandatory requirement.

## DP-011-C3 — Reference Pricing — Dual-Pricing Co-occurrence and Anchoring

Operational reading: Evaluate the source formula over directly observed evidence; preserve every Boolean operator, quantifier, comparison, unit, scope, and threshold.

Pseudocode:
- `CONTEXT GATE: identify the rule's specific object/context and mechanism anchor before evaluation.`
- `IF only a generic UI object, generic price, generic CTA, or mere absence is observed: provide no support.`
- `IF a required atom is unavailable: provide no support, preserve the base judgment, and record the evidence as missing.`
- `IF a required atom depends on a UI transition: snapshot the scoped DOM, activate the most direct in-scope control once, wait for rendered settlement, and snapshot again.`
- `IF a visible standard next step is required by the rule: follow one such step and capture the resulting scoped DOM; an unverified click is missing evidence, not a negative.`
- `INPUT evidence_ledger, with state IDs and modality availability.`
- `CHECK the pattern-specific domain/context gate.`
- `IF the core context is contradicted: RETURN NO_MATCH.`
- `PARSE the original formula while preserving every AND, OR, NOT, quantifier, and grouping.`
- `EVALUATE each atomic operand only from directly observed evidence.`
- `EVALUATE implication only as an observed premise-and-consequence proof obligation; never use vacuous truth.`
- `IF the formula result is FALSE: RETURN FALSE.`
- `IF direct_counterevidence is observed: RETURN FALSE.`
- `RETURN TRUE.`

Required observable atoms:
- Every atomic clause in the original formula is identified and evaluated in the stated scope.

Direct counterevidence:
- A directly observed atom contradicts a mandatory requirement.

## DP-012-C1 — Conflicting Information — Structural Proximity of Contradictory Factual Nodes

Operational reading: Two nearby factual propositions trigger only when their extracted truth conditions are explicitly mutually unsatisfiable within the same DOM scope.

Pseudocode:
- `CONTEXT GATE: identify the rule's specific object/context and mechanism anchor before evaluation.`
- `IF only a generic UI object, generic price, generic CTA, or mere absence is observed: provide no support.`
- `IF a required atom is unavailable: provide no support, preserve the base judgment, and record the evidence as missing.`
- `IF a required atom depends on a UI transition: snapshot the scoped DOM, activate the most direct in-scope control once, wait for rendered settlement, and snapshot again.`
- `IF a visible standard next step is required by the rule: follow one such step and capture the resulting scoped DOM; an unverified click is missing evidence, not a negative.`
- `INPUT evidence_ledger, with state IDs and modality availability.`
- `CHECK the pattern-specific domain/context gate.`
- `IF the core context is contradicted: RETURN NO_MATCH.`
- `PARSE the original formula while preserving every AND, OR, NOT, quantifier, and grouping.`
- `EVALUATE each atomic operand only from directly observed evidence.`
- `EVALUATE implication only as an observed premise-and-consequence proof obligation; never use vacuous truth.`
- `IF the formula result is FALSE: RETURN FALSE.`
- `IF direct_counterevidence is observed: RETURN FALSE.`
- `RETURN TRUE.`

Required observable atoms:
- Two factual propositions are identified within the same named container.
- Their proposition representations are directly evaluated as mutually unsatisfiable.
- Their DOM distance is below the stated proximity threshold.

Direct counterevidence:
- The propositions are jointly satisfiable or are not in the same relevant scope.

## DP-012-C2 — Conflicting Information — Semantic-Visual Mismatch

Operational reading: A label--affordance mismatch triggers only when both semantic intent and the comparison affordance are directly observed as incompatible under a declared mapping.

Pseudocode:
- `CONTEXT GATE: identify the rule's specific object/context and mechanism anchor before evaluation.`
- `IF only a generic UI object, generic price, generic CTA, or mere absence is observed: provide no support.`
- `IF a required atom is unavailable: provide no support, preserve the base judgment, and record the evidence as missing.`
- `IF a required atom depends on a UI transition: snapshot the scoped DOM, activate the most direct in-scope control once, wait for rendered settlement, and snapshot again.`
- `IF a visible standard next step is required by the rule: follow one such step and capture the resulting scoped DOM; an unverified click is missing evidence, not a negative.`
- `INPUT evidence_ledger, with state IDs and modality availability.`
- `CHECK the pattern-specific domain/context gate.`
- `IF the core context is contradicted: RETURN NO_MATCH.`
- `PARSE the original formula while preserving every AND, OR, NOT, quantifier, and grouping.`
- `EVALUATE each atomic operand only from directly observed evidence.`
- `EVALUATE implication only as an observed premise-and-consequence proof obligation; never use vacuous truth.`
- `IF the formula result is FALSE: RETURN FALSE.`
- `IF direct_counterevidence is observed: RETURN FALSE.`
- `RETURN TRUE.`

Required observable atoms:
- The interactive label and its affordance are identified in the same state.
- The intent and affordance mappings are explicitly defined and directly supported.
- The mapped sets have no compatible intersection.

Direct counterevidence:
- The label and affordance have a compatible interpretation under the declared mapping.

## DP-012-C3 — Conflicting Information — Mutually Exclusive Factual Claims

Operational reading: Two claims trigger only when their extracted semantic constraints form an explicitly unsatisfiable conjunction in one informational container.

Pseudocode:
- `CONTEXT GATE: identify the rule's specific object/context and mechanism anchor before evaluation.`
- `IF only a generic UI object, generic price, generic CTA, or mere absence is observed: provide no support.`
- `IF a required atom is unavailable: provide no support, preserve the base judgment, and record the evidence as missing.`
- `IF a required atom depends on a UI transition: snapshot the scoped DOM, activate the most direct in-scope control once, wait for rendered settlement, and snapshot again.`
- `IF a visible standard next step is required by the rule: follow one such step and capture the resulting scoped DOM; an unverified click is missing evidence, not a negative.`
- `INPUT evidence_ledger, with state IDs and modality availability.`
- `CHECK the pattern-specific domain/context gate.`
- `IF the core context is contradicted: RETURN NO_MATCH.`
- `PARSE the original formula while preserving every AND, OR, NOT, quantifier, and grouping.`
- `EVALUATE each atomic operand only from directly observed evidence.`
- `EVALUATE implication only as an observed premise-and-consequence proof obligation; never use vacuous truth.`
- `IF the formula result is FALSE: RETURN FALSE.`
- `IF direct_counterevidence is observed: RETURN FALSE.`
- `RETURN TRUE.`

Required observable atoms:
- Two truth-bearing claims are identified in the same container.
- Their extracted constraints are directly evaluated as unsatisfiable together.

Direct counterevidence:
- The claims are jointly satisfiable.

## DP-013-C1 — Information Without Context — Structural Orphaned Nodes

Operational reading: Evaluate the source formula over directly observed evidence; preserve every Boolean operator, quantifier, comparison, unit, scope, and threshold.

Pseudocode:
- `CONTEXT GATE: identify the rule's specific object/context and mechanism anchor before evaluation.`
- `IF only a generic UI object, generic price, generic CTA, or mere absence is observed: provide no support.`
- `IF a required atom is unavailable: provide no support, preserve the base judgment, and record the evidence as missing.`
- `IF a required atom depends on a UI transition: snapshot the scoped DOM, activate the most direct in-scope control once, wait for rendered settlement, and snapshot again.`
- `IF a visible standard next step is required by the rule: follow one such step and capture the resulting scoped DOM; an unverified click is missing evidence, not a negative.`
- `INPUT evidence_ledger, with state IDs and modality availability.`
- `CHECK the pattern-specific domain/context gate.`
- `IF the core context is contradicted: RETURN NO_MATCH.`
- `PARSE the original formula while preserving every AND, OR, NOT, quantifier, and grouping.`
- `EVALUATE each atomic operand only from directly observed evidence.`
- `EVALUATE implication only as an observed premise-and-consequence proof obligation; never use vacuous truth.`
- `IF the formula result is FALSE: RETURN FALSE.`
- `IF direct_counterevidence is observed: RETURN FALSE.`
- `RETURN TRUE.`

Required observable atoms:
- Every atomic clause in the original formula is identified and evaluated in the stated scope.

Direct counterevidence:
- A directly observed atom contradicts a mandatory requirement.

## DP-013-C2 — Information Without Context — Visual Prominence Imbalance Between Metric and Baseline

Operational reading: Evaluate the source formula over directly observed evidence; preserve every Boolean operator, quantifier, comparison, unit, scope, and threshold.

Pseudocode:
- `CONTEXT GATE: identify the rule's specific object/context and mechanism anchor before evaluation.`
- `IF only a generic UI object, generic price, generic CTA, or mere absence is observed: provide no support.`
- `IF a required atom is unavailable: provide no support, preserve the base judgment, and record the evidence as missing.`
- `IF a required atom depends on a UI transition: snapshot the scoped DOM, activate the most direct in-scope control once, wait for rendered settlement, and snapshot again.`
- `IF a visible standard next step is required by the rule: follow one such step and capture the resulting scoped DOM; an unverified click is missing evidence, not a negative.`
- `INPUT evidence_ledger, with state IDs and modality availability.`
- `CHECK the pattern-specific domain/context gate.`
- `IF the core context is contradicted: RETURN NO_MATCH.`
- `PARSE the original formula while preserving every AND, OR, NOT, quantifier, and grouping.`
- `EVALUATE each atomic operand only from directly observed evidence.`
- `EVALUATE implication only as an observed premise-and-consequence proof obligation; never use vacuous truth.`
- `IF the formula result is FALSE: RETURN FALSE.`
- `IF direct_counterevidence is observed: RETURN FALSE.`
- `RETURN TRUE.`

Required observable atoms:
- Every atomic clause in the original formula is identified and evaluated in the stated scope.

Direct counterevidence:
- A directly observed atom contradicts a mandatory requirement.

## DP-013-C3 — Information Without Context — Unanchored Quantitative Metrics

Operational reading: Evaluate the source formula over directly observed evidence; preserve every Boolean operator, quantifier, comparison, unit, scope, and threshold.

Pseudocode:
- `CONTEXT GATE: identify the rule's specific object/context and mechanism anchor before evaluation.`
- `IF only a generic UI object, generic price, generic CTA, or mere absence is observed: provide no support.`
- `IF a required atom is unavailable: provide no support, preserve the base judgment, and record the evidence as missing.`
- `IF a required atom depends on a UI transition: snapshot the scoped DOM, activate the most direct in-scope control once, wait for rendered settlement, and snapshot again.`
- `IF a visible standard next step is required by the rule: follow one such step and capture the resulting scoped DOM; an unverified click is missing evidence, not a negative.`
- `INPUT evidence_ledger, with state IDs and modality availability.`
- `CHECK the pattern-specific domain/context gate.`
- `IF the core context is contradicted: RETURN NO_MATCH.`
- `PARSE the original formula while preserving every AND, OR, NOT, quantifier, and grouping.`
- `EVALUATE each atomic operand only from directly observed evidence.`
- `EVALUATE implication only as an observed premise-and-consequence proof obligation; never use vacuous truth.`
- `IF the formula result is FALSE: RETURN FALSE.`
- `IF direct_counterevidence is observed: RETURN FALSE.`
- `RETURN TRUE.`

Required observable atoms:
- Every atomic clause in the original formula is identified and evaluated in the stated scope.

Direct counterevidence:
- A directly observed atom contradicts a mandatory requirement.

## DP-014-C1 — False Hierarchy — Structural Element Downgrading

Operational reading: Evaluate the source formula over directly observed evidence; preserve every Boolean operator, quantifier, comparison, unit, scope, and threshold.

Pseudocode:
- `CONTEXT GATE: identify the rule's specific object/context and mechanism anchor before evaluation.`
- `IF only a generic UI object, generic price, generic CTA, or mere absence is observed: provide no support.`
- `IF a required atom is unavailable: provide no support, preserve the base judgment, and record the evidence as missing.`
- `IF a required atom depends on a UI transition: snapshot the scoped DOM, activate the most direct in-scope control once, wait for rendered settlement, and snapshot again.`
- `IF a visible standard next step is required by the rule: follow one such step and capture the resulting scoped DOM; an unverified click is missing evidence, not a negative.`
- `INPUT evidence_ledger, with state IDs and modality availability.`
- `CHECK the pattern-specific domain/context gate.`
- `IF the core context is contradicted: RETURN NO_MATCH.`
- `PARSE the original formula while preserving every AND, OR, NOT, quantifier, and grouping.`
- `EVALUATE each atomic operand only from directly observed evidence.`
- `EVALUATE implication only as an observed premise-and-consequence proof obligation; never use vacuous truth.`
- `IF the formula result is FALSE: RETURN FALSE.`
- `IF direct_counterevidence is observed: RETURN FALSE.`
- `RETURN TRUE.`

Required observable atoms:
- Every atomic clause in the original formula is identified and evaluated in the stated scope.

Direct counterevidence:
- A directly observed atom contradicts a mandatory requirement.

## DP-014-C2 — False Hierarchy — Relational Visual Weight Disparity

Operational reading: Evaluate the source formula over directly observed evidence; preserve every Boolean operator, quantifier, comparison, unit, scope, and threshold.

Pseudocode:
- `CONTEXT GATE: identify the rule's specific object/context and mechanism anchor before evaluation.`
- `IF only a generic UI object, generic price, generic CTA, or mere absence is observed: provide no support.`
- `IF a required atom is unavailable: provide no support, preserve the base judgment, and record the evidence as missing.`
- `IF a required atom depends on a UI transition: snapshot the scoped DOM, activate the most direct in-scope control once, wait for rendered settlement, and snapshot again.`
- `IF a visible standard next step is required by the rule: follow one such step and capture the resulting scoped DOM; an unverified click is missing evidence, not a negative.`
- `INPUT evidence_ledger, with state IDs and modality availability.`
- `CHECK the pattern-specific domain/context gate.`
- `IF the core context is contradicted: RETURN NO_MATCH.`
- `PARSE the original formula while preserving every AND, OR, NOT, quantifier, and grouping.`
- `EVALUATE each atomic operand only from directly observed evidence.`
- `EVALUATE implication only as an observed premise-and-consequence proof obligation; never use vacuous truth.`
- `IF the formula result is FALSE: RETURN FALSE.`
- `IF direct_counterevidence is observed: RETURN FALSE.`
- `RETURN TRUE.`

Required observable atoms:
- Every atomic clause in the original formula is identified and evaluated in the stated scope.

Direct counterevidence:
- A directly observed atom contradicts a mandatory requirement.

## DP-014-C3 — False Hierarchy — Strict Semantic Opposition

Operational reading: Evaluate the source formula over directly observed evidence; preserve every Boolean operator, quantifier, comparison, unit, scope, and threshold.

Pseudocode:
- `CONTEXT GATE: identify the rule's specific object/context and mechanism anchor before evaluation.`
- `IF only a generic UI object, generic price, generic CTA, or mere absence is observed: provide no support.`
- `IF a required atom is unavailable: provide no support, preserve the base judgment, and record the evidence as missing.`
- `IF a required atom depends on a UI transition: snapshot the scoped DOM, activate the most direct in-scope control once, wait for rendered settlement, and snapshot again.`
- `IF a visible standard next step is required by the rule: follow one such step and capture the resulting scoped DOM; an unverified click is missing evidence, not a negative.`
- `INPUT evidence_ledger, with state IDs and modality availability.`
- `CHECK the pattern-specific domain/context gate.`
- `IF the core context is contradicted: RETURN NO_MATCH.`
- `PARSE the original formula while preserving every AND, OR, NOT, quantifier, and grouping.`
- `EVALUATE each atomic operand only from directly observed evidence.`
- `EVALUATE implication only as an observed premise-and-consequence proof obligation; never use vacuous truth.`
- `IF the formula result is FALSE: RETURN FALSE.`
- `IF direct_counterevidence is observed: RETURN FALSE.`
- `RETURN TRUE.`

Required observable atoms:
- Every atomic clause in the original formula is identified and evaluated in the stated scope.

Direct counterevidence:
- A directly observed atom contradicts a mandatory requirement.

## DP-015-C1 — Visual Prominence — Structural Asymmetry in DOM Subtree Weight

Operational reading: Evaluate the source formula over directly observed evidence; preserve every Boolean operator, quantifier, comparison, unit, scope, and threshold.

Pseudocode:
- `CONTEXT GATE: identify the rule's specific object/context and mechanism anchor before evaluation.`
- `IF only a generic UI object, generic price, generic CTA, or mere absence is observed: provide no support.`
- `IF a required atom is unavailable: provide no support, preserve the base judgment, and record the evidence as missing.`
- `IF a required atom depends on a UI transition: snapshot the scoped DOM, activate the most direct in-scope control once, wait for rendered settlement, and snapshot again.`
- `IF a visible standard next step is required by the rule: follow one such step and capture the resulting scoped DOM; an unverified click is missing evidence, not a negative.`
- `INPUT evidence_ledger, with state IDs and modality availability.`
- `CHECK the pattern-specific domain/context gate.`
- `IF the core context is contradicted: RETURN NO_MATCH.`
- `PARSE the original formula while preserving every AND, OR, NOT, quantifier, and grouping.`
- `EVALUATE each atomic operand only from directly observed evidence.`
- `EVALUATE implication only as an observed premise-and-consequence proof obligation; never use vacuous truth.`
- `IF the formula result is FALSE: RETURN FALSE.`
- `IF direct_counterevidence is observed: RETURN FALSE.`
- `RETURN TRUE.`

Required observable atoms:
- Every atomic clause in the original formula is identified and evaluated in the stated scope.

Direct counterevidence:
- A directly observed atom contradicts a mandatory requirement.

## DP-015-C2 — Visual Prominence — Absolute Bounding Box Dominance

Operational reading: Evaluate the source formula over directly observed evidence; preserve every Boolean operator, quantifier, comparison, unit, scope, and threshold.

Pseudocode:
- `CONTEXT GATE: identify the rule's specific object/context and mechanism anchor before evaluation.`
- `IF only a generic UI object, generic price, generic CTA, or mere absence is observed: provide no support.`
- `IF a required atom is unavailable: provide no support, preserve the base judgment, and record the evidence as missing.`
- `IF a required atom depends on a UI transition: snapshot the scoped DOM, activate the most direct in-scope control once, wait for rendered settlement, and snapshot again.`
- `IF a visible standard next step is required by the rule: follow one such step and capture the resulting scoped DOM; an unverified click is missing evidence, not a negative.`
- `INPUT evidence_ledger, with state IDs and modality availability.`
- `CHECK the pattern-specific domain/context gate.`
- `IF the core context is contradicted: RETURN NO_MATCH.`
- `PARSE the original formula while preserving every AND, OR, NOT, quantifier, and grouping.`
- `EVALUATE each atomic operand only from directly observed evidence.`
- `EVALUATE implication only as an observed premise-and-consequence proof obligation; never use vacuous truth.`
- `IF the formula result is FALSE: RETURN FALSE.`
- `IF direct_counterevidence is observed: RETURN FALSE.`
- `RETURN TRUE.`

Required observable atoms:
- Every atomic clause in the original formula is identified and evaluated in the stated scope.

Direct counterevidence:
- A directly observed atom contradicts a mandatory requirement.

## DP-015-C3 — Visual Prominence — Semantic Emphasis of Dominant Action Labels

Operational reading: Evaluate the source formula over directly observed evidence; preserve every Boolean operator, quantifier, comparison, unit, scope, and threshold.

Pseudocode:
- `CONTEXT GATE: identify the rule's specific object/context and mechanism anchor before evaluation.`
- `IF only a generic UI object, generic price, generic CTA, or mere absence is observed: provide no support.`
- `IF a required atom is unavailable: provide no support, preserve the base judgment, and record the evidence as missing.`
- `IF a required atom depends on a UI transition: snapshot the scoped DOM, activate the most direct in-scope control once, wait for rendered settlement, and snapshot again.`
- `IF a visible standard next step is required by the rule: follow one such step and capture the resulting scoped DOM; an unverified click is missing evidence, not a negative.`
- `INPUT evidence_ledger, with state IDs and modality availability.`
- `CHECK the pattern-specific domain/context gate.`
- `IF the core context is contradicted: RETURN NO_MATCH.`
- `PARSE the original formula while preserving every AND, OR, NOT, quantifier, and grouping.`
- `EVALUATE each atomic operand only from directly observed evidence.`
- `EVALUATE implication only as an observed premise-and-consequence proof obligation; never use vacuous truth.`
- `IF the formula result is FALSE: RETURN FALSE.`
- `IF direct_counterevidence is observed: RETURN FALSE.`
- `RETURN TRUE.`

Required observable atoms:
- Every atomic clause in the original formula is identified and evaluated in the stated scope.

Direct counterevidence:
- A directly observed atom contradicts a mandatory requirement.

## DP-018-C1 — Persuasive Language — Structural Density of Event Listeners on Coercive Text Nodes

Operational reading: Coercive text has greater event-listener density than neutral text

Pseudocode:
- `CONTEXT GATE: identify the rule's specific object/context and mechanism anchor before evaluation.`
- `IF only a generic UI object, generic price, generic CTA, or mere absence is observed: provide no support.`
- `IF a required atom is unavailable: provide no support, preserve the base judgment, and record the evidence as missing.`
- `IF a required atom depends on a UI transition: snapshot the scoped DOM, activate the most direct in-scope control once, wait for rendered settlement, and snapshot again.`
- `IF a visible standard next step is required by the rule: follow one such step and capture the resulting scoped DOM; an unverified click is missing evidence, not a negative.`
- `INPUT evidence_ledger, with state IDs and modality availability.`
- `CHECK the pattern-specific domain/context gate.`
- `IF the core context is contradicted: RETURN NO_MATCH.`
- `PARSE the original formula while preserving every AND, OR, NOT, quantifier, and grouping.`
- `EVALUATE each atomic operand only from directly observed evidence.`
- `EVALUATE implication only as an observed premise-and-consequence proof obligation; never use vacuous truth.`
- `IF the formula result is FALSE: RETURN FALSE.`
- `IF direct_counterevidence is observed: RETURN FALSE.`
- `RETURN TRUE.`

Required observable atoms:
- Coercive and neutral comparison text nodes are identified.
- Event-listener counts are directly exposed for both nodes.
- The ratio exceeds the stated threshold.

Direct counterevidence:
- The ratio does not exceed the threshold.

## DP-018-C2 — Persuasive Language — Visual Emphasis Asymmetry on Coercive Text

Operational reading: Coercive text is more visually emphasized than neutral text

Pseudocode:
- `CONTEXT GATE: identify the rule's specific object/context and mechanism anchor before evaluation.`
- `IF only a generic UI object, generic price, generic CTA, or mere absence is observed: provide no support.`
- `IF a required atom is unavailable: provide no support, preserve the base judgment, and record the evidence as missing.`
- `IF a required atom depends on a UI transition: snapshot the scoped DOM, activate the most direct in-scope control once, wait for rendered settlement, and snapshot again.`
- `IF a visible standard next step is required by the rule: follow one such step and capture the resulting scoped DOM; an unverified click is missing evidence, not a negative.`
- `INPUT evidence_ledger, with state IDs and modality availability.`
- `CHECK the pattern-specific domain/context gate.`
- `IF the core context is contradicted: RETURN NO_MATCH.`
- `PARSE the original formula while preserving every AND, OR, NOT, quantifier, and grouping.`
- `EVALUATE each atomic operand only from directly observed evidence.`
- `EVALUATE implication only as an observed premise-and-consequence proof obligation; never use vacuous truth.`
- `IF the formula result is FALSE: RETURN FALSE.`
- `IF direct_counterevidence is observed: RETURN FALSE.`
- `RETURN TRUE.`

Required observable atoms:
- Coercive and neutral comparison text nodes are identified in the same state.
- Font-weight and/or area measurements are comparable.
- At least one stated emphasis ratio exceeds threshold.

Direct counterevidence:
- No stated emphasis ratio is exceeded.

## DP-018-C3 — Persuasive Language — Jointly Satisfiable Claims (Counterevidence)

Operational reading: Joint satisfiability is counterevidence to contradiction and cannot create a persuasive-language trigger.

Pseudocode:
- `CONTEXT GATE: identify the rule's specific object/context and mechanism anchor before evaluation.`
- `IF only a generic UI object, generic price, generic CTA, or mere absence is observed: provide no support.`
- `IF a required atom is unavailable: provide no support, preserve the base judgment, and record the evidence as missing.`
- `IF a required atom depends on a UI transition: snapshot the scoped DOM, activate the most direct in-scope control once, wait for rendered settlement, and snapshot again.`
- `IF a visible standard next step is required by the rule: follow one such step and capture the resulting scoped DOM; an unverified click is missing evidence, not a negative.`
- `INPUT evidence_ledger, with state IDs and modality availability.`
- `CHECK the pattern-specific domain/context gate.`
- `IF the core context is contradicted: RETURN NO_MATCH.`
- `PARSE the original formula while preserving every AND, OR, NOT, quantifier, and grouping.`
- `EVALUATE each atomic operand only from directly observed evidence.`
- `EVALUATE implication only as an observed premise-and-consequence proof obligation; never use vacuous truth.`
- `IF the formula result is FALSE: RETURN FALSE.`
- `IF the source formula evaluates TRUE: RETURN TRUE as counterevidence, never as a pattern trigger.`

Required observable atoms:
- The relevant claims are identified in the same decision context.
- Their semantic relation is evaluated under the displayed context.
- The claims are jointly satisfiable.

Direct counterevidence:
- The claims are mutually inconsistent or unsatisfiable.

## DP-021-C1 — Cuteness — Structural Conditional Injection of Affective Assets

Operational reading: Affective assets appear in cancellation but not onboarding DOM

Pseudocode:
- `CONTEXT GATE: identify the rule's specific object/context and mechanism anchor before evaluation.`
- `IF only a generic UI object, generic price, generic CTA, or mere absence is observed: provide no support.`
- `IF a required atom is unavailable: provide no support, preserve the base judgment, and record the evidence as missing.`
- `IF a required atom depends on a UI transition: snapshot the scoped DOM, activate the most direct in-scope control once, wait for rendered settlement, and snapshot again.`
- `IF a visible standard next step is required by the rule: follow one such step and capture the resulting scoped DOM; an unverified click is missing evidence, not a negative.`
- `INPUT evidence_ledger, with state IDs and modality availability.`
- `CHECK the pattern-specific domain/context gate.`
- `IF the core context is contradicted: RETURN NO_MATCH.`
- `PARSE the original formula while preserving every AND, OR, NOT, quantifier, and grouping.`
- `EVALUATE each atomic operand only from directly observed evidence.`
- `EVALUATE implication only as an observed premise-and-consequence proof obligation; never use vacuous truth.`
- `IF the formula result is FALSE: RETURN FALSE.`
- `IF direct_counterevidence is observed: RETURN FALSE.`
- `RETURN TRUE.`

Required observable atoms:
- The onboarding and cancellation states are identified and aligned.
- The affective asset is absent from onboarding DOM and present in cancellation DOM.

Direct counterevidence:
- The asset is present in both states or absent from both.

## DP-021-C2 — Cuteness — Context-Dependent Image Injection

Operational reading: Affective image appears only in the cancellation context

Pseudocode:
- `CONTEXT GATE: identify the rule's specific object/context and mechanism anchor before evaluation.`
- `IF only a generic UI object, generic price, generic CTA, or mere absence is observed: provide no support.`
- `IF a required atom is unavailable: provide no support, preserve the base judgment, and record the evidence as missing.`
- `IF a required atom depends on a UI transition: snapshot the scoped DOM, activate the most direct in-scope control once, wait for rendered settlement, and snapshot again.`
- `IF a visible standard next step is required by the rule: follow one such step and capture the resulting scoped DOM; an unverified click is missing evidence, not a negative.`
- `INPUT evidence_ledger, with state IDs and modality availability.`
- `CHECK the pattern-specific domain/context gate.`
- `IF the core context is contradicted: RETURN NO_MATCH.`
- `PARSE the original formula while preserving every AND, OR, NOT, quantifier, and grouping.`
- `EVALUATE each atomic operand only from directly observed evidence.`
- `EVALUATE implication only as an observed premise-and-consequence proof obligation; never use vacuous truth.`
- `IF the formula result is FALSE: RETURN FALSE.`
- `IF direct_counterevidence is observed: RETURN FALSE.`
- `RETURN TRUE.`

Required observable atoms:
- The onboarding and cancellation image sets are identified and aligned.
- The affective image is absent from onboarding and present in cancellation.
- The image identity is matched across the two states.

Direct counterevidence:
- The image is not context-dependent under the stated comparison.

## DP-021-C3 — Cuteness — Semantic Pairing of Guilt

Operational reading: Guilt-inducing text is spatially paired with an affective image

Pseudocode:
- `CONTEXT GATE: identify the rule's specific object/context and mechanism anchor before evaluation.`
- `IF only a generic UI object, generic price, generic CTA, or mere absence is observed: provide no support.`
- `IF a required atom is unavailable: provide no support, preserve the base judgment, and record the evidence as missing.`
- `IF a required atom depends on a UI transition: snapshot the scoped DOM, activate the most direct in-scope control once, wait for rendered settlement, and snapshot again.`
- `IF a visible standard next step is required by the rule: follow one such step and capture the resulting scoped DOM; an unverified click is missing evidence, not a negative.`
- `INPUT evidence_ledger, with state IDs and modality availability.`
- `CHECK the pattern-specific domain/context gate.`
- `IF the core context is contradicted: RETURN NO_MATCH.`
- `PARSE the original formula while preserving every AND, OR, NOT, quantifier, and grouping.`
- `EVALUATE each atomic operand only from directly observed evidence.`
- `EVALUATE implication only as an observed premise-and-consequence proof obligation; never use vacuous truth.`
- `IF the formula result is FALSE: RETURN FALSE.`
- `IF direct_counterevidence is observed: RETURN FALSE.`
- `RETURN TRUE.`

Required observable atoms:
- Affect score exceeds the threshold for the relevant text.
- The affective text and image are spatially within the stated proximity.

Direct counterevidence:
- The text is neutral or not spatially paired with the image.

## DP-022-C1 — Positive Or Negative Framing — Structural Asymmetry of Framed Option Subtrees

Operational reading: Evaluate the source formula over directly observed evidence; preserve every Boolean operator, quantifier, comparison, unit, scope, and threshold.

Pseudocode:
- `CONTEXT GATE: identify the rule's specific object/context and mechanism anchor before evaluation.`
- `IF only a generic UI object, generic price, generic CTA, or mere absence is observed: provide no support.`
- `IF a required atom is unavailable: provide no support, preserve the base judgment, and record the evidence as missing.`
- `IF a required atom depends on a UI transition: snapshot the scoped DOM, activate the most direct in-scope control once, wait for rendered settlement, and snapshot again.`
- `IF a visible standard next step is required by the rule: follow one such step and capture the resulting scoped DOM; an unverified click is missing evidence, not a negative.`
- `INPUT evidence_ledger, with state IDs and modality availability.`
- `CHECK the pattern-specific domain/context gate.`
- `IF the core context is contradicted: RETURN NO_MATCH.`
- `PARSE the original formula while preserving every AND, OR, NOT, quantifier, and grouping.`
- `EVALUATE each atomic operand only from directly observed evidence.`
- `EVALUATE implication only as an observed premise-and-consequence proof obligation; never use vacuous truth.`
- `IF the formula result is FALSE: RETURN FALSE.`
- `IF direct_counterevidence is observed: RETURN FALSE.`
- `RETURN TRUE.`

Required observable atoms:
- Every atomic clause in the original formula is identified and evaluated in the stated scope.

Direct counterevidence:
- A directly observed atom contradicts a mandatory requirement.

## DP-022-C2 — Positive Or Negative Framing — Visual Weight Asymmetry Between Framing Poles

Operational reading: Evaluate the source formula over directly observed evidence; preserve every Boolean operator, quantifier, comparison, unit, scope, and threshold.

Pseudocode:
- `CONTEXT GATE: identify the rule's specific object/context and mechanism anchor before evaluation.`
- `IF only a generic UI object, generic price, generic CTA, or mere absence is observed: provide no support.`
- `IF a required atom is unavailable: provide no support, preserve the base judgment, and record the evidence as missing.`
- `IF a required atom depends on a UI transition: snapshot the scoped DOM, activate the most direct in-scope control once, wait for rendered settlement, and snapshot again.`
- `IF a visible standard next step is required by the rule: follow one such step and capture the resulting scoped DOM; an unverified click is missing evidence, not a negative.`
- `INPUT evidence_ledger, with state IDs and modality availability.`
- `CHECK the pattern-specific domain/context gate.`
- `IF the core context is contradicted: RETURN NO_MATCH.`
- `PARSE the original formula while preserving every AND, OR, NOT, quantifier, and grouping.`
- `EVALUATE each atomic operand only from directly observed evidence.`
- `EVALUATE implication only as an observed premise-and-consequence proof obligation; never use vacuous truth.`
- `IF the formula result is FALSE: RETURN FALSE.`
- `IF direct_counterevidence is observed: RETURN FALSE.`
- `RETURN TRUE.`

Required observable atoms:
- Every atomic clause in the original formula is identified and evaluated in the stated scope.

Direct counterevidence:
- A directly observed atom contradicts a mandatory requirement.

## DP-022-C3 — Positive Or Negative Framing — Identification of Mutually Exclusive Vectors

Operational reading: Evaluate the source formula over directly observed evidence; preserve every Boolean operator, quantifier, comparison, unit, scope, and threshold.

Pseudocode:
- `CONTEXT GATE: identify the rule's specific object/context and mechanism anchor before evaluation.`
- `IF only a generic UI object, generic price, generic CTA, or mere absence is observed: provide no support.`
- `IF a required atom is unavailable: provide no support, preserve the base judgment, and record the evidence as missing.`
- `IF a required atom depends on a UI transition: snapshot the scoped DOM, activate the most direct in-scope control once, wait for rendered settlement, and snapshot again.`
- `IF a visible standard next step is required by the rule: follow one such step and capture the resulting scoped DOM; an unverified click is missing evidence, not a negative.`
- `INPUT evidence_ledger, with state IDs and modality availability.`
- `CHECK the pattern-specific domain/context gate.`
- `IF the core context is contradicted: RETURN NO_MATCH.`
- `PARSE the original formula while preserving every AND, OR, NOT, quantifier, and grouping.`
- `EVALUATE each atomic operand only from directly observed evidence.`
- `EVALUATE implication only as an observed premise-and-consequence proof obligation; never use vacuous truth.`
- `IF the formula result is FALSE: RETURN FALSE.`
- `IF direct_counterevidence is observed: RETURN FALSE.`
- `RETURN TRUE.`

Required observable atoms:
- Every atomic clause in the original formula is identified and evaluated in the stated scope.

Direct counterevidence:
- A directly observed atom contradicts a mandatory requirement.

## DP-025-C1 — Choice Overload — Excessive Element Quantization

Operational reading: Evaluate the source formula over directly observed evidence; preserve every Boolean operator, quantifier, comparison, unit, scope, and threshold.

Pseudocode:
- `CONTEXT GATE: identify the rule's specific object/context and mechanism anchor before evaluation.`
- `IF only a generic UI object, generic price, generic CTA, or mere absence is observed: provide no support.`
- `IF a required atom is unavailable: provide no support, preserve the base judgment, and record the evidence as missing.`
- `IF a required atom depends on a UI transition: snapshot the scoped DOM, activate the most direct in-scope control once, wait for rendered settlement, and snapshot again.`
- `IF a visible standard next step is required by the rule: follow one such step and capture the resulting scoped DOM; an unverified click is missing evidence, not a negative.`
- `INPUT evidence_ledger, with state IDs and modality availability.`
- `CHECK the pattern-specific domain/context gate.`
- `IF the core context is contradicted: RETURN NO_MATCH.`
- `PARSE the original formula while preserving every AND, OR, NOT, quantifier, and grouping.`
- `EVALUATE each atomic operand only from directly observed evidence.`
- `EVALUATE implication only as an observed premise-and-consequence proof obligation; never use vacuous truth.`
- `IF the formula result is FALSE: RETURN FALSE.`
- `IF direct_counterevidence is observed: RETURN FALSE.`
- `RETURN TRUE.`

Required observable atoms:
- Every atomic clause in the original formula is identified and evaluated in the stated scope.

Direct counterevidence:
- A directly observed atom contradicts a mandatory requirement.

## DP-025-C2 — Choice Overload — Visual Density of Interactive Decision Elements

Operational reading: Evaluate the source formula over directly observed evidence; preserve every Boolean operator, quantifier, comparison, unit, scope, and threshold.

Pseudocode:
- `CONTEXT GATE: identify the rule's specific object/context and mechanism anchor before evaluation.`
- `IF only a generic UI object, generic price, generic CTA, or mere absence is observed: provide no support.`
- `IF a required atom is unavailable: provide no support, preserve the base judgment, and record the evidence as missing.`
- `IF a required atom depends on a UI transition: snapshot the scoped DOM, activate the most direct in-scope control once, wait for rendered settlement, and snapshot again.`
- `IF a visible standard next step is required by the rule: follow one such step and capture the resulting scoped DOM; an unverified click is missing evidence, not a negative.`
- `INPUT evidence_ledger, with state IDs and modality availability.`
- `CHECK the pattern-specific domain/context gate.`
- `IF the core context is contradicted: RETURN NO_MATCH.`
- `PARSE the original formula while preserving every AND, OR, NOT, quantifier, and grouping.`
- `EVALUATE each atomic operand only from directly observed evidence.`
- `EVALUATE implication only as an observed premise-and-consequence proof obligation; never use vacuous truth.`
- `IF the formula result is FALSE: RETURN FALSE.`
- `IF direct_counterevidence is observed: RETURN FALSE.`
- `RETURN TRUE.`

Required observable atoms:
- Every atomic clause in the original formula is identified and evaluated in the stated scope.

Direct counterevidence:
- A directly observed atom contradicts a mandatory requirement.

## DP-025-C3 — Choice Overload — Semantic Similarity Collapse Among Options

Operational reading: Evaluate the source formula over directly observed evidence; preserve every Boolean operator, quantifier, comparison, unit, scope, and threshold.

Pseudocode:
- `CONTEXT GATE: identify the rule's specific object/context and mechanism anchor before evaluation.`
- `IF only a generic UI object, generic price, generic CTA, or mere absence is observed: provide no support.`
- `IF a required atom is unavailable: provide no support, preserve the base judgment, and record the evidence as missing.`
- `IF a required atom depends on a UI transition: snapshot the scoped DOM, activate the most direct in-scope control once, wait for rendered settlement, and snapshot again.`
- `IF a visible standard next step is required by the rule: follow one such step and capture the resulting scoped DOM; an unverified click is missing evidence, not a negative.`
- `INPUT evidence_ledger, with state IDs and modality availability.`
- `CHECK the pattern-specific domain/context gate.`
- `IF the core context is contradicted: RETURN NO_MATCH.`
- `PARSE the original formula while preserving every AND, OR, NOT, quantifier, and grouping.`
- `EVALUATE each atomic operand only from directly observed evidence.`
- `EVALUATE implication only as an observed premise-and-consequence proof obligation; never use vacuous truth.`
- `IF the formula result is FALSE: RETURN FALSE.`
- `IF direct_counterevidence is observed: RETURN FALSE.`
- `RETURN TRUE.`

Required observable atoms:
- Every atomic clause in the original formula is identified and evaluated in the stated scope.

Direct counterevidence:
- A directly observed atom contradicts a mandatory requirement.

## DP-030-C1 — Plain Evil (Theoretical Construct) — Dark Pattern Singularity

Operational reading: Evaluate the source formula over directly observed evidence; preserve every Boolean operator, quantifier, comparison, unit, scope, and threshold.

Pseudocode:
- `CONTEXT GATE: identify the rule's specific object/context and mechanism anchor before evaluation.`
- `IF only a generic UI object, generic price, generic CTA, or mere absence is observed: provide no support.`
- `IF a required atom is unavailable: provide no support, preserve the base judgment, and record the evidence as missing.`
- `IF a required atom depends on a UI transition: snapshot the scoped DOM, activate the most direct in-scope control once, wait for rendered settlement, and snapshot again.`
- `IF a visible standard next step is required by the rule: follow one such step and capture the resulting scoped DOM; an unverified click is missing evidence, not a negative.`
- `INPUT evidence_ledger, with state IDs and modality availability.`
- `CHECK the pattern-specific domain/context gate.`
- `IF the core context is contradicted: RETURN NO_MATCH.`
- `PARSE the original formula while preserving every AND, OR, NOT, quantifier, and grouping.`
- `EVALUATE each atomic operand only from directly observed evidence.`
- `EVALUATE implication only as an observed premise-and-consequence proof obligation; never use vacuous truth.`
- `IF the formula result is FALSE: RETURN FALSE.`
- `IF direct_counterevidence is observed: RETURN FALSE.`
- `RETURN TRUE.`

Required observable atoms:
- Every atomic clause in the original formula is identified and evaluated in the stated scope.

Direct counterevidence:
- A directly observed atom contradicts a mandatory requirement.

## DP-030-C2 — Plain Evil (Theoretical Construct) — Visual Hostility Density Index

Operational reading: Evaluate the source formula over directly observed evidence; preserve every Boolean operator, quantifier, comparison, unit, scope, and threshold.

Pseudocode:
- `CONTEXT GATE: identify the rule's specific object/context and mechanism anchor before evaluation.`
- `IF only a generic UI object, generic price, generic CTA, or mere absence is observed: provide no support.`
- `IF a required atom is unavailable: provide no support, preserve the base judgment, and record the evidence as missing.`
- `IF a required atom depends on a UI transition: snapshot the scoped DOM, activate the most direct in-scope control once, wait for rendered settlement, and snapshot again.`
- `IF a visible standard next step is required by the rule: follow one such step and capture the resulting scoped DOM; an unverified click is missing evidence, not a negative.`
- `INPUT evidence_ledger, with state IDs and modality availability.`
- `CHECK the pattern-specific domain/context gate.`
- `IF the core context is contradicted: RETURN NO_MATCH.`
- `PARSE the original formula while preserving every AND, OR, NOT, quantifier, and grouping.`
- `EVALUATE each atomic operand only from directly observed evidence.`
- `EVALUATE implication only as an observed premise-and-consequence proof obligation; never use vacuous truth.`
- `IF the formula result is FALSE: RETURN FALSE.`
- `IF direct_counterevidence is observed: RETURN FALSE.`
- `RETURN TRUE.`

Required observable atoms:
- Every atomic clause in the original formula is identified and evaluated in the stated scope.

Direct counterevidence:
- A directly observed atom contradicts a mandatory requirement.

## DP-030-C3 — Plain Evil (Theoretical Construct) — Semantic Hostility Density Score

Operational reading: Evaluate the source formula over directly observed evidence; preserve every Boolean operator, quantifier, comparison, unit, scope, and threshold.

Pseudocode:
- `CONTEXT GATE: identify the rule's specific object/context and mechanism anchor before evaluation.`
- `IF only a generic UI object, generic price, generic CTA, or mere absence is observed: provide no support.`
- `IF a required atom is unavailable: provide no support, preserve the base judgment, and record the evidence as missing.`
- `IF a required atom depends on a UI transition: snapshot the scoped DOM, activate the most direct in-scope control once, wait for rendered settlement, and snapshot again.`
- `IF a visible standard next step is required by the rule: follow one such step and capture the resulting scoped DOM; an unverified click is missing evidence, not a negative.`
- `INPUT evidence_ledger, with state IDs and modality availability.`
- `CHECK the pattern-specific domain/context gate.`
- `IF the core context is contradicted: RETURN NO_MATCH.`
- `PARSE the original formula while preserving every AND, OR, NOT, quantifier, and grouping.`
- `EVALUATE each atomic operand only from directly observed evidence.`
- `EVALUATE implication only as an observed premise-and-consequence proof obligation; never use vacuous truth.`
- `IF the formula result is FALSE: RETURN FALSE.`
- `IF direct_counterevidence is observed: RETURN FALSE.`
- `RETURN TRUE.`

Required observable atoms:
- Every atomic clause in the original formula is identified and evaluated in the stated scope.

Direct counterevidence:
- A directly observed atom contradicts a mandatory requirement.

## DP-054-C1 — Endorsement And Testimonials — Statistical Implausibility

Operational reading: Review distribution is uniform-looking and requires provenance checking

Pseudocode:
- `CONTEXT GATE: identify the rule's specific object/context and mechanism anchor before evaluation.`
- `IF only a generic UI object, generic price, generic CTA, or mere absence is observed: provide no support.`
- `IF a required atom is unavailable: provide no support, preserve the base judgment, and record the evidence as missing.`
- `IF a required atom depends on a UI transition: snapshot the scoped DOM, activate the most direct in-scope control once, wait for rendered settlement, and snapshot again.`
- `IF a visible standard next step is required by the rule: follow one such step and capture the resulting scoped DOM; an unverified click is missing evidence, not a negative.`
- `INPUT evidence_ledger, with state IDs and modality availability.`
- `CHECK the pattern-specific domain/context gate.`
- `IF the core context is contradicted: RETURN NO_MATCH.`
- `PARSE the original formula while preserving every AND, OR, NOT, quantifier, and grouping.`
- `EVALUATE each atomic operand only from directly observed evidence.`
- `EVALUATE implication only as an observed premise-and-consequence proof obligation; never use vacuous truth.`
- `IF the formula result is FALSE: RETURN FALSE.`
- `IF direct_counterevidence is observed: RETURN FALSE.`
- `RETURN TRUE.`

Required observable atoms:
- The complete displayed review-score distribution is available.
- Mean and variance meet the stated approximate values.
- A separate provenance observation is available before claiming rendered/organic mismatch.

Direct counterevidence:
- The distribution does not meet the stated values or provenance is directly established as organic.

## DP-054-C2 — Endorsement And Testimonials — Visual Verifiability of Testimonial Attribution

Operational reading: A testimonial has no attribution in its complete container DOM

Pseudocode:
- `CONTEXT GATE: identify the rule's specific object/context and mechanism anchor before evaluation.`
- `IF only a generic UI object, generic price, generic CTA, or mere absence is observed: provide no support.`
- `IF a required atom is unavailable: provide no support, preserve the base judgment, and record the evidence as missing.`
- `IF a required atom depends on a UI transition: snapshot the scoped DOM, activate the most direct in-scope control once, wait for rendered settlement, and snapshot again.`
- `IF a visible standard next step is required by the rule: follow one such step and capture the resulting scoped DOM; an unverified click is missing evidence, not a negative.`
- `INPUT evidence_ledger, with state IDs and modality availability.`
- `CHECK the pattern-specific domain/context gate.`
- `IF the core context is contradicted: RETURN NO_MATCH.`
- `PARSE the original formula while preserving every AND, OR, NOT, quantifier, and grouping.`
- `EVALUATE each atomic operand only from directly observed evidence.`
- `EVALUATE implication only as an observed premise-and-consequence proof obligation; never use vacuous truth.`
- `IF the formula result is FALSE: RETURN FALSE.`
- `IF direct_counterevidence is observed: RETURN FALSE.`
- `RETURN TRUE.`

Required observable atoms:
- A testimonial container is identified and its descendant DOM is completely inspected.
- No attribution node is present in that container.

Direct counterevidence:
- An attribution node is present.

## DP-054-C3 — Endorsement And Testimonials — Provenance Obfuscation

Operational reading: Testimonial avatar or text matches a stock/template source

Pseudocode:
- `CONTEXT GATE: identify the rule's specific object/context and mechanism anchor before evaluation.`
- `IF only a generic UI object, generic price, generic CTA, or mere absence is observed: provide no support.`
- `IF a required atom is unavailable: provide no support, preserve the base judgment, and record the evidence as missing.`
- `IF a required atom depends on a UI transition: snapshot the scoped DOM, activate the most direct in-scope control once, wait for rendered settlement, and snapshot again.`
- `IF a visible standard next step is required by the rule: follow one such step and capture the resulting scoped DOM; an unverified click is missing evidence, not a negative.`
- `INPUT evidence_ledger, with state IDs and modality availability.`
- `CHECK the pattern-specific domain/context gate.`
- `IF the core context is contradicted: RETURN NO_MATCH.`
- `PARSE the original formula while preserving every AND, OR, NOT, quantifier, and grouping.`
- `EVALUATE each atomic operand only from directly observed evidence.`
- `EVALUATE implication only as an observed premise-and-consequence proof obligation; never use vacuous truth.`
- `IF the formula result is FALSE: RETURN FALSE.`
- `IF direct_counterevidence is observed: RETURN FALSE.`
- `RETURN TRUE.`

Required observable atoms:
- A testimonial instance and comparison source are identified.
- The stated image or text similarity threshold is met.
- The similarity result is treated as provenance evidence, not proof of deception without context.

Direct counterevidence:
- The stated similarity threshold is not met.

## DP-059-C1 — Confirmshaming — Structural Asymmetry in Decline-Option Accessibility

Operational reading: Decline is not exposed as an equivalent interactive control

Pseudocode:
- `CONTEXT GATE: identify the rule's specific object/context and mechanism anchor before evaluation.`
- `IF only a generic UI object, generic price, generic CTA, or mere absence is observed: provide no support.`
- `IF a required atom is unavailable: provide no support, preserve the base judgment, and record the evidence as missing.`
- `IF a required atom depends on a UI transition: snapshot the scoped DOM, activate the most direct in-scope control once, wait for rendered settlement, and snapshot again.`
- `IF a visible standard next step is required by the rule: follow one such step and capture the resulting scoped DOM; an unverified click is missing evidence, not a negative.`
- `INPUT evidence_ledger, with state IDs and modality availability.`
- `CHECK the pattern-specific domain/context gate.`
- `IF the core context is contradicted: RETURN NO_MATCH.`
- `PARSE the original formula while preserving every AND, OR, NOT, quantifier, and grouping.`
- `EVALUATE each atomic operand only from directly observed evidence.`
- `EVALUATE implication only as an observed premise-and-consequence proof obligation; never use vacuous truth.`
- `IF the formula result is FALSE: RETURN FALSE.`
- `IF direct_counterevidence is observed: RETURN FALSE.`
- `RETURN TRUE.`

Required observable atoms:
- Accept and decline nodes are identified in the same state.
- The decline node is not a button/equivalent button role while accept is.

Direct counterevidence:
- Both choices expose equivalent interactive semantics.

## DP-059-C2 — Confirmshaming — Visual Hierarchy Subversion

Operational reading: Decline is visually subordinated below the stated accessibility threshold

Pseudocode:
- `CONTEXT GATE: identify the rule's specific object/context and mechanism anchor before evaluation.`
- `IF only a generic UI object, generic price, generic CTA, or mere absence is observed: provide no support.`
- `IF a required atom is unavailable: provide no support, preserve the base judgment, and record the evidence as missing.`
- `IF a required atom depends on a UI transition: snapshot the scoped DOM, activate the most direct in-scope control once, wait for rendered settlement, and snapshot again.`
- `IF a visible standard next step is required by the rule: follow one such step and capture the resulting scoped DOM; an unverified click is missing evidence, not a negative.`
- `INPUT evidence_ledger, with state IDs and modality availability.`
- `CHECK the pattern-specific domain/context gate.`
- `IF the core context is contradicted: RETURN NO_MATCH.`
- `PARSE the original formula while preserving every AND, OR, NOT, quantifier, and grouping.`
- `EVALUATE each atomic operand only from directly observed evidence.`
- `EVALUATE implication only as an observed premise-and-consequence proof obligation; never use vacuous truth.`
- `IF the formula result is FALSE: RETURN FALSE.`
- `IF direct_counterevidence is observed: RETURN FALSE.`
- `RETURN TRUE.`

Required observable atoms:
- Accept and decline visual nodes are identified in the same state.
- Their relative visibility is observed and exceeds the stated disparity.
- Decline visibility meets the stated minimum-accessibility failure condition.

Direct counterevidence:
- Decline is comparably visible and accessible.

## DP-059-C3 — Confirmshaming — Semantic Asymmetry

Operational reading: Accept language is positive while decline language is negative

Pseudocode:
- `CONTEXT GATE: identify the rule's specific object/context and mechanism anchor before evaluation.`
- `IF only a generic UI object, generic price, generic CTA, or mere absence is observed: provide no support.`
- `IF a required atom is unavailable: provide no support, preserve the base judgment, and record the evidence as missing.`
- `IF a required atom depends on a UI transition: snapshot the scoped DOM, activate the most direct in-scope control once, wait for rendered settlement, and snapshot again.`
- `IF a visible standard next step is required by the rule: follow one such step and capture the resulting scoped DOM; an unverified click is missing evidence, not a negative.`
- `INPUT evidence_ledger, with state IDs and modality availability.`
- `CHECK the pattern-specific domain/context gate.`
- `IF the core context is contradicted: RETURN NO_MATCH.`
- `PARSE the original formula while preserving every AND, OR, NOT, quantifier, and grouping.`
- `EVALUATE each atomic operand only from directly observed evidence.`
- `EVALUATE implication only as an observed premise-and-consequence proof obligation; never use vacuous truth.`
- `IF the formula result is FALSE: RETURN FALSE.`
- `IF direct_counterevidence is observed: RETURN FALSE.`
- `RETURN TRUE.`

Required observable atoms:
- Accept and decline labels are identified as the opposing choices.
- Their sentiment values meet the stated positive/negative relationship.

Direct counterevidence:
- Decline language is neutral/respectful or accept language is not positive under the stated measure.

## DP-061-C1 — Psychological Tricks — Asymmetric Dominance

Operational reading: Evaluate the source formula over directly observed evidence; preserve every Boolean operator, quantifier, comparison, unit, scope, and threshold.

Pseudocode:
- `CONTEXT GATE: identify the rule's specific object/context and mechanism anchor before evaluation.`
- `IF only a generic UI object, generic price, generic CTA, or mere absence is observed: provide no support.`
- `IF a required atom is unavailable: provide no support, preserve the base judgment, and record the evidence as missing.`
- `IF a required atom depends on a UI transition: snapshot the scoped DOM, activate the most direct in-scope control once, wait for rendered settlement, and snapshot again.`
- `IF a visible standard next step is required by the rule: follow one such step and capture the resulting scoped DOM; an unverified click is missing evidence, not a negative.`
- `INPUT evidence_ledger, with state IDs and modality availability.`
- `CHECK the pattern-specific domain/context gate.`
- `IF the core context is contradicted: RETURN NO_MATCH.`
- `PARSE the original formula while preserving every AND, OR, NOT, quantifier, and grouping.`
- `EVALUATE each atomic operand only from directly observed evidence.`
- `EVALUATE implication only as an observed premise-and-consequence proof obligation; never use vacuous truth.`
- `IF the formula result is FALSE: RETURN FALSE.`
- `IF direct_counterevidence is observed: RETURN FALSE.`
- `RETURN TRUE.`

Required observable atoms:
- Every atomic clause in the original formula is identified and evaluated in the stated scope.

Direct counterevidence:
- A directly observed atom contradicts a mandatory requirement.

## DP-061-C2 — Psychological Tricks — Cognitive Overload

Operational reading: Evaluate the source formula over directly observed evidence; preserve every Boolean operator, quantifier, comparison, unit, scope, and threshold.

Pseudocode:
- `CONTEXT GATE: identify the rule's specific object/context and mechanism anchor before evaluation.`
- `IF only a generic UI object, generic price, generic CTA, or mere absence is observed: provide no support.`
- `IF a required atom is unavailable: provide no support, preserve the base judgment, and record the evidence as missing.`
- `IF a required atom depends on a UI transition: snapshot the scoped DOM, activate the most direct in-scope control once, wait for rendered settlement, and snapshot again.`
- `IF a visible standard next step is required by the rule: follow one such step and capture the resulting scoped DOM; an unverified click is missing evidence, not a negative.`
- `INPUT evidence_ledger, with state IDs and modality availability.`
- `CHECK the pattern-specific domain/context gate.`
- `IF the core context is contradicted: RETURN NO_MATCH.`
- `PARSE the original formula while preserving every AND, OR, NOT, quantifier, and grouping.`
- `EVALUATE each atomic operand only from directly observed evidence.`
- `EVALUATE implication only as an observed premise-and-consequence proof obligation; never use vacuous truth.`
- `IF the formula result is FALSE: RETURN FALSE.`
- `IF direct_counterevidence is observed: RETURN FALSE.`
- `RETURN TRUE.`

Required observable atoms:
- Every atomic clause in the original formula is identified and evaluated in the stated scope.

Direct counterevidence:
- A directly observed atom contradicts a mandatory requirement.

## DP-061-C3 — Psychological Tricks — Reference Point Obfuscation

Operational reading: Evaluate the source formula over directly observed evidence; preserve every Boolean operator, quantifier, comparison, unit, scope, and threshold.

Pseudocode:
- `CONTEXT GATE: identify the rule's specific object/context and mechanism anchor before evaluation.`
- `IF only a generic UI object, generic price, generic CTA, or mere absence is observed: provide no support.`
- `IF a required atom is unavailable: provide no support, preserve the base judgment, and record the evidence as missing.`
- `IF a required atom depends on a UI transition: snapshot the scoped DOM, activate the most direct in-scope control once, wait for rendered settlement, and snapshot again.`
- `IF a visible standard next step is required by the rule: follow one such step and capture the resulting scoped DOM; an unverified click is missing evidence, not a negative.`
- `INPUT evidence_ledger, with state IDs and modality availability.`
- `CHECK the pattern-specific domain/context gate.`
- `IF the core context is contradicted: RETURN NO_MATCH.`
- `PARSE the original formula while preserving every AND, OR, NOT, quantifier, and grouping.`
- `EVALUATE each atomic operand only from directly observed evidence.`
- `EVALUATE implication only as an observed premise-and-consequence proof obligation; never use vacuous truth.`
- `IF the formula result is FALSE: RETURN FALSE.`
- `IF direct_counterevidence is observed: RETURN FALSE.`
- `RETURN TRUE.`

Required observable atoms:
- Every atomic clause in the original formula is identified and evaluated in the stated scope.

Direct counterevidence:
- A directly observed atom contradicts a mandatory requirement.

## DP-017-C1 — Pressured Selling — Transactional Flow Interruption

Operational reading: Evaluate the source formula over directly observed evidence; preserve every Boolean operator, quantifier, comparison, unit, scope, and threshold.

Pseudocode:
- `CONTEXT GATE: identify the rule's specific object/context and mechanism anchor before evaluation.`
- `IF only a generic UI object, generic price, generic CTA, or mere absence is observed: provide no support.`
- `IF a required atom is unavailable: provide no support, preserve the base judgment, and record the evidence as missing.`
- `IF a required atom depends on a UI transition: snapshot the scoped DOM, activate the most direct in-scope control once, wait for rendered settlement, and snapshot again.`
- `IF a visible standard next step is required by the rule: follow one such step and capture the resulting scoped DOM; an unverified click is missing evidence, not a negative.`
- `INPUT evidence_ledger, with state IDs and modality availability.`
- `CHECK the pattern-specific domain/context gate.`
- `IF the core context is contradicted: RETURN NO_MATCH.`
- `PARSE the original formula while preserving every AND, OR, NOT, quantifier, and grouping.`
- `EVALUATE each atomic operand only from directly observed evidence.`
- `EVALUATE implication only as an observed premise-and-consequence proof obligation; never use vacuous truth.`
- `IF the formula result is FALSE: RETURN FALSE.`
- `IF direct_counterevidence is observed: RETURN FALSE.`
- `RETURN TRUE.`

Required observable atoms:
- Every atomic clause in the original formula is identified and evaluated in the stated scope.

Direct counterevidence:
- A directly observed atom contradicts a mandatory requirement.

## DP-017-C2 — Pressured Selling — Localized Temporal or Visual Constraints

Operational reading: Evaluate the source formula over directly observed evidence; preserve every Boolean operator, quantifier, comparison, unit, scope, and threshold.

Pseudocode:
- `CONTEXT GATE: identify the rule's specific object/context and mechanism anchor before evaluation.`
- `IF only a generic UI object, generic price, generic CTA, or mere absence is observed: provide no support.`
- `IF a required atom is unavailable: provide no support, preserve the base judgment, and record the evidence as missing.`
- `IF a required atom depends on a UI transition: snapshot the scoped DOM, activate the most direct in-scope control once, wait for rendered settlement, and snapshot again.`
- `IF a visible standard next step is required by the rule: follow one such step and capture the resulting scoped DOM; an unverified click is missing evidence, not a negative.`
- `INPUT evidence_ledger, with state IDs and modality availability.`
- `CHECK the pattern-specific domain/context gate.`
- `IF the core context is contradicted: RETURN NO_MATCH.`
- `PARSE the original formula while preserving every AND, OR, NOT, quantifier, and grouping.`
- `EVALUATE each atomic operand only from directly observed evidence.`
- `EVALUATE implication only as an observed premise-and-consequence proof obligation; never use vacuous truth.`
- `IF the formula result is FALSE: RETURN FALSE.`
- `IF direct_counterevidence is observed: RETURN FALSE.`
- `RETURN TRUE.`

Required observable atoms:
- Every atomic clause in the original formula is identified and evaluated in the stated scope.

Direct counterevidence:
- A directly observed atom contradicts a mandatory requirement.

## DP-017-C3 — Pressured Selling — High-Arousal Lexical Density

Operational reading: Evaluate the source formula over directly observed evidence; preserve every Boolean operator, quantifier, comparison, unit, scope, and threshold.

Pseudocode:
- `CONTEXT GATE: identify the rule's specific object/context and mechanism anchor before evaluation.`
- `IF only a generic UI object, generic price, generic CTA, or mere absence is observed: provide no support.`
- `IF a required atom is unavailable: provide no support, preserve the base judgment, and record the evidence as missing.`
- `IF a required atom depends on a UI transition: snapshot the scoped DOM, activate the most direct in-scope control once, wait for rendered settlement, and snapshot again.`
- `IF a visible standard next step is required by the rule: follow one such step and capture the resulting scoped DOM; an unverified click is missing evidence, not a negative.`
- `INPUT evidence_ledger, with state IDs and modality availability.`
- `CHECK the pattern-specific domain/context gate.`
- `IF the core context is contradicted: RETURN NO_MATCH.`
- `PARSE the original formula while preserving every AND, OR, NOT, quantifier, and grouping.`
- `EVALUATE each atomic operand only from directly observed evidence.`
- `EVALUATE implication only as an observed premise-and-consequence proof obligation; never use vacuous truth.`
- `IF the formula result is FALSE: RETURN FALSE.`
- `IF direct_counterevidence is observed: RETURN FALSE.`
- `RETURN TRUE.`

Required observable atoms:
- Every atomic clause in the original formula is identified and evaluated in the stated scope.

Direct counterevidence:
- A directly observed atom contradicts a mandatory requirement.

## DP-019-C1 — Small or Moving Close Button — Structural Event-Listener Commandeering on Dismissal Vectors

Operational reading: Evaluate the source formula over directly observed evidence; preserve every Boolean operator, quantifier, comparison, unit, scope, and threshold.

Pseudocode:
- `CONTEXT GATE: identify the rule's specific object/context and mechanism anchor before evaluation.`
- `IF only a generic UI object, generic price, generic CTA, or mere absence is observed: provide no support.`
- `IF a required atom is unavailable: provide no support, preserve the base judgment, and record the evidence as missing.`
- `IF a required atom depends on a UI transition: snapshot the scoped DOM, activate the most direct in-scope control once, wait for rendered settlement, and snapshot again.`
- `IF a visible standard next step is required by the rule: follow one such step and capture the resulting scoped DOM; an unverified click is missing evidence, not a negative.`
- `INPUT evidence_ledger, with state IDs and modality availability.`
- `CHECK the pattern-specific domain/context gate.`
- `IF the core context is contradicted: RETURN NO_MATCH.`
- `PARSE the original formula while preserving every AND, OR, NOT, quantifier, and grouping.`
- `EVALUATE each atomic operand only from directly observed evidence.`
- `EVALUATE implication only as an observed premise-and-consequence proof obligation; never use vacuous truth.`
- `IF the formula result is FALSE: RETURN FALSE.`
- `IF direct_counterevidence is observed: RETURN FALSE.`
- `RETURN TRUE.`

Required observable atoms:
- Every atomic clause in the original formula is identified and evaluated in the stated scope.

Direct counterevidence:
- A directly observed atom contradicts a mandatory requirement.

## DP-019-C2 — Small or Moving Close Button — Microscopic Hitbox

Operational reading: Evaluate the source formula over directly observed evidence; preserve every Boolean operator, quantifier, comparison, unit, scope, and threshold.

Pseudocode:
- `CONTEXT GATE: identify the rule's specific object/context and mechanism anchor before evaluation.`
- `IF only a generic UI object, generic price, generic CTA, or mere absence is observed: provide no support.`
- `IF a required atom is unavailable: provide no support, preserve the base judgment, and record the evidence as missing.`
- `IF a required atom depends on a UI transition: snapshot the scoped DOM, activate the most direct in-scope control once, wait for rendered settlement, and snapshot again.`
- `IF a visible standard next step is required by the rule: follow one such step and capture the resulting scoped DOM; an unverified click is missing evidence, not a negative.`
- `INPUT evidence_ledger, with state IDs and modality availability.`
- `CHECK the pattern-specific domain/context gate.`
- `IF the core context is contradicted: RETURN NO_MATCH.`
- `PARSE the original formula while preserving every AND, OR, NOT, quantifier, and grouping.`
- `EVALUATE each atomic operand only from directly observed evidence.`
- `EVALUATE implication only as an observed premise-and-consequence proof obligation; never use vacuous truth.`
- `IF the formula result is FALSE: RETURN FALSE.`
- `IF direct_counterevidence is observed: RETURN FALSE.`
- `RETURN TRUE.`

Required observable atoms:
- Every atomic clause in the original formula is identified and evaluated in the stated scope.

Direct counterevidence:
- A directly observed atom contradicts a mandatory requirement.

## DP-019-C3 — Small or Moving Close Button — Semantic Obfuscation of Dismissal Labels

Operational reading: Evaluate the source formula over directly observed evidence; preserve every Boolean operator, quantifier, comparison, unit, scope, and threshold.

Pseudocode:
- `CONTEXT GATE: identify the rule's specific object/context and mechanism anchor before evaluation.`
- `IF only a generic UI object, generic price, generic CTA, or mere absence is observed: provide no support.`
- `IF a required atom is unavailable: provide no support, preserve the base judgment, and record the evidence as missing.`
- `IF a required atom depends on a UI transition: snapshot the scoped DOM, activate the most direct in-scope control once, wait for rendered settlement, and snapshot again.`
- `IF a visible standard next step is required by the rule: follow one such step and capture the resulting scoped DOM; an unverified click is missing evidence, not a negative.`
- `INPUT evidence_ledger, with state IDs and modality availability.`
- `CHECK the pattern-specific domain/context gate.`
- `IF the core context is contradicted: RETURN NO_MATCH.`
- `PARSE the original formula while preserving every AND, OR, NOT, quantifier, and grouping.`
- `EVALUATE each atomic operand only from directly observed evidence.`
- `EVALUATE implication only as an observed premise-and-consequence proof obligation; never use vacuous truth.`
- `IF the formula result is FALSE: RETURN FALSE.`
- `IF direct_counterevidence is observed: RETURN FALSE.`
- `RETURN TRUE.`

Required observable atoms:
- Every atomic clause in the original formula is identified and evaluated in the stated scope.

Direct counterevidence:
- A directly observed atom contradicts a mandatory requirement.

## DP-020-C1 — Bad Defaults / Preselection — Pre-initialized Activation State

Operational reading: Evaluate the source formula over directly observed evidence; preserve every Boolean operator, quantifier, comparison, unit, scope, and threshold.

Pseudocode:
- `CONTEXT GATE: identify the rule's specific object/context and mechanism anchor before evaluation.`
- `IF only a generic UI object, generic price, generic CTA, or mere absence is observed: provide no support.`
- `IF a required atom is unavailable: provide no support, preserve the base judgment, and record the evidence as missing.`
- `IF a required atom depends on a UI transition: snapshot the scoped DOM, activate the most direct in-scope control once, wait for rendered settlement, and snapshot again.`
- `IF a visible standard next step is required by the rule: follow one such step and capture the resulting scoped DOM; an unverified click is missing evidence, not a negative.`
- `INPUT evidence_ledger, with state IDs and modality availability.`
- `CHECK the pattern-specific domain/context gate.`
- `IF the core context is contradicted: RETURN NO_MATCH.`
- `PARSE the original formula while preserving every AND, OR, NOT, quantifier, and grouping.`
- `EVALUATE each atomic operand only from directly observed evidence.`
- `EVALUATE implication only as an observed premise-and-consequence proof obligation; never use vacuous truth.`
- `IF the formula result is FALSE: RETURN FALSE.`
- `IF direct_counterevidence is observed: RETURN FALSE.`
- `RETURN TRUE.`

Required observable atoms:
- Every atomic clause in the original formula is identified and evaluated in the stated scope.

Direct counterevidence:
- A directly observed atom contradicts a mandatory requirement.

## DP-020-C2 — Bad Defaults / Preselection — Visual or Structural Obfuscation

Operational reading: Evaluate the source formula over directly observed evidence; preserve every Boolean operator, quantifier, comparison, unit, scope, and threshold.

Pseudocode:
- `CONTEXT GATE: identify the rule's specific object/context and mechanism anchor before evaluation.`
- `IF only a generic UI object, generic price, generic CTA, or mere absence is observed: provide no support.`
- `IF a required atom is unavailable: provide no support, preserve the base judgment, and record the evidence as missing.`
- `IF a required atom depends on a UI transition: snapshot the scoped DOM, activate the most direct in-scope control once, wait for rendered settlement, and snapshot again.`
- `IF a visible standard next step is required by the rule: follow one such step and capture the resulting scoped DOM; an unverified click is missing evidence, not a negative.`
- `INPUT evidence_ledger, with state IDs and modality availability.`
- `CHECK the pattern-specific domain/context gate.`
- `IF the core context is contradicted: RETURN NO_MATCH.`
- `PARSE the original formula while preserving every AND, OR, NOT, quantifier, and grouping.`
- `EVALUATE each atomic operand only from directly observed evidence.`
- `EVALUATE implication only as an observed premise-and-consequence proof obligation; never use vacuous truth.`
- `IF the formula result is FALSE: RETURN FALSE.`
- `IF direct_counterevidence is observed: RETURN FALSE.`
- `RETURN TRUE.`

Required observable atoms:
- Every atomic clause in the original formula is identified and evaluated in the stated scope.

Direct counterevidence:
- A directly observed atom contradicts a mandatory requirement.

## DP-020-C3 — Bad Defaults / Preselection — Semantic Intent of the Default Action

Operational reading: Evaluate the source formula over directly observed evidence; preserve every Boolean operator, quantifier, comparison, unit, scope, and threshold.

Pseudocode:
- `CONTEXT GATE: identify the rule's specific object/context and mechanism anchor before evaluation.`
- `IF only a generic UI object, generic price, generic CTA, or mere absence is observed: provide no support.`
- `IF a required atom is unavailable: provide no support, preserve the base judgment, and record the evidence as missing.`
- `IF a required atom depends on a UI transition: snapshot the scoped DOM, activate the most direct in-scope control once, wait for rendered settlement, and snapshot again.`
- `IF a visible standard next step is required by the rule: follow one such step and capture the resulting scoped DOM; an unverified click is missing evidence, not a negative.`
- `INPUT evidence_ledger, with state IDs and modality availability.`
- `CHECK the pattern-specific domain/context gate.`
- `IF the core context is contradicted: RETURN NO_MATCH.`
- `PARSE the original formula while preserving every AND, OR, NOT, quantifier, and grouping.`
- `EVALUATE each atomic operand only from directly observed evidence.`
- `EVALUATE implication only as an observed premise-and-consequence proof obligation; never use vacuous truth.`
- `IF the formula result is FALSE: RETURN FALSE.`
- `IF direct_counterevidence is observed: RETURN FALSE.`
- `RETURN TRUE.`

Required observable atoms:
- Every atomic clause in the original formula is identified and evaluated in the stated scope.

Direct counterevidence:
- A directly observed atom contradicts a mandatory requirement.

## DP-024-C1 — Trick Questions — Structural Label-Input Semantic Mismatch

Operational reading: Evaluate the source formula over directly observed evidence; preserve every Boolean operator, quantifier, comparison, unit, scope, and threshold.

Pseudocode:
- `CONTEXT GATE: identify the rule's specific object/context and mechanism anchor before evaluation.`
- `IF only a generic UI object, generic price, generic CTA, or mere absence is observed: provide no support.`
- `IF a required atom is unavailable: provide no support, preserve the base judgment, and record the evidence as missing.`
- `IF a required atom depends on a UI transition: snapshot the scoped DOM, activate the most direct in-scope control once, wait for rendered settlement, and snapshot again.`
- `IF a visible standard next step is required by the rule: follow one such step and capture the resulting scoped DOM; an unverified click is missing evidence, not a negative.`
- `INPUT evidence_ledger, with state IDs and modality availability.`
- `CHECK the pattern-specific domain/context gate.`
- `IF the core context is contradicted: RETURN NO_MATCH.`
- `PARSE the original formula while preserving every AND, OR, NOT, quantifier, and grouping.`
- `EVALUATE each atomic operand only from directly observed evidence.`
- `EVALUATE implication only as an observed premise-and-consequence proof obligation; never use vacuous truth.`
- `IF the formula result is FALSE: RETURN FALSE.`
- `IF direct_counterevidence is observed: RETURN FALSE.`
- `RETURN TRUE.`

Required observable atoms:
- Every atomic clause in the original formula is identified and evaluated in the stated scope.

Direct counterevidence:
- A directly observed atom contradicts a mandatory requirement.

## DP-024-C2 — Trick Questions — Affordance-Consequence Mismatch

Operational reading: Evaluate the source formula over directly observed evidence; preserve every Boolean operator, quantifier, comparison, unit, scope, and threshold.

Pseudocode:
- `CONTEXT GATE: identify the rule's specific object/context and mechanism anchor before evaluation.`
- `IF only a generic UI object, generic price, generic CTA, or mere absence is observed: provide no support.`
- `IF a required atom is unavailable: provide no support, preserve the base judgment, and record the evidence as missing.`
- `IF a required atom depends on a UI transition: snapshot the scoped DOM, activate the most direct in-scope control once, wait for rendered settlement, and snapshot again.`
- `IF a visible standard next step is required by the rule: follow one such step and capture the resulting scoped DOM; an unverified click is missing evidence, not a negative.`
- `INPUT evidence_ledger, with state IDs and modality availability.`
- `CHECK the pattern-specific domain/context gate.`
- `IF the core context is contradicted: RETURN NO_MATCH.`
- `PARSE the original formula while preserving every AND, OR, NOT, quantifier, and grouping.`
- `EVALUATE each atomic operand only from directly observed evidence.`
- `EVALUATE implication only as an observed premise-and-consequence proof obligation; never use vacuous truth.`
- `IF the formula result is FALSE: RETURN FALSE.`
- `IF direct_counterevidence is observed: RETURN FALSE.`
- `RETURN TRUE.`

Required observable atoms:
- Every atomic clause in the original formula is identified and evaluated in the stated scope.

Direct counterevidence:
- A directly observed atom contradicts a mandatory requirement.

## DP-024-C3 — Trick Questions — Syntactic Obfuscation via Multiple Negations

Operational reading: Evaluate the source formula over directly observed evidence; preserve every Boolean operator, quantifier, comparison, unit, scope, and threshold.

Pseudocode:
- `CONTEXT GATE: identify the rule's specific object/context and mechanism anchor before evaluation.`
- `IF only a generic UI object, generic price, generic CTA, or mere absence is observed: provide no support.`
- `IF a required atom is unavailable: provide no support, preserve the base judgment, and record the evidence as missing.`
- `IF a required atom depends on a UI transition: snapshot the scoped DOM, activate the most direct in-scope control once, wait for rendered settlement, and snapshot again.`
- `IF a visible standard next step is required by the rule: follow one such step and capture the resulting scoped DOM; an unverified click is missing evidence, not a negative.`
- `INPUT evidence_ledger, with state IDs and modality availability.`
- `CHECK the pattern-specific domain/context gate.`
- `IF the core context is contradicted: RETURN NO_MATCH.`
- `PARSE the original formula while preserving every AND, OR, NOT, quantifier, and grouping.`
- `EVALUATE each atomic operand only from directly observed evidence.`
- `EVALUATE implication only as an observed premise-and-consequence proof obligation; never use vacuous truth.`
- `IF the formula result is FALSE: RETURN FALSE.`
- `IF direct_counterevidence is observed: RETURN FALSE.`
- `RETURN TRUE.`

Required observable atoms:
- Every atomic clause in the original formula is identified and evaluated in the stated scope.

Direct counterevidence:
- A directly observed atom contradicts a mandatory requirement.

## DP-027-C1 — Wrong Language — Asymmetric State Application

Operational reading: Evaluate the source formula over directly observed evidence; preserve every Boolean operator, quantifier, comparison, unit, scope, and threshold.

Pseudocode:
- `CONTEXT GATE: identify the rule's specific object/context and mechanism anchor before evaluation.`
- `IF only a generic UI object, generic price, generic CTA, or mere absence is observed: provide no support.`
- `IF a required atom is unavailable: provide no support, preserve the base judgment, and record the evidence as missing.`
- `IF a required atom depends on a UI transition: snapshot the scoped DOM, activate the most direct in-scope control once, wait for rendered settlement, and snapshot again.`
- `IF a visible standard next step is required by the rule: follow one such step and capture the resulting scoped DOM; an unverified click is missing evidence, not a negative.`
- `INPUT evidence_ledger, with state IDs and modality availability.`
- `CHECK the pattern-specific domain/context gate.`
- `IF the core context is contradicted: RETURN NO_MATCH.`
- `PARSE the original formula while preserving every AND, OR, NOT, quantifier, and grouping.`
- `EVALUATE each atomic operand only from directly observed evidence.`
- `EVALUATE implication only as an observed premise-and-consequence proof obligation; never use vacuous truth.`
- `IF the formula result is FALSE: RETURN FALSE.`
- `IF direct_counterevidence is observed: RETURN FALSE.`
- `RETURN TRUE.`

Required observable atoms:
- Every atomic clause in the original formula is identified and evaluated in the stated scope.

Direct counterevidence:
- A directly observed atom contradicts a mandatory requirement.

## DP-027-C2 — Wrong Language — Visual-Linguistic Locale Mismatch

Operational reading: Evaluate the source formula over directly observed evidence; preserve every Boolean operator, quantifier, comparison, unit, scope, and threshold.

Pseudocode:
- `CONTEXT GATE: identify the rule's specific object/context and mechanism anchor before evaluation.`
- `IF only a generic UI object, generic price, generic CTA, or mere absence is observed: provide no support.`
- `IF a required atom is unavailable: provide no support, preserve the base judgment, and record the evidence as missing.`
- `IF a required atom depends on a UI transition: snapshot the scoped DOM, activate the most direct in-scope control once, wait for rendered settlement, and snapshot again.`
- `IF a visible standard next step is required by the rule: follow one such step and capture the resulting scoped DOM; an unverified click is missing evidence, not a negative.`
- `INPUT evidence_ledger, with state IDs and modality availability.`
- `CHECK the pattern-specific domain/context gate.`
- `IF the core context is contradicted: RETURN NO_MATCH.`
- `PARSE the original formula while preserving every AND, OR, NOT, quantifier, and grouping.`
- `EVALUATE each atomic operand only from directly observed evidence.`
- `EVALUATE implication only as an observed premise-and-consequence proof obligation; never use vacuous truth.`
- `IF the formula result is FALSE: RETURN FALSE.`
- `IF direct_counterevidence is observed: RETURN FALSE.`
- `RETURN TRUE.`

Required observable atoms:
- Every atomic clause in the original formula is identified and evaluated in the stated scope.

Direct counterevidence:
- A directly observed atom contradicts a mandatory requirement.

## DP-027-C3 — Wrong Language — Localized Linguistic Discrepancy

Operational reading: Evaluate the source formula over directly observed evidence; preserve every Boolean operator, quantifier, comparison, unit, scope, and threshold.

Pseudocode:
- `CONTEXT GATE: identify the rule's specific object/context and mechanism anchor before evaluation.`
- `IF only a generic UI object, generic price, generic CTA, or mere absence is observed: provide no support.`
- `IF a required atom is unavailable: provide no support, preserve the base judgment, and record the evidence as missing.`
- `IF a required atom depends on a UI transition: snapshot the scoped DOM, activate the most direct in-scope control once, wait for rendered settlement, and snapshot again.`
- `IF a visible standard next step is required by the rule: follow one such step and capture the resulting scoped DOM; an unverified click is missing evidence, not a negative.`
- `INPUT evidence_ledger, with state IDs and modality availability.`
- `CHECK the pattern-specific domain/context gate.`
- `IF the core context is contradicted: RETURN NO_MATCH.`
- `PARSE the original formula while preserving every AND, OR, NOT, quantifier, and grouping.`
- `EVALUATE each atomic operand only from directly observed evidence.`
- `EVALUATE implication only as an observed premise-and-consequence proof obligation; never use vacuous truth.`
- `IF the formula result is FALSE: RETURN FALSE.`
- `IF direct_counterevidence is observed: RETURN FALSE.`
- `RETURN TRUE.`

Required observable atoms:
- Every atomic clause in the original formula is identified and evaluated in the stated scope.

Direct counterevidence:
- A directly observed atom contradicts a mandatory requirement.

## DP-028-C1 — Complex Language — Structural Nesting Depth of Legal/Technical Text Nodes

Operational reading: Evaluate the source formula over directly observed evidence; preserve every Boolean operator, quantifier, comparison, unit, scope, and threshold.

Pseudocode:
- `CONTEXT GATE: identify the rule's specific object/context and mechanism anchor before evaluation.`
- `IF only a generic UI object, generic price, generic CTA, or mere absence is observed: provide no support.`
- `IF a required atom is unavailable: provide no support, preserve the base judgment, and record the evidence as missing.`
- `IF a required atom depends on a UI transition: snapshot the scoped DOM, activate the most direct in-scope control once, wait for rendered settlement, and snapshot again.`
- `IF a visible standard next step is required by the rule: follow one such step and capture the resulting scoped DOM; an unverified click is missing evidence, not a negative.`
- `INPUT evidence_ledger, with state IDs and modality availability.`
- `CHECK the pattern-specific domain/context gate.`
- `IF the core context is contradicted: RETURN NO_MATCH.`
- `PARSE the original formula while preserving every AND, OR, NOT, quantifier, and grouping.`
- `EVALUATE each atomic operand only from directly observed evidence.`
- `EVALUATE implication only as an observed premise-and-consequence proof obligation; never use vacuous truth.`
- `IF the formula result is FALSE: RETURN FALSE.`
- `IF direct_counterevidence is observed: RETURN FALSE.`
- `RETURN TRUE.`

Required observable atoms:
- Every atomic clause in the original formula is identified and evaluated in the stated scope.

Direct counterevidence:
- A directly observed atom contradicts a mandatory requirement.

## DP-028-C2 — Complex Language — Visual Density of Legalese Text Blocks

Operational reading: Evaluate the source formula over directly observed evidence; preserve every Boolean operator, quantifier, comparison, unit, scope, and threshold.

Pseudocode:
- `CONTEXT GATE: identify the rule's specific object/context and mechanism anchor before evaluation.`
- `IF only a generic UI object, generic price, generic CTA, or mere absence is observed: provide no support.`
- `IF a required atom is unavailable: provide no support, preserve the base judgment, and record the evidence as missing.`
- `IF a required atom depends on a UI transition: snapshot the scoped DOM, activate the most direct in-scope control once, wait for rendered settlement, and snapshot again.`
- `IF a visible standard next step is required by the rule: follow one such step and capture the resulting scoped DOM; an unverified click is missing evidence, not a negative.`
- `INPUT evidence_ledger, with state IDs and modality availability.`
- `CHECK the pattern-specific domain/context gate.`
- `IF the core context is contradicted: RETURN NO_MATCH.`
- `PARSE the original formula while preserving every AND, OR, NOT, quantifier, and grouping.`
- `EVALUATE each atomic operand only from directly observed evidence.`
- `EVALUATE implication only as an observed premise-and-consequence proof obligation; never use vacuous truth.`
- `IF the formula result is FALSE: RETURN FALSE.`
- `IF direct_counterevidence is observed: RETURN FALSE.`
- `RETURN TRUE.`

Required observable atoms:
- Every atomic clause in the original formula is identified and evaluated in the stated scope.

Direct counterevidence:
- A directly observed atom contradicts a mandatory requirement.

## DP-028-C3 — Complex Language — Exceedance of Baseline Readability Indices

Operational reading: Evaluate the source formula over directly observed evidence; preserve every Boolean operator, quantifier, comparison, unit, scope, and threshold.

Pseudocode:
- `CONTEXT GATE: identify the rule's specific object/context and mechanism anchor before evaluation.`
- `IF only a generic UI object, generic price, generic CTA, or mere absence is observed: provide no support.`
- `IF a required atom is unavailable: provide no support, preserve the base judgment, and record the evidence as missing.`
- `IF a required atom depends on a UI transition: snapshot the scoped DOM, activate the most direct in-scope control once, wait for rendered settlement, and snapshot again.`
- `IF a visible standard next step is required by the rule: follow one such step and capture the resulting scoped DOM; an unverified click is missing evidence, not a negative.`
- `INPUT evidence_ledger, with state IDs and modality availability.`
- `CHECK the pattern-specific domain/context gate.`
- `IF the core context is contradicted: RETURN NO_MATCH.`
- `PARSE the original formula while preserving every AND, OR, NOT, quantifier, and grouping.`
- `EVALUATE each atomic operand only from directly observed evidence.`
- `EVALUATE implication only as an observed premise-and-consequence proof obligation; never use vacuous truth.`
- `IF the formula result is FALSE: RETURN FALSE.`
- `IF direct_counterevidence is observed: RETURN FALSE.`
- `RETURN TRUE.`

Required observable atoms:
- Every atomic clause in the original formula is identified and evaluated in the stated scope.

Direct counterevidence:
- A directly observed atom contradicts a mandatory requirement.

## DP-029-C1 — Feedforward Ambiguity — Structural Ambiguity of Action-Outcome Mapping

Operational reading: Evaluate the source formula over directly observed evidence; preserve every Boolean operator, quantifier, comparison, unit, scope, and threshold.

Pseudocode:
- `CONTEXT GATE: identify the rule's specific object/context and mechanism anchor before evaluation.`
- `IF only a generic UI object, generic price, generic CTA, or mere absence is observed: provide no support.`
- `IF a required atom is unavailable: provide no support, preserve the base judgment, and record the evidence as missing.`
- `IF a required atom depends on a UI transition: snapshot the scoped DOM, activate the most direct in-scope control once, wait for rendered settlement, and snapshot again.`
- `IF a visible standard next step is required by the rule: follow one such step and capture the resulting scoped DOM; an unverified click is missing evidence, not a negative.`
- `INPUT evidence_ledger, with state IDs and modality availability.`
- `CHECK the pattern-specific domain/context gate.`
- `IF the core context is contradicted: RETURN NO_MATCH.`
- `PARSE the original formula while preserving every AND, OR, NOT, quantifier, and grouping.`
- `EVALUATE each atomic operand only from directly observed evidence.`
- `EVALUATE implication only as an observed premise-and-consequence proof obligation; never use vacuous truth.`
- `IF the formula result is FALSE: RETURN FALSE.`
- `IF direct_counterevidence is observed: RETURN FALSE.`
- `RETURN TRUE.`

Required observable atoms:
- Every atomic clause in the original formula is identified and evaluated in the stated scope.

Direct counterevidence:
- A directly observed atom contradicts a mandatory requirement.

## DP-029-C2 — Feedforward Ambiguity — Iconographic Entropy and Missing Affordances

Operational reading: Evaluate the source formula over directly observed evidence; preserve every Boolean operator, quantifier, comparison, unit, scope, and threshold.

Pseudocode:
- `CONTEXT GATE: identify the rule's specific object/context and mechanism anchor before evaluation.`
- `IF only a generic UI object, generic price, generic CTA, or mere absence is observed: provide no support.`
- `IF a required atom is unavailable: provide no support, preserve the base judgment, and record the evidence as missing.`
- `IF a required atom depends on a UI transition: snapshot the scoped DOM, activate the most direct in-scope control once, wait for rendered settlement, and snapshot again.`
- `IF a visible standard next step is required by the rule: follow one such step and capture the resulting scoped DOM; an unverified click is missing evidence, not a negative.`
- `INPUT evidence_ledger, with state IDs and modality availability.`
- `CHECK the pattern-specific domain/context gate.`
- `IF the core context is contradicted: RETURN NO_MATCH.`
- `PARSE the original formula while preserving every AND, OR, NOT, quantifier, and grouping.`
- `EVALUATE each atomic operand only from directly observed evidence.`
- `EVALUATE implication only as an observed premise-and-consequence proof obligation; never use vacuous truth.`
- `IF the formula result is FALSE: RETURN FALSE.`
- `IF direct_counterevidence is observed: RETURN FALSE.`
- `RETURN TRUE.`

Required observable atoms:
- Every atomic clause in the original formula is identified and evaluated in the stated scope.

Direct counterevidence:
- A directly observed atom contradicts a mandatory requirement.

## DP-029-C3 — Feedforward Ambiguity — Semantic Divergence of Action and Outcome

Operational reading: Evaluate the source formula over directly observed evidence; preserve every Boolean operator, quantifier, comparison, unit, scope, and threshold.

Pseudocode:
- `CONTEXT GATE: identify the rule's specific object/context and mechanism anchor before evaluation.`
- `IF only a generic UI object, generic price, generic CTA, or mere absence is observed: provide no support.`
- `IF a required atom is unavailable: provide no support, preserve the base judgment, and record the evidence as missing.`
- `IF a required atom depends on a UI transition: snapshot the scoped DOM, activate the most direct in-scope control once, wait for rendered settlement, and snapshot again.`
- `IF a visible standard next step is required by the rule: follow one such step and capture the resulting scoped DOM; an unverified click is missing evidence, not a negative.`
- `INPUT evidence_ledger, with state IDs and modality availability.`
- `CHECK the pattern-specific domain/context gate.`
- `IF the core context is contradicted: RETURN NO_MATCH.`
- `PARSE the original formula while preserving every AND, OR, NOT, quantifier, and grouping.`
- `EVALUATE each atomic operand only from directly observed evidence.`
- `EVALUATE implication only as an observed premise-and-consequence proof obligation; never use vacuous truth.`
- `IF the formula result is FALSE: RETURN FALSE.`
- `IF direct_counterevidence is observed: RETURN FALSE.`
- `RETURN TRUE.`

Required observable atoms:
- Every atomic clause in the original formula is identified and evaluated in the stated scope.

Direct counterevidence:
- A directly observed atom contradicts a mandatory requirement.

## DP-037-C1 — Forced Registration — Absolute State Blocking

Operational reading: Evaluate the source formula over directly observed evidence; preserve every Boolean operator, quantifier, comparison, unit, scope, and threshold.

Pseudocode:
- `CONTEXT GATE: identify the rule's specific object/context and mechanism anchor before evaluation.`
- `IF only a generic UI object, generic price, generic CTA, or mere absence is observed: provide no support.`
- `IF a required atom is unavailable: provide no support, preserve the base judgment, and record the evidence as missing.`
- `IF a required atom depends on a UI transition: snapshot the scoped DOM, activate the most direct in-scope control once, wait for rendered settlement, and snapshot again.`
- `IF a visible standard next step is required by the rule: follow one such step and capture the resulting scoped DOM; an unverified click is missing evidence, not a negative.`
- `INPUT evidence_ledger, with state IDs and modality availability.`
- `CHECK the pattern-specific domain/context gate.`
- `IF the core context is contradicted: RETURN NO_MATCH.`
- `PARSE the original formula while preserving every AND, OR, NOT, quantifier, and grouping.`
- `EVALUATE each atomic operand only from directly observed evidence.`
- `EVALUATE implication only as an observed premise-and-consequence proof obligation; never use vacuous truth.`
- `IF the formula result is FALSE: RETURN FALSE.`
- `IF direct_counterevidence is observed: RETURN FALSE.`
- `RETURN TRUE.`

Required observable atoms:
- Every atomic clause in the original formula is identified and evaluated in the stated scope.

Direct counterevidence:
- A directly observed atom contradicts a mandatory requirement.

## DP-037-C2 — Forced Registration — Visual Degradation of the Guest Checkout Pathway

Operational reading: Evaluate the source formula over directly observed evidence; preserve every Boolean operator, quantifier, comparison, unit, scope, and threshold.

Pseudocode:
- `CONTEXT GATE: identify the rule's specific object/context and mechanism anchor before evaluation.`
- `IF only a generic UI object, generic price, generic CTA, or mere absence is observed: provide no support.`
- `IF a required atom is unavailable: provide no support, preserve the base judgment, and record the evidence as missing.`
- `IF a required atom depends on a UI transition: snapshot the scoped DOM, activate the most direct in-scope control once, wait for rendered settlement, and snapshot again.`
- `IF a visible standard next step is required by the rule: follow one such step and capture the resulting scoped DOM; an unverified click is missing evidence, not a negative.`
- `INPUT evidence_ledger, with state IDs and modality availability.`
- `CHECK the pattern-specific domain/context gate.`
- `IF the core context is contradicted: RETURN NO_MATCH.`
- `PARSE the original formula while preserving every AND, OR, NOT, quantifier, and grouping.`
- `EVALUATE each atomic operand only from directly observed evidence.`
- `EVALUATE implication only as an observed premise-and-consequence proof obligation; never use vacuous truth.`
- `IF the formula result is FALSE: RETURN FALSE.`
- `IF direct_counterevidence is observed: RETURN FALSE.`
- `RETURN TRUE.`

Required observable atoms:
- Every atomic clause in the original formula is identified and evaluated in the stated scope.

Direct counterevidence:
- A directly observed atom contradicts a mandatory requirement.

## DP-037-C3 — Forced Registration — Semantic Framing of the Guest-Checkout Option

Operational reading: Evaluate the source formula over directly observed evidence; preserve every Boolean operator, quantifier, comparison, unit, scope, and threshold.

Pseudocode:
- `CONTEXT GATE: identify the rule's specific object/context and mechanism anchor before evaluation.`
- `IF only a generic UI object, generic price, generic CTA, or mere absence is observed: provide no support.`
- `IF a required atom is unavailable: provide no support, preserve the base judgment, and record the evidence as missing.`
- `IF a required atom depends on a UI transition: snapshot the scoped DOM, activate the most direct in-scope control once, wait for rendered settlement, and snapshot again.`
- `IF a visible standard next step is required by the rule: follow one such step and capture the resulting scoped DOM; an unverified click is missing evidence, not a negative.`
- `INPUT evidence_ledger, with state IDs and modality availability.`
- `CHECK the pattern-specific domain/context gate.`
- `IF the core context is contradicted: RETURN NO_MATCH.`
- `PARSE the original formula while preserving every AND, OR, NOT, quantifier, and grouping.`
- `EVALUATE each atomic operand only from directly observed evidence.`
- `EVALUATE implication only as an observed premise-and-consequence proof obligation; never use vacuous truth.`
- `IF the formula result is FALSE: RETURN FALSE.`
- `IF direct_counterevidence is observed: RETURN FALSE.`
- `RETURN TRUE.`

Required observable atoms:
- Every atomic clause in the original formula is identified and evaluated in the stated scope.

Direct counterevidence:
- A directly observed atom contradicts a mandatory requirement.

## DP-041-C1 — Social Pyramid — Referral-Gated Progression

Operational reading: Evaluate the source formula over directly observed evidence; preserve every Boolean operator, quantifier, comparison, unit, scope, and threshold.

Pseudocode:
- `CONTEXT GATE: identify the rule's specific object/context and mechanism anchor before evaluation.`
- `IF only a generic UI object, generic price, generic CTA, or mere absence is observed: provide no support.`
- `IF a required atom is unavailable: provide no support, preserve the base judgment, and record the evidence as missing.`
- `IF a required atom depends on a UI transition: snapshot the scoped DOM, activate the most direct in-scope control once, wait for rendered settlement, and snapshot again.`
- `IF a visible standard next step is required by the rule: follow one such step and capture the resulting scoped DOM; an unverified click is missing evidence, not a negative.`
- `INPUT evidence_ledger, with state IDs and modality availability.`
- `CHECK the pattern-specific domain/context gate.`
- `IF the core context is contradicted: RETURN NO_MATCH.`
- `PARSE the original formula while preserving every AND, OR, NOT, quantifier, and grouping.`
- `EVALUATE each atomic operand only from directly observed evidence.`
- `EVALUATE implication only as an observed premise-and-consequence proof obligation; never use vacuous truth.`
- `IF the formula result is FALSE: RETURN FALSE.`
- `IF direct_counterevidence is observed: RETURN FALSE.`
- `RETURN TRUE.`

Required observable atoms:
- Every atomic clause in the original formula is identified and evaluated in the stated scope.

Direct counterevidence:
- A directly observed atom contradicts a mandatory requirement.

## DP-041-C2 — Social Pyramid — Visual Prominence of Referral-Progress Gamification

Operational reading: Evaluate the source formula over directly observed evidence; preserve every Boolean operator, quantifier, comparison, unit, scope, and threshold.

Pseudocode:
- `CONTEXT GATE: identify the rule's specific object/context and mechanism anchor before evaluation.`
- `IF only a generic UI object, generic price, generic CTA, or mere absence is observed: provide no support.`
- `IF a required atom is unavailable: provide no support, preserve the base judgment, and record the evidence as missing.`
- `IF a required atom depends on a UI transition: snapshot the scoped DOM, activate the most direct in-scope control once, wait for rendered settlement, and snapshot again.`
- `IF a visible standard next step is required by the rule: follow one such step and capture the resulting scoped DOM; an unverified click is missing evidence, not a negative.`
- `INPUT evidence_ledger, with state IDs and modality availability.`
- `CHECK the pattern-specific domain/context gate.`
- `IF the core context is contradicted: RETURN NO_MATCH.`
- `PARSE the original formula while preserving every AND, OR, NOT, quantifier, and grouping.`
- `EVALUATE each atomic operand only from directly observed evidence.`
- `EVALUATE implication only as an observed premise-and-consequence proof obligation; never use vacuous truth.`
- `IF the formula result is FALSE: RETURN FALSE.`
- `IF direct_counterevidence is observed: RETURN FALSE.`
- `RETURN TRUE.`

Required observable atoms:
- Every atomic clause in the original formula is identified and evaluated in the stated scope.

Direct counterevidence:
- A directly observed atom contradicts a mandatory requirement.

## DP-041-C3 — Social Pyramid — Semantic Escalation of Referral-Reward Language

Operational reading: Evaluate the source formula over directly observed evidence; preserve every Boolean operator, quantifier, comparison, unit, scope, and threshold.

Pseudocode:
- `CONTEXT GATE: identify the rule's specific object/context and mechanism anchor before evaluation.`
- `IF only a generic UI object, generic price, generic CTA, or mere absence is observed: provide no support.`
- `IF a required atom is unavailable: provide no support, preserve the base judgment, and record the evidence as missing.`
- `IF a required atom depends on a UI transition: snapshot the scoped DOM, activate the most direct in-scope control once, wait for rendered settlement, and snapshot again.`
- `IF a visible standard next step is required by the rule: follow one such step and capture the resulting scoped DOM; an unverified click is missing evidence, not a negative.`
- `INPUT evidence_ledger, with state IDs and modality availability.`
- `CHECK the pattern-specific domain/context gate.`
- `IF the core context is contradicted: RETURN NO_MATCH.`
- `PARSE the original formula while preserving every AND, OR, NOT, quantifier, and grouping.`
- `EVALUATE each atomic operand only from directly observed evidence.`
- `EVALUATE implication only as an observed premise-and-consequence proof obligation; never use vacuous truth.`
- `IF the formula result is FALSE: RETURN FALSE.`
- `IF direct_counterevidence is observed: RETURN FALSE.`
- `RETURN TRUE.`

Required observable atoms:
- Every atomic clause in the original formula is identified and evaluated in the stated scope.

Direct counterevidence:
- A directly observed atom contradicts a mandatory requirement.

## DP-042-C1 — Granting and Interaction — Interaction Gating

Operational reading: Evaluate the source formula over directly observed evidence; preserve every Boolean operator, quantifier, comparison, unit, scope, and threshold.

Pseudocode:
- `CONTEXT GATE: identify the rule's specific object/context and mechanism anchor before evaluation.`
- `IF only a generic UI object, generic price, generic CTA, or mere absence is observed: provide no support.`
- `IF a required atom is unavailable: provide no support, preserve the base judgment, and record the evidence as missing.`
- `IF a required atom depends on a UI transition: snapshot the scoped DOM, activate the most direct in-scope control once, wait for rendered settlement, and snapshot again.`
- `IF a visible standard next step is required by the rule: follow one such step and capture the resulting scoped DOM; an unverified click is missing evidence, not a negative.`
- `INPUT evidence_ledger, with state IDs and modality availability.`
- `CHECK the pattern-specific domain/context gate.`
- `IF the core context is contradicted: RETURN NO_MATCH.`
- `PARSE the original formula while preserving every AND, OR, NOT, quantifier, and grouping.`
- `EVALUATE each atomic operand only from directly observed evidence.`
- `EVALUATE implication only as an observed premise-and-consequence proof obligation; never use vacuous truth.`
- `IF the formula result is FALSE: RETURN FALSE.`
- `IF direct_counterevidence is observed: RETURN FALSE.`
- `RETURN TRUE.`

Required observable atoms:
- Every atomic clause in the original formula is identified and evaluated in the stated scope.

Direct counterevidence:
- A directly observed atom contradicts a mandatory requirement.

## DP-042-C2 — Granting and Interaction — Asynchronous Overlay Misdirection

Operational reading: Evaluate the source formula over directly observed evidence; preserve every Boolean operator, quantifier, comparison, unit, scope, and threshold.

Pseudocode:
- `CONTEXT GATE: identify the rule's specific object/context and mechanism anchor before evaluation.`
- `IF only a generic UI object, generic price, generic CTA, or mere absence is observed: provide no support.`
- `IF a required atom is unavailable: provide no support, preserve the base judgment, and record the evidence as missing.`
- `IF a required atom depends on a UI transition: snapshot the scoped DOM, activate the most direct in-scope control once, wait for rendered settlement, and snapshot again.`
- `IF a visible standard next step is required by the rule: follow one such step and capture the resulting scoped DOM; an unverified click is missing evidence, not a negative.`
- `INPUT evidence_ledger, with state IDs and modality availability.`
- `CHECK the pattern-specific domain/context gate.`
- `IF the core context is contradicted: RETURN NO_MATCH.`
- `PARSE the original formula while preserving every AND, OR, NOT, quantifier, and grouping.`
- `EVALUATE each atomic operand only from directly observed evidence.`
- `EVALUATE implication only as an observed premise-and-consequence proof obligation; never use vacuous truth.`
- `IF the formula result is FALSE: RETURN FALSE.`
- `IF direct_counterevidence is observed: RETURN FALSE.`
- `RETURN TRUE.`

Required observable atoms:
- Every atomic clause in the original formula is identified and evaluated in the stated scope.

Direct counterevidence:
- A directly observed atom contradicts a mandatory requirement.

## DP-042-C3 — Granting and Interaction — Semantic Scope Creep in Permission Requests

Operational reading: Evaluate the source formula over directly observed evidence; preserve every Boolean operator, quantifier, comparison, unit, scope, and threshold.

Pseudocode:
- `CONTEXT GATE: identify the rule's specific object/context and mechanism anchor before evaluation.`
- `IF only a generic UI object, generic price, generic CTA, or mere absence is observed: provide no support.`
- `IF a required atom is unavailable: provide no support, preserve the base judgment, and record the evidence as missing.`
- `IF a required atom depends on a UI transition: snapshot the scoped DOM, activate the most direct in-scope control once, wait for rendered settlement, and snapshot again.`
- `IF a visible standard next step is required by the rule: follow one such step and capture the resulting scoped DOM; an unverified click is missing evidence, not a negative.`
- `INPUT evidence_ledger, with state IDs and modality availability.`
- `CHECK the pattern-specific domain/context gate.`
- `IF the core context is contradicted: RETURN NO_MATCH.`
- `PARSE the original formula while preserving every AND, OR, NOT, quantifier, and grouping.`
- `EVALUATE each atomic operand only from directly observed evidence.`
- `EVALUATE implication only as an observed premise-and-consequence proof obligation; never use vacuous truth.`
- `IF the formula result is FALSE: RETURN FALSE.`
- `IF direct_counterevidence is observed: RETURN FALSE.`
- `RETURN TRUE.`

Required observable atoms:
- Every atomic clause in the original formula is identified and evaluated in the stated scope.

Direct counterevidence:
- A directly observed atom contradicts a mandatory requirement.

## DP-043-C1 — Pay-To-Play — Increasing Free Effort and Paid Bypass

Operational reading: Evaluate the source formula over directly observed evidence; preserve every Boolean operator, quantifier, comparison, unit, scope, and threshold.

Pseudocode:
- `CONTEXT GATE: identify the rule's specific object/context and mechanism anchor before evaluation.`
- `IF only a generic UI object, generic price, generic CTA, or mere absence is observed: provide no support.`
- `IF a required atom is unavailable: provide no support, preserve the base judgment, and record the evidence as missing.`
- `IF a required atom depends on a UI transition: snapshot the scoped DOM, activate the most direct in-scope control once, wait for rendered settlement, and snapshot again.`
- `IF a visible standard next step is required by the rule: follow one such step and capture the resulting scoped DOM; an unverified click is missing evidence, not a negative.`
- `INPUT evidence_ledger, with state IDs and modality availability.`
- `CHECK the pattern-specific domain/context gate.`
- `IF the core context is contradicted: RETURN NO_MATCH.`
- `PARSE the original formula while preserving every AND, OR, NOT, quantifier, and grouping.`
- `EVALUATE each atomic operand only from directly observed evidence.`
- `EVALUATE implication only as an observed premise-and-consequence proof obligation; never use vacuous truth.`
- `IF the formula result is FALSE: RETURN FALSE.`
- `IF direct_counterevidence is observed: RETURN FALSE.`
- `RETURN TRUE.`

Required observable atoms:
- Every atomic clause in the original formula is identified and evaluated in the stated scope.

Direct counterevidence:
- A directly observed atom contradicts a mandatory requirement.

## DP-043-C2 — Pay-To-Play — Visual Dominance of Payment-Unlock Overlays

Operational reading: Evaluate the source formula over directly observed evidence; preserve every Boolean operator, quantifier, comparison, unit, scope, and threshold.

Pseudocode:
- `CONTEXT GATE: identify the rule's specific object/context and mechanism anchor before evaluation.`
- `IF only a generic UI object, generic price, generic CTA, or mere absence is observed: provide no support.`
- `IF a required atom is unavailable: provide no support, preserve the base judgment, and record the evidence as missing.`
- `IF a required atom depends on a UI transition: snapshot the scoped DOM, activate the most direct in-scope control once, wait for rendered settlement, and snapshot again.`
- `IF a visible standard next step is required by the rule: follow one such step and capture the resulting scoped DOM; an unverified click is missing evidence, not a negative.`
- `INPUT evidence_ledger, with state IDs and modality availability.`
- `CHECK the pattern-specific domain/context gate.`
- `IF the core context is contradicted: RETURN NO_MATCH.`
- `PARSE the original formula while preserving every AND, OR, NOT, quantifier, and grouping.`
- `EVALUATE each atomic operand only from directly observed evidence.`
- `EVALUATE implication only as an observed premise-and-consequence proof obligation; never use vacuous truth.`
- `IF the formula result is FALSE: RETURN FALSE.`
- `IF direct_counterevidence is observed: RETURN FALSE.`
- `RETURN TRUE.`

Required observable atoms:
- Every atomic clause in the original formula is identified and evaluated in the stated scope.

Direct counterevidence:
- A directly observed atom contradicts a mandatory requirement.

## DP-043-C3 — Pay-To-Play — Semantic Framing of Payment as Unlock

Operational reading: Evaluate the source formula over directly observed evidence; preserve every Boolean operator, quantifier, comparison, unit, scope, and threshold.

Pseudocode:
- `CONTEXT GATE: identify the rule's specific object/context and mechanism anchor before evaluation.`
- `IF only a generic UI object, generic price, generic CTA, or mere absence is observed: provide no support.`
- `IF a required atom is unavailable: provide no support, preserve the base judgment, and record the evidence as missing.`
- `IF a required atom depends on a UI transition: snapshot the scoped DOM, activate the most direct in-scope control once, wait for rendered settlement, and snapshot again.`
- `IF a visible standard next step is required by the rule: follow one such step and capture the resulting scoped DOM; an unverified click is missing evidence, not a negative.`
- `INPUT evidence_ledger, with state IDs and modality availability.`
- `CHECK the pattern-specific domain/context gate.`
- `IF the core context is contradicted: RETURN NO_MATCH.`
- `PARSE the original formula while preserving every AND, OR, NOT, quantifier, and grouping.`
- `EVALUATE each atomic operand only from directly observed evidence.`
- `EVALUATE implication only as an observed premise-and-consequence proof obligation; never use vacuous truth.`
- `IF the formula result is FALSE: RETURN FALSE.`
- `IF direct_counterevidence is observed: RETURN FALSE.`
- `RETURN TRUE.`

Required observable atoms:
- Every atomic clause in the original formula is identified and evaluated in the stated scope.

Direct counterevidence:
- A directly observed atom contradicts a mandatory requirement.

## DP-044-C1 — Grinding — Increasing Effort with Limited Reward Change

Operational reading: Evaluate the source formula over directly observed evidence; preserve every Boolean operator, quantifier, comparison, unit, scope, and threshold.

Pseudocode:
- `CONTEXT GATE: identify the rule's specific object/context and mechanism anchor before evaluation.`
- `IF only a generic UI object, generic price, generic CTA, or mere absence is observed: provide no support.`
- `IF a required atom is unavailable: provide no support, preserve the base judgment, and record the evidence as missing.`
- `IF a required atom depends on a UI transition: snapshot the scoped DOM, activate the most direct in-scope control once, wait for rendered settlement, and snapshot again.`
- `IF a visible standard next step is required by the rule: follow one such step and capture the resulting scoped DOM; an unverified click is missing evidence, not a negative.`
- `INPUT evidence_ledger, with state IDs and modality availability.`
- `CHECK the pattern-specific domain/context gate.`
- `IF the core context is contradicted: RETURN NO_MATCH.`
- `PARSE the original formula while preserving every AND, OR, NOT, quantifier, and grouping.`
- `EVALUATE each atomic operand only from directly observed evidence.`
- `EVALUATE implication only as an observed premise-and-consequence proof obligation; never use vacuous truth.`
- `IF the formula result is FALSE: RETURN FALSE.`
- `IF direct_counterevidence is observed: RETURN FALSE.`
- `RETURN TRUE.`

Required observable atoms:
- Every atomic clause in the original formula is identified and evaluated in the stated scope.

Direct counterevidence:
- A directly observed atom contradicts a mandatory requirement.

## DP-044-C2 — Grinding — Visual Diminishing-Returns Feedback Loop

Operational reading: Evaluate the source formula over directly observed evidence; preserve every Boolean operator, quantifier, comparison, unit, scope, and threshold.

Pseudocode:
- `CONTEXT GATE: identify the rule's specific object/context and mechanism anchor before evaluation.`
- `IF only a generic UI object, generic price, generic CTA, or mere absence is observed: provide no support.`
- `IF a required atom is unavailable: provide no support, preserve the base judgment, and record the evidence as missing.`
- `IF a required atom depends on a UI transition: snapshot the scoped DOM, activate the most direct in-scope control once, wait for rendered settlement, and snapshot again.`
- `IF a visible standard next step is required by the rule: follow one such step and capture the resulting scoped DOM; an unverified click is missing evidence, not a negative.`
- `INPUT evidence_ledger, with state IDs and modality availability.`
- `CHECK the pattern-specific domain/context gate.`
- `IF the core context is contradicted: RETURN NO_MATCH.`
- `PARSE the original formula while preserving every AND, OR, NOT, quantifier, and grouping.`
- `EVALUATE each atomic operand only from directly observed evidence.`
- `EVALUATE implication only as an observed premise-and-consequence proof obligation; never use vacuous truth.`
- `IF the formula result is FALSE: RETURN FALSE.`
- `IF direct_counterevidence is observed: RETURN FALSE.`
- `RETURN TRUE.`

Required observable atoms:
- Every atomic clause in the original formula is identified and evaluated in the stated scope.

Direct counterevidence:
- A directly observed atom contradicts a mandatory requirement.

## DP-044-C3 — Grinding — Semantic Attenuation of Progress-Milestone Language

Operational reading: Evaluate the source formula over directly observed evidence; preserve every Boolean operator, quantifier, comparison, unit, scope, and threshold.

Pseudocode:
- `CONTEXT GATE: identify the rule's specific object/context and mechanism anchor before evaluation.`
- `IF only a generic UI object, generic price, generic CTA, or mere absence is observed: provide no support.`
- `IF a required atom is unavailable: provide no support, preserve the base judgment, and record the evidence as missing.`
- `IF a required atom depends on a UI transition: snapshot the scoped DOM, activate the most direct in-scope control once, wait for rendered settlement, and snapshot again.`
- `IF a visible standard next step is required by the rule: follow one such step and capture the resulting scoped DOM; an unverified click is missing evidence, not a negative.`
- `INPUT evidence_ledger, with state IDs and modality availability.`
- `CHECK the pattern-specific domain/context gate.`
- `IF the core context is contradicted: RETURN NO_MATCH.`
- `PARSE the original formula while preserving every AND, OR, NOT, quantifier, and grouping.`
- `EVALUATE each atomic operand only from directly observed evidence.`
- `EVALUATE implication only as an observed premise-and-consequence proof obligation; never use vacuous truth.`
- `IF the formula result is FALSE: RETURN FALSE.`
- `IF direct_counterevidence is observed: RETURN FALSE.`
- `RETURN TRUE.`

Required observable atoms:
- Every atomic clause in the original formula is identified and evaluated in the stated scope.

Direct counterevidence:
- A directly observed atom contradicts a mandatory requirement.

## DP-045-C1 — Playing By Appointment — Temporal Gating

Operational reading: Evaluate the source formula over directly observed evidence; preserve every Boolean operator, quantifier, comparison, unit, scope, and threshold.

Pseudocode:
- `CONTEXT GATE: identify the rule's specific object/context and mechanism anchor before evaluation.`
- `IF only a generic UI object, generic price, generic CTA, or mere absence is observed: provide no support.`
- `IF a required atom is unavailable: provide no support, preserve the base judgment, and record the evidence as missing.`
- `IF a required atom depends on a UI transition: snapshot the scoped DOM, activate the most direct in-scope control once, wait for rendered settlement, and snapshot again.`
- `IF a visible standard next step is required by the rule: follow one such step and capture the resulting scoped DOM; an unverified click is missing evidence, not a negative.`
- `INPUT evidence_ledger, with state IDs and modality availability.`
- `CHECK the pattern-specific domain/context gate.`
- `IF the core context is contradicted: RETURN NO_MATCH.`
- `PARSE the original formula while preserving every AND, OR, NOT, quantifier, and grouping.`
- `EVALUATE each atomic operand only from directly observed evidence.`
- `EVALUATE implication only as an observed premise-and-consequence proof obligation; never use vacuous truth.`
- `IF the formula result is FALSE: RETURN FALSE.`
- `IF direct_counterevidence is observed: RETURN FALSE.`
- `RETURN TRUE.`

Required observable atoms:
- Every atomic clause in the original formula is identified and evaluated in the stated scope.

Direct counterevidence:
- A directly observed atom contradicts a mandatory requirement.

## DP-045-C2 — Playing By Appointment — Visual Prominence of Temporal-Gating Indicators

Operational reading: Evaluate the source formula over directly observed evidence; preserve every Boolean operator, quantifier, comparison, unit, scope, and threshold.

Pseudocode:
- `CONTEXT GATE: identify the rule's specific object/context and mechanism anchor before evaluation.`
- `IF only a generic UI object, generic price, generic CTA, or mere absence is observed: provide no support.`
- `IF a required atom is unavailable: provide no support, preserve the base judgment, and record the evidence as missing.`
- `IF a required atom depends on a UI transition: snapshot the scoped DOM, activate the most direct in-scope control once, wait for rendered settlement, and snapshot again.`
- `IF a visible standard next step is required by the rule: follow one such step and capture the resulting scoped DOM; an unverified click is missing evidence, not a negative.`
- `INPUT evidence_ledger, with state IDs and modality availability.`
- `CHECK the pattern-specific domain/context gate.`
- `IF the core context is contradicted: RETURN NO_MATCH.`
- `PARSE the original formula while preserving every AND, OR, NOT, quantifier, and grouping.`
- `EVALUATE each atomic operand only from directly observed evidence.`
- `EVALUATE implication only as an observed premise-and-consequence proof obligation; never use vacuous truth.`
- `IF the formula result is FALSE: RETURN FALSE.`
- `IF direct_counterevidence is observed: RETURN FALSE.`
- `RETURN TRUE.`

Required observable atoms:
- Every atomic clause in the original formula is identified and evaluated in the stated scope.

Direct counterevidence:
- A directly observed atom contradicts a mandatory requirement.

## DP-045-C3 — Playing By Appointment — Semantic Urgency Encoding in Temporal-Gating Messages

Operational reading: Evaluate the source formula over directly observed evidence; preserve every Boolean operator, quantifier, comparison, unit, scope, and threshold.

Pseudocode:
- `CONTEXT GATE: identify the rule's specific object/context and mechanism anchor before evaluation.`
- `IF only a generic UI object, generic price, generic CTA, or mere absence is observed: provide no support.`
- `IF a required atom is unavailable: provide no support, preserve the base judgment, and record the evidence as missing.`
- `IF a required atom depends on a UI transition: snapshot the scoped DOM, activate the most direct in-scope control once, wait for rendered settlement, and snapshot again.`
- `IF a visible standard next step is required by the rule: follow one such step and capture the resulting scoped DOM; an unverified click is missing evidence, not a negative.`
- `INPUT evidence_ledger, with state IDs and modality availability.`
- `CHECK the pattern-specific domain/context gate.`
- `IF the core context is contradicted: RETURN NO_MATCH.`
- `PARSE the original formula while preserving every AND, OR, NOT, quantifier, and grouping.`
- `EVALUATE each atomic operand only from directly observed evidence.`
- `EVALUATE implication only as an observed premise-and-consequence proof obligation; never use vacuous truth.`
- `IF the formula result is FALSE: RETURN FALSE.`
- `IF direct_counterevidence is observed: RETURN FALSE.`
- `RETURN TRUE.`

Required observable atoms:
- Every atomic clause in the original formula is identified and evaluated in the stated scope.

Direct counterevidence:
- A directly observed atom contradicts a mandatory requirement.

## DP-047-C1 — Watch Ads To Unlock Features Or Get Rewards — Attention as Transactional Currency

Operational reading: Evaluate the source formula over directly observed evidence; preserve every Boolean operator, quantifier, comparison, unit, scope, and threshold.

Pseudocode:
- `CONTEXT GATE: identify the rule's specific object/context and mechanism anchor before evaluation.`
- `IF only a generic UI object, generic price, generic CTA, or mere absence is observed: provide no support.`
- `IF a required atom is unavailable: provide no support, preserve the base judgment, and record the evidence as missing.`
- `IF a required atom depends on a UI transition: snapshot the scoped DOM, activate the most direct in-scope control once, wait for rendered settlement, and snapshot again.`
- `IF a visible standard next step is required by the rule: follow one such step and capture the resulting scoped DOM; an unverified click is missing evidence, not a negative.`
- `INPUT evidence_ledger, with state IDs and modality availability.`
- `CHECK the pattern-specific domain/context gate.`
- `IF the core context is contradicted: RETURN NO_MATCH.`
- `PARSE the original formula while preserving every AND, OR, NOT, quantifier, and grouping.`
- `EVALUATE each atomic operand only from directly observed evidence.`
- `EVALUATE implication only as an observed premise-and-consequence proof obligation; never use vacuous truth.`
- `IF the formula result is FALSE: RETURN FALSE.`
- `IF direct_counterevidence is observed: RETURN FALSE.`
- `RETURN TRUE.`

Required observable atoms:
- Every atomic clause in the original formula is identified and evaluated in the stated scope.

Direct counterevidence:
- A directly observed atom contradicts a mandatory requirement.

## DP-047-C2 — Watch Ads To Unlock Features Or Get Rewards — Visual Mismatch Between Reward Progress Display and Actual Progress

Operational reading: Evaluate the source formula over directly observed evidence; preserve every Boolean operator, quantifier, comparison, unit, scope, and threshold.

Pseudocode:
- `CONTEXT GATE: identify the rule's specific object/context and mechanism anchor before evaluation.`
- `IF only a generic UI object, generic price, generic CTA, or mere absence is observed: provide no support.`
- `IF a required atom is unavailable: provide no support, preserve the base judgment, and record the evidence as missing.`
- `IF a required atom depends on a UI transition: snapshot the scoped DOM, activate the most direct in-scope control once, wait for rendered settlement, and snapshot again.`
- `IF a visible standard next step is required by the rule: follow one such step and capture the resulting scoped DOM; an unverified click is missing evidence, not a negative.`
- `INPUT evidence_ledger, with state IDs and modality availability.`
- `CHECK the pattern-specific domain/context gate.`
- `IF the core context is contradicted: RETURN NO_MATCH.`
- `PARSE the original formula while preserving every AND, OR, NOT, quantifier, and grouping.`
- `EVALUATE each atomic operand only from directly observed evidence.`
- `EVALUATE implication only as an observed premise-and-consequence proof obligation; never use vacuous truth.`
- `IF the formula result is FALSE: RETURN FALSE.`
- `IF direct_counterevidence is observed: RETURN FALSE.`
- `RETURN TRUE.`

Required observable atoms:
- Every atomic clause in the original formula is identified and evaluated in the stated scope.

Direct counterevidence:
- A directly observed atom contradicts a mandatory requirement.

## DP-047-C3 — Watch Ads To Unlock Features Or Get Rewards — Semantic Inflation of Reward Value

Operational reading: Evaluate the source formula over directly observed evidence; preserve every Boolean operator, quantifier, comparison, unit, scope, and threshold.

Pseudocode:
- `CONTEXT GATE: identify the rule's specific object/context and mechanism anchor before evaluation.`
- `IF only a generic UI object, generic price, generic CTA, or mere absence is observed: provide no support.`
- `IF a required atom is unavailable: provide no support, preserve the base judgment, and record the evidence as missing.`
- `IF a required atom depends on a UI transition: snapshot the scoped DOM, activate the most direct in-scope control once, wait for rendered settlement, and snapshot again.`
- `IF a visible standard next step is required by the rule: follow one such step and capture the resulting scoped DOM; an unverified click is missing evidence, not a negative.`
- `INPUT evidence_ledger, with state IDs and modality availability.`
- `CHECK the pattern-specific domain/context gate.`
- `IF the core context is contradicted: RETURN NO_MATCH.`
- `PARSE the original formula while preserving every AND, OR, NOT, quantifier, and grouping.`
- `EVALUATE each atomic operand only from directly observed evidence.`
- `EVALUATE implication only as an observed premise-and-consequence proof obligation; never use vacuous truth.`
- `IF the formula result is FALSE: RETURN FALSE.`
- `IF direct_counterevidence is observed: RETURN FALSE.`
- `RETURN TRUE.`

Required observable atoms:
- Every atomic clause in the original formula is identified and evaluated in the stated scope.

Direct counterevidence:
- A directly observed atom contradicts a mandatory requirement.

## DP-048-C1 — Pay To Avoid — Artificial State Degradation

Operational reading: The default state is materially degraded by an artificial penalty

Pseudocode:
- `CONTEXT GATE: identify the rule's specific object/context and mechanism anchor before evaluation.`
- `IF only a generic UI object, generic price, generic CTA, or mere absence is observed: provide no support.`
- `IF a required atom is unavailable: provide no support, preserve the base judgment, and record the evidence as missing.`
- `IF a required atom depends on a UI transition: snapshot the scoped DOM, activate the most direct in-scope control once, wait for rendered settlement, and snapshot again.`
- `IF a visible standard next step is required by the rule: follow one such step and capture the resulting scoped DOM; an unverified click is missing evidence, not a negative.`
- `INPUT evidence_ledger, with state IDs and modality availability.`
- `CHECK the pattern-specific domain/context gate.`
- `IF the core context is contradicted: RETURN NO_MATCH.`
- `PARSE the original formula while preserving every AND, OR, NOT, quantifier, and grouping.`
- `EVALUATE each atomic operand only from directly observed evidence.`
- `EVALUATE implication only as an observed premise-and-consequence proof obligation; never use vacuous truth.`
- `IF the formula result is FALSE: RETURN FALSE.`
- `IF direct_counterevidence is observed: RETURN FALSE.`
- `RETURN TRUE.`

Required observable atoms:
- Default and system/reference utility states are identified for the same feature.
- The artificial degradation term is observed or explicitly represented.
- The stated utility inequality is met.

Direct counterevidence:
- The default state is not materially degraded or the feature is inherently unavailable.

## DP-048-C2 — Pay To Avoid — Visual Occupancy of the Pain-Point Element

Operational reading: Pain-point element occupies more than the stated viewport share

Pseudocode:
- `CONTEXT GATE: identify the rule's specific object/context and mechanism anchor before evaluation.`
- `IF only a generic UI object, generic price, generic CTA, or mere absence is observed: provide no support.`
- `IF a required atom is unavailable: provide no support, preserve the base judgment, and record the evidence as missing.`
- `IF a required atom depends on a UI transition: snapshot the scoped DOM, activate the most direct in-scope control once, wait for rendered settlement, and snapshot again.`
- `IF a visible standard next step is required by the rule: follow one such step and capture the resulting scoped DOM; an unverified click is missing evidence, not a negative.`
- `INPUT evidence_ledger, with state IDs and modality availability.`
- `CHECK the pattern-specific domain/context gate.`
- `IF the core context is contradicted: RETURN NO_MATCH.`
- `PARSE the original formula while preserving every AND, OR, NOT, quantifier, and grouping.`
- `EVALUATE each atomic operand only from directly observed evidence.`
- `EVALUATE implication only as an observed premise-and-consequence proof obligation; never use vacuous truth.`
- `IF the formula result is FALSE: RETURN FALSE.`
- `IF direct_counterevidence is observed: RETURN FALSE.`
- `RETURN TRUE.`

Required observable atoms:
- The pain-point element is identified in the same viewport as its denominator.
- Its area ratio exceeds the stated threshold.

Direct counterevidence:
- The area ratio does not exceed threshold.

## DP-048-C3 — Pay To Avoid — Pain-Point Amplification

Operational reading: Observed friction increases and is followed by a payment prompt

Pseudocode:
- `CONTEXT GATE: identify the rule's specific object/context and mechanism anchor before evaluation.`
- `IF only a generic UI object, generic price, generic CTA, or mere absence is observed: provide no support.`
- `IF a required atom is unavailable: provide no support, preserve the base judgment, and record the evidence as missing.`
- `IF a required atom depends on a UI transition: snapshot the scoped DOM, activate the most direct in-scope control once, wait for rendered settlement, and snapshot again.`
- `IF a visible standard next step is required by the rule: follow one such step and capture the resulting scoped DOM; an unverified click is missing evidence, not a negative.`
- `INPUT evidence_ledger, with state IDs and modality availability.`
- `CHECK the pattern-specific domain/context gate.`
- `IF the core context is contradicted: RETURN NO_MATCH.`
- `PARSE the original formula while preserving every AND, OR, NOT, quantifier, and grouping.`
- `EVALUATE each atomic operand only from directly observed evidence.`
- `EVALUATE implication only as an observed premise-and-consequence proof obligation; never use vacuous truth.`
- `IF the formula result is FALSE: RETURN FALSE.`
- `IF direct_counterevidence is observed: RETURN FALSE.`
- `RETURN TRUE.`

Required observable atoms:
- At least two aligned states expose a measurable increase in friction.
- The payment prompt appears after that increase in the observed path.

Direct counterevidence:
- No increase precedes the prompt or the prompt is unrelated to the feature.

## DP-049-C1 — Automating The User Away — Autonomous Action Execution

Operational reading: Evaluate the source formula over directly observed evidence; preserve every Boolean operator, quantifier, comparison, unit, scope, and threshold.

Pseudocode:
- `CONTEXT GATE: identify the rule's specific object/context and mechanism anchor before evaluation.`
- `IF only a generic UI object, generic price, generic CTA, or mere absence is observed: provide no support.`
- `IF a required atom is unavailable: provide no support, preserve the base judgment, and record the evidence as missing.`
- `IF a required atom depends on a UI transition: snapshot the scoped DOM, activate the most direct in-scope control once, wait for rendered settlement, and snapshot again.`
- `IF a visible standard next step is required by the rule: follow one such step and capture the resulting scoped DOM; an unverified click is missing evidence, not a negative.`
- `INPUT evidence_ledger, with state IDs and modality availability.`
- `CHECK the pattern-specific domain/context gate.`
- `IF the core context is contradicted: RETURN NO_MATCH.`
- `PARSE the original formula while preserving every AND, OR, NOT, quantifier, and grouping.`
- `EVALUATE each atomic operand only from directly observed evidence.`
- `EVALUATE implication only as an observed premise-and-consequence proof obligation; never use vacuous truth.`
- `IF the formula result is FALSE: RETURN FALSE.`
- `IF direct_counterevidence is observed: RETURN FALSE.`
- `RETURN TRUE.`

Required observable atoms:
- Every atomic clause in the original formula is identified and evaluated in the stated scope.

Direct counterevidence:
- A directly observed atom contradicts a mandatory requirement.

## DP-049-C2 — Automating The User Away — Omission of the Interrupt Vector

Operational reading: Evaluate the source formula over directly observed evidence; preserve every Boolean operator, quantifier, comparison, unit, scope, and threshold.

Pseudocode:
- `CONTEXT GATE: identify the rule's specific object/context and mechanism anchor before evaluation.`
- `IF only a generic UI object, generic price, generic CTA, or mere absence is observed: provide no support.`
- `IF a required atom is unavailable: provide no support, preserve the base judgment, and record the evidence as missing.`
- `IF a required atom depends on a UI transition: snapshot the scoped DOM, activate the most direct in-scope control once, wait for rendered settlement, and snapshot again.`
- `IF a visible standard next step is required by the rule: follow one such step and capture the resulting scoped DOM; an unverified click is missing evidence, not a negative.`
- `INPUT evidence_ledger, with state IDs and modality availability.`
- `CHECK the pattern-specific domain/context gate.`
- `IF the core context is contradicted: RETURN NO_MATCH.`
- `PARSE the original formula while preserving every AND, OR, NOT, quantifier, and grouping.`
- `EVALUATE each atomic operand only from directly observed evidence.`
- `EVALUATE implication only as an observed premise-and-consequence proof obligation; never use vacuous truth.`
- `IF the formula result is FALSE: RETURN FALSE.`
- `IF direct_counterevidence is observed: RETURN FALSE.`
- `RETURN TRUE.`

Required observable atoms:
- Every atomic clause in the original formula is identified and evaluated in the stated scope.

Direct counterevidence:
- A directly observed atom contradicts a mandatory requirement.

## DP-049-C3 — Automating The User Away — Semantic Speed of Consent-Timing Language

Operational reading: Evaluate the source formula over directly observed evidence; preserve every Boolean operator, quantifier, comparison, unit, scope, and threshold.

Pseudocode:
- `CONTEXT GATE: identify the rule's specific object/context and mechanism anchor before evaluation.`
- `IF only a generic UI object, generic price, generic CTA, or mere absence is observed: provide no support.`
- `IF a required atom is unavailable: provide no support, preserve the base judgment, and record the evidence as missing.`
- `IF a required atom depends on a UI transition: snapshot the scoped DOM, activate the most direct in-scope control once, wait for rendered settlement, and snapshot again.`
- `IF a visible standard next step is required by the rule: follow one such step and capture the resulting scoped DOM; an unverified click is missing evidence, not a negative.`
- `INPUT evidence_ledger, with state IDs and modality availability.`
- `CHECK the pattern-specific domain/context gate.`
- `IF the core context is contradicted: RETURN NO_MATCH.`
- `PARSE the original formula while preserving every AND, OR, NOT, quantifier, and grouping.`
- `EVALUATE each atomic operand only from directly observed evidence.`
- `EVALUATE implication only as an observed premise-and-consequence proof obligation; never use vacuous truth.`
- `IF the formula result is FALSE: RETURN FALSE.`
- `IF direct_counterevidence is observed: RETURN FALSE.`
- `RETURN TRUE.`

Required observable atoms:
- Every atomic clause in the original formula is identified and evaluated in the stated scope.

Direct counterevidence:
- A directly observed atom contradicts a mandatory requirement.

## DP-055-C1 — Parasocial Pressure — Manufactured Livelihood Dependency

Operational reading: Evaluate the source formula over directly observed evidence; preserve every Boolean operator, quantifier, comparison, unit, scope, and threshold.

Pseudocode:
- `CONTEXT GATE: identify the rule's specific object/context and mechanism anchor before evaluation.`
- `IF only a generic UI object, generic price, generic CTA, or mere absence is observed: provide no support.`
- `IF a required atom is unavailable: provide no support, preserve the base judgment, and record the evidence as missing.`
- `IF a required atom depends on a UI transition: snapshot the scoped DOM, activate the most direct in-scope control once, wait for rendered settlement, and snapshot again.`
- `IF a visible standard next step is required by the rule: follow one such step and capture the resulting scoped DOM; an unverified click is missing evidence, not a negative.`
- `INPUT evidence_ledger, with state IDs and modality availability.`
- `CHECK the pattern-specific domain/context gate.`
- `IF the core context is contradicted: RETURN NO_MATCH.`
- `PARSE the original formula while preserving every AND, OR, NOT, quantifier, and grouping.`
- `EVALUATE each atomic operand only from directly observed evidence.`
- `EVALUATE implication only as an observed premise-and-consequence proof obligation; never use vacuous truth.`
- `IF the formula result is FALSE: RETURN FALSE.`
- `IF direct_counterevidence is observed: RETURN FALSE.`
- `RETURN TRUE.`

Required observable atoms:
- Every atomic clause in the original formula is identified and evaluated in the stated scope.

Direct counterevidence:
- A directly observed atom contradicts a mandatory requirement.

## DP-055-C2 — Parasocial Pressure — Visual Proximity of Anthropomorphic Imagery to Action Prompts

Operational reading: Evaluate the source formula over directly observed evidence; preserve every Boolean operator, quantifier, comparison, unit, scope, and threshold.

Pseudocode:
- `CONTEXT GATE: identify the rule's specific object/context and mechanism anchor before evaluation.`
- `IF only a generic UI object, generic price, generic CTA, or mere absence is observed: provide no support.`
- `IF a required atom is unavailable: provide no support, preserve the base judgment, and record the evidence as missing.`
- `IF a required atom depends on a UI transition: snapshot the scoped DOM, activate the most direct in-scope control once, wait for rendered settlement, and snapshot again.`
- `IF a visible standard next step is required by the rule: follow one such step and capture the resulting scoped DOM; an unverified click is missing evidence, not a negative.`
- `INPUT evidence_ledger, with state IDs and modality availability.`
- `CHECK the pattern-specific domain/context gate.`
- `IF the core context is contradicted: RETURN NO_MATCH.`
- `PARSE the original formula while preserving every AND, OR, NOT, quantifier, and grouping.`
- `EVALUATE each atomic operand only from directly observed evidence.`
- `EVALUATE implication only as an observed premise-and-consequence proof obligation; never use vacuous truth.`
- `IF the formula result is FALSE: RETURN FALSE.`
- `IF direct_counterevidence is observed: RETURN FALSE.`
- `RETURN TRUE.`

Required observable atoms:
- Every atomic clause in the original formula is identified and evaluated in the stated scope.

Direct counterevidence:
- A directly observed atom contradicts a mandatory requirement.

## DP-055-C3 — Parasocial Pressure — Emotional Asymmetry

Operational reading: Evaluate the source formula over directly observed evidence; preserve every Boolean operator, quantifier, comparison, unit, scope, and threshold.

Pseudocode:
- `CONTEXT GATE: identify the rule's specific object/context and mechanism anchor before evaluation.`
- `IF only a generic UI object, generic price, generic CTA, or mere absence is observed: provide no support.`
- `IF a required atom is unavailable: provide no support, preserve the base judgment, and record the evidence as missing.`
- `IF a required atom depends on a UI transition: snapshot the scoped DOM, activate the most direct in-scope control once, wait for rendered settlement, and snapshot again.`
- `IF a visible standard next step is required by the rule: follow one such step and capture the resulting scoped DOM; an unverified click is missing evidence, not a negative.`
- `INPUT evidence_ledger, with state IDs and modality availability.`
- `CHECK the pattern-specific domain/context gate.`
- `IF the core context is contradicted: RETURN NO_MATCH.`
- `PARSE the original formula while preserving every AND, OR, NOT, quantifier, and grouping.`
- `EVALUATE each atomic operand only from directly observed evidence.`
- `EVALUATE implication only as an observed premise-and-consequence proof obligation; never use vacuous truth.`
- `IF the formula result is FALSE: RETURN FALSE.`
- `IF direct_counterevidence is observed: RETURN FALSE.`
- `RETURN TRUE.`

Required observable atoms:
- Every atomic clause in the original formula is identified and evaluated in the stated scope.

Direct counterevidence:
- A directly observed atom contradicts a mandatory requirement.

## DP-060-C1 — Encouraging Anti-Social Behavior — Reward-Coupled Social Externality

Operational reading: Evaluate the source formula over directly observed evidence; preserve every Boolean operator, quantifier, comparison, unit, scope, and threshold.

Pseudocode:
- `CONTEXT GATE: identify the rule's specific object/context and mechanism anchor before evaluation.`
- `IF only a generic UI object, generic price, generic CTA, or mere absence is observed: provide no support.`
- `IF a required atom is unavailable: provide no support, preserve the base judgment, and record the evidence as missing.`
- `IF a required atom depends on a UI transition: snapshot the scoped DOM, activate the most direct in-scope control once, wait for rendered settlement, and snapshot again.`
- `IF a visible standard next step is required by the rule: follow one such step and capture the resulting scoped DOM; an unverified click is missing evidence, not a negative.`
- `INPUT evidence_ledger, with state IDs and modality availability.`
- `CHECK the pattern-specific domain/context gate.`
- `IF the core context is contradicted: RETURN NO_MATCH.`
- `PARSE the original formula while preserving every AND, OR, NOT, quantifier, and grouping.`
- `EVALUATE each atomic operand only from directly observed evidence.`
- `EVALUATE implication only as an observed premise-and-consequence proof obligation; never use vacuous truth.`
- `IF the formula result is FALSE: RETURN FALSE.`
- `IF direct_counterevidence is observed: RETURN FALSE.`
- `RETURN TRUE.`

Required observable atoms:
- Every atomic clause in the original formula is identified and evaluated in the stated scope.

Direct counterevidence:
- A directly observed atom contradicts a mandatory requirement.

## DP-060-C2 — Encouraging Anti-Social Behavior — Visual Framing of Competitive Antagonism

Operational reading: Evaluate the source formula over directly observed evidence; preserve every Boolean operator, quantifier, comparison, unit, scope, and threshold.

Pseudocode:
- `CONTEXT GATE: identify the rule's specific object/context and mechanism anchor before evaluation.`
- `IF only a generic UI object, generic price, generic CTA, or mere absence is observed: provide no support.`
- `IF a required atom is unavailable: provide no support, preserve the base judgment, and record the evidence as missing.`
- `IF a required atom depends on a UI transition: snapshot the scoped DOM, activate the most direct in-scope control once, wait for rendered settlement, and snapshot again.`
- `IF a visible standard next step is required by the rule: follow one such step and capture the resulting scoped DOM; an unverified click is missing evidence, not a negative.`
- `INPUT evidence_ledger, with state IDs and modality availability.`
- `CHECK the pattern-specific domain/context gate.`
- `IF the core context is contradicted: RETURN NO_MATCH.`
- `PARSE the original formula while preserving every AND, OR, NOT, quantifier, and grouping.`
- `EVALUATE each atomic operand only from directly observed evidence.`
- `EVALUATE implication only as an observed premise-and-consequence proof obligation; never use vacuous truth.`
- `IF the formula result is FALSE: RETURN FALSE.`
- `IF direct_counterevidence is observed: RETURN FALSE.`
- `RETURN TRUE.`

Required observable atoms:
- Every atomic clause in the original formula is identified and evaluated in the stated scope.

Direct counterevidence:
- A directly observed atom contradicts a mandatory requirement.

## DP-060-C3 — Encouraging Anti-Social Behavior — Visibility of Hostile Content

Operational reading: Evaluate the source formula over directly observed evidence; preserve every Boolean operator, quantifier, comparison, unit, scope, and threshold.

Pseudocode:
- `CONTEXT GATE: identify the rule's specific object/context and mechanism anchor before evaluation.`
- `IF only a generic UI object, generic price, generic CTA, or mere absence is observed: provide no support.`
- `IF a required atom is unavailable: provide no support, preserve the base judgment, and record the evidence as missing.`
- `IF a required atom depends on a UI transition: snapshot the scoped DOM, activate the most direct in-scope control once, wait for rendered settlement, and snapshot again.`
- `IF a visible standard next step is required by the rule: follow one such step and capture the resulting scoped DOM; an unverified click is missing evidence, not a negative.`
- `INPUT evidence_ledger, with state IDs and modality availability.`
- `CHECK the pattern-specific domain/context gate.`
- `IF the core context is contradicted: RETURN NO_MATCH.`
- `PARSE the original formula while preserving every AND, OR, NOT, quantifier, and grouping.`
- `EVALUATE each atomic operand only from directly observed evidence.`
- `EVALUATE implication only as an observed premise-and-consequence proof obligation; never use vacuous truth.`
- `IF the formula result is FALSE: RETURN FALSE.`
- `IF direct_counterevidence is observed: RETURN FALSE.`
- `RETURN TRUE.`

Required observable atoms:
- Every atomic clause in the original formula is identified and evaluated in the stated scope.

Direct counterevidence:
- A directly observed atom contradicts a mandatory requirement.

## DP-031-C1 — Addictive Design — Infinite Frictionless Continuation

Operational reading: Near-end scrolling triggers observed content append

Pseudocode:
- `CONTEXT GATE: identify the rule's specific object/context and mechanism anchor before evaluation.`
- `IF only a generic UI object, generic price, generic CTA, or mere absence is observed: provide no support.`
- `IF a required atom is unavailable: provide no support, preserve the base judgment, and record the evidence as missing.`
- `IF a required atom depends on a UI transition: snapshot the scoped DOM, activate the most direct in-scope control once, wait for rendered settlement, and snapshot again.`
- `IF a visible standard next step is required by the rule: follow one such step and capture the resulting scoped DOM; an unverified click is missing evidence, not a negative.`
- `INPUT evidence_ledger, with state IDs and modality availability.`
- `CHECK the pattern-specific domain/context gate.`
- `IF the core context is contradicted: RETURN NO_MATCH.`
- `PARSE the original formula while preserving every AND, OR, NOT, quantifier, and grouping.`
- `EVALUATE each atomic operand only from directly observed evidence.`
- `EVALUATE implication only as an observed premise-and-consequence proof obligation; never use vacuous truth.`
- `IF the formula result is FALSE: RETURN FALSE.`
- `IF direct_counterevidence is observed: RETURN FALSE.`
- `RETURN TRUE.`

Required observable atoms:
- The evidence shows the pattern-specific scroll/feed context near a document boundary.
- A fetch/append transition is observed after that near-end state, with repeated continuation or no stable end boundary.

Direct counterevidence:
- A stable end boundary is observed, or the interface is a bounded reward/play loop without scroll/feed continuation.

## DP-031-C2 — Addictive Design — Eradication of Natural Stopping Cues

Operational reading: Stopping cues are absent or below threshold in the inspected state

Pseudocode:
- `CONTEXT GATE: identify the rule's specific object/context and mechanism anchor before evaluation.`
- `IF only a generic UI object, generic price, generic CTA, or mere absence is observed: provide no support.`
- `IF a required atom is unavailable: provide no support, preserve the base judgment, and record the evidence as missing.`
- `IF a required atom depends on a UI transition: snapshot the scoped DOM, activate the most direct in-scope control once, wait for rendered settlement, and snapshot again.`
- `IF a visible standard next step is required by the rule: follow one such step and capture the resulting scoped DOM; an unverified click is missing evidence, not a negative.`
- `INPUT evidence_ledger, with state IDs and modality availability.`
- `CHECK the pattern-specific domain/context gate.`
- `IF the core context is contradicted: RETURN NO_MATCH.`
- `PARSE the original formula while preserving every AND, OR, NOT, quantifier, and grouping.`
- `EVALUATE each atomic operand only from directly observed evidence.`
- `EVALUATE implication only as an observed premise-and-consequence proof obligation; never use vacuous truth.`
- `IF the formula result is FALSE: RETURN FALSE.`
- `IF direct_counterevidence is observed: RETURN FALSE.`
- `RETURN TRUE.`

Required observable atoms:
- The relevant stopping-cue set is defined for the inspected interface.
- Every relevant cue fails visibility/area/contrast in the same state(s).

Direct counterevidence:
- A clear stopping cue is visible at the stated threshold.

## DP-031-C3 — Addictive Design — Semantic Reinforcement-Trigger Lexicon Density

Operational reading: Reinforcement lexemes exceed the stated density in the viewport

Pseudocode:
- `CONTEXT GATE: identify the rule's specific object/context and mechanism anchor before evaluation.`
- `IF only a generic UI object, generic price, generic CTA, or mere absence is observed: provide no support.`
- `IF a required atom is unavailable: provide no support, preserve the base judgment, and record the evidence as missing.`
- `IF a required atom depends on a UI transition: snapshot the scoped DOM, activate the most direct in-scope control once, wait for rendered settlement, and snapshot again.`
- `IF a visible standard next step is required by the rule: follow one such step and capture the resulting scoped DOM; an unverified click is missing evidence, not a negative.`
- `INPUT evidence_ledger, with state IDs and modality availability.`
- `CHECK the pattern-specific domain/context gate.`
- `IF the core context is contradicted: RETURN NO_MATCH.`
- `PARSE the original formula while preserving every AND, OR, NOT, quantifier, and grouping.`
- `EVALUATE each atomic operand only from directly observed evidence.`
- `EVALUATE implication only as an observed premise-and-consequence proof obligation; never use vacuous truth.`
- `IF the formula result is FALSE: RETURN FALSE.`
- `IF direct_counterevidence is observed: RETURN FALSE.`
- `RETURN TRUE.`

Required observable atoms:
- The relevant viewport text and reinforcement lexicon are available.
- The stated normalized density exceeds threshold.

Direct counterevidence:
- The density does not exceed the threshold.

## DP-032-C1 — Infinite Scrolling — Autonomous Content Injection

Operational reading: Evaluate the source formula over directly observed evidence; preserve every Boolean operator, quantifier, comparison, unit, scope, and threshold.

Pseudocode:
- `CONTEXT GATE: identify the rule's specific object/context and mechanism anchor before evaluation.`
- `IF only a generic UI object, generic price, generic CTA, or mere absence is observed: provide no support.`
- `IF a required atom is unavailable: provide no support, preserve the base judgment, and record the evidence as missing.`
- `IF a required atom depends on a UI transition: snapshot the scoped DOM, activate the most direct in-scope control once, wait for rendered settlement, and snapshot again.`
- `IF a visible standard next step is required by the rule: follow one such step and capture the resulting scoped DOM; an unverified click is missing evidence, not a negative.`
- `INPUT evidence_ledger, with state IDs and modality availability.`
- `CHECK the pattern-specific domain/context gate.`
- `IF the core context is contradicted: RETURN NO_MATCH.`
- `PARSE the original formula while preserving every AND, OR, NOT, quantifier, and grouping.`
- `EVALUATE each atomic operand only from directly observed evidence.`
- `EVALUATE implication only as an observed premise-and-consequence proof obligation; never use vacuous truth.`
- `IF the formula result is FALSE: RETURN FALSE.`
- `IF direct_counterevidence is observed: RETURN FALSE.`
- `RETURN TRUE.`

Required observable atoms:
- Every atomic clause in the original formula is identified and evaluated in the stated scope.

Direct counterevidence:
- A directly observed atom contradicts a mandatory requirement.

## DP-032-C2 — Infinite Scrolling — The Unreachable Footer

Operational reading: Evaluate the source formula over directly observed evidence; preserve every Boolean operator, quantifier, comparison, unit, scope, and threshold.

Pseudocode:
- `CONTEXT GATE: identify the rule's specific object/context and mechanism anchor before evaluation.`
- `IF only a generic UI object, generic price, generic CTA, or mere absence is observed: provide no support.`
- `IF a required atom is unavailable: provide no support, preserve the base judgment, and record the evidence as missing.`
- `IF a required atom depends on a UI transition: snapshot the scoped DOM, activate the most direct in-scope control once, wait for rendered settlement, and snapshot again.`
- `IF a visible standard next step is required by the rule: follow one such step and capture the resulting scoped DOM; an unverified click is missing evidence, not a negative.`
- `INPUT evidence_ledger, with state IDs and modality availability.`
- `CHECK the pattern-specific domain/context gate.`
- `IF the core context is contradicted: RETURN NO_MATCH.`
- `PARSE the original formula while preserving every AND, OR, NOT, quantifier, and grouping.`
- `EVALUATE each atomic operand only from directly observed evidence.`
- `EVALUATE implication only as an observed premise-and-consequence proof obligation; never use vacuous truth.`
- `IF the formula result is FALSE: RETURN FALSE.`
- `IF direct_counterevidence is observed: RETURN FALSE.`
- `RETURN TRUE.`

Required observable atoms:
- Every atomic clause in the original formula is identified and evaluated in the stated scope.

Direct counterevidence:
- A directly observed atom contradicts a mandatory requirement.

## DP-032-C3 — Infinite Scrolling — Semantic Attenuation of Content Boundaries

Operational reading: Evaluate the source formula over directly observed evidence; preserve every Boolean operator, quantifier, comparison, unit, scope, and threshold.

Pseudocode:
- `CONTEXT GATE: identify the rule's specific object/context and mechanism anchor before evaluation.`
- `IF only a generic UI object, generic price, generic CTA, or mere absence is observed: provide no support.`
- `IF a required atom is unavailable: provide no support, preserve the base judgment, and record the evidence as missing.`
- `IF a required atom depends on a UI transition: snapshot the scoped DOM, activate the most direct in-scope control once, wait for rendered settlement, and snapshot again.`
- `IF a visible standard next step is required by the rule: follow one such step and capture the resulting scoped DOM; an unverified click is missing evidence, not a negative.`
- `INPUT evidence_ledger, with state IDs and modality availability.`
- `CHECK the pattern-specific domain/context gate.`
- `IF the core context is contradicted: RETURN NO_MATCH.`
- `PARSE the original formula while preserving every AND, OR, NOT, quantifier, and grouping.`
- `EVALUATE each atomic operand only from directly observed evidence.`
- `EVALUATE implication only as an observed premise-and-consequence proof obligation; never use vacuous truth.`
- `IF the formula result is FALSE: RETURN FALSE.`
- `IF direct_counterevidence is observed: RETURN FALSE.`
- `RETURN TRUE.`

Required observable atoms:
- Every atomic clause in the original formula is identified and evaluated in the stated scope.

Direct counterevidence:
- A directly observed atom contradicts a mandatory requirement.

## DP-033-C1 — Pull To Refresh (Variable-Reward Trap) — Kinesthetic Resistance and Action Commitment

Operational reading: Evaluate the source formula over directly observed evidence; preserve every Boolean operator, quantifier, comparison, unit, scope, and threshold.

Pseudocode:
- `CONTEXT GATE: identify the rule's specific object/context and mechanism anchor before evaluation.`
- `IF only a generic UI object, generic price, generic CTA, or mere absence is observed: provide no support.`
- `IF a required atom is unavailable: provide no support, preserve the base judgment, and record the evidence as missing.`
- `IF a required atom depends on a UI transition: snapshot the scoped DOM, activate the most direct in-scope control once, wait for rendered settlement, and snapshot again.`
- `IF a visible standard next step is required by the rule: follow one such step and capture the resulting scoped DOM; an unverified click is missing evidence, not a negative.`
- `INPUT evidence_ledger, with state IDs and modality availability.`
- `CHECK the pattern-specific domain/context gate.`
- `IF the core context is contradicted: RETURN NO_MATCH.`
- `PARSE the original formula while preserving every AND, OR, NOT, quantifier, and grouping.`
- `EVALUATE each atomic operand only from directly observed evidence.`
- `EVALUATE implication only as an observed premise-and-consequence proof obligation; never use vacuous truth.`
- `IF the formula result is FALSE: RETURN FALSE.`
- `IF direct_counterevidence is observed: RETURN FALSE.`
- `RETURN TRUE.`

Required observable atoms:
- Every atomic clause in the original formula is identified and evaluated in the stated scope.

Direct counterevidence:
- A directly observed atom contradicts a mandatory requirement.

## DP-033-C2 — Pull To Refresh (Variable-Reward Trap) — Artificial Anticipation Injection

Operational reading: Evaluate the source formula over directly observed evidence; preserve every Boolean operator, quantifier, comparison, unit, scope, and threshold.

Pseudocode:
- `CONTEXT GATE: identify the rule's specific object/context and mechanism anchor before evaluation.`
- `IF only a generic UI object, generic price, generic CTA, or mere absence is observed: provide no support.`
- `IF a required atom is unavailable: provide no support, preserve the base judgment, and record the evidence as missing.`
- `IF a required atom depends on a UI transition: snapshot the scoped DOM, activate the most direct in-scope control once, wait for rendered settlement, and snapshot again.`
- `IF a visible standard next step is required by the rule: follow one such step and capture the resulting scoped DOM; an unverified click is missing evidence, not a negative.`
- `INPUT evidence_ledger, with state IDs and modality availability.`
- `CHECK the pattern-specific domain/context gate.`
- `IF the core context is contradicted: RETURN NO_MATCH.`
- `PARSE the original formula while preserving every AND, OR, NOT, quantifier, and grouping.`
- `EVALUATE each atomic operand only from directly observed evidence.`
- `EVALUATE implication only as an observed premise-and-consequence proof obligation; never use vacuous truth.`
- `IF the formula result is FALSE: RETURN FALSE.`
- `IF direct_counterevidence is observed: RETURN FALSE.`
- `RETURN TRUE.`

Required observable atoms:
- Every atomic clause in the original formula is identified and evaluated in the stated scope.

Direct counterevidence:
- A directly observed atom contradicts a mandatory requirement.

## DP-033-C3 — Pull To Refresh (Variable-Reward Trap) — Semantic Variability of Refresh-Outcome Messaging

Operational reading: Evaluate the source formula over directly observed evidence; preserve every Boolean operator, quantifier, comparison, unit, scope, and threshold.

Pseudocode:
- `CONTEXT GATE: identify the rule's specific object/context and mechanism anchor before evaluation.`
- `IF only a generic UI object, generic price, generic CTA, or mere absence is observed: provide no support.`
- `IF a required atom is unavailable: provide no support, preserve the base judgment, and record the evidence as missing.`
- `IF a required atom depends on a UI transition: snapshot the scoped DOM, activate the most direct in-scope control once, wait for rendered settlement, and snapshot again.`
- `IF a visible standard next step is required by the rule: follow one such step and capture the resulting scoped DOM; an unverified click is missing evidence, not a negative.`
- `INPUT evidence_ledger, with state IDs and modality availability.`
- `CHECK the pattern-specific domain/context gate.`
- `IF the core context is contradicted: RETURN NO_MATCH.`
- `PARSE the original formula while preserving every AND, OR, NOT, quantifier, and grouping.`
- `EVALUATE each atomic operand only from directly observed evidence.`
- `EVALUATE implication only as an observed premise-and-consequence proof obligation; never use vacuous truth.`
- `IF the formula result is FALSE: RETURN FALSE.`
- `IF direct_counterevidence is observed: RETURN FALSE.`
- `RETURN TRUE.`

Required observable atoms:
- Every atomic clause in the original formula is identified and evaluated in the stated scope.

Direct counterevidence:
- A directly observed atom contradicts a mandatory requirement.

## DP-046-C1 — Countdown On Ads — Temporal Gating of Navigational Agency

Operational reading: Evaluate the source formula over directly observed evidence; preserve every Boolean operator, quantifier, comparison, unit, scope, and threshold.

Pseudocode:
- `CONTEXT GATE: identify the rule's specific object/context and mechanism anchor before evaluation.`
- `IF only a generic UI object, generic price, generic CTA, or mere absence is observed: provide no support.`
- `IF a required atom is unavailable: provide no support, preserve the base judgment, and record the evidence as missing.`
- `IF a required atom depends on a UI transition: snapshot the scoped DOM, activate the most direct in-scope control once, wait for rendered settlement, and snapshot again.`
- `IF a visible standard next step is required by the rule: follow one such step and capture the resulting scoped DOM; an unverified click is missing evidence, not a negative.`
- `INPUT evidence_ledger, with state IDs and modality availability.`
- `CHECK the pattern-specific domain/context gate.`
- `IF the core context is contradicted: RETURN NO_MATCH.`
- `PARSE the original formula while preserving every AND, OR, NOT, quantifier, and grouping.`
- `EVALUATE each atomic operand only from directly observed evidence.`
- `EVALUATE implication only as an observed premise-and-consequence proof obligation; never use vacuous truth.`
- `IF the formula result is FALSE: RETURN FALSE.`
- `IF direct_counterevidence is observed: RETURN FALSE.`
- `RETURN TRUE.`

Required observable atoms:
- Every atomic clause in the original formula is identified and evaluated in the stated scope.

Direct counterevidence:
- A directly observed atom contradicts a mandatory requirement.

## DP-046-C2 — Countdown On Ads — Dynamic Affordance Injection

Operational reading: Evaluate the source formula over directly observed evidence; preserve every Boolean operator, quantifier, comparison, unit, scope, and threshold.

Pseudocode:
- `CONTEXT GATE: identify the rule's specific object/context and mechanism anchor before evaluation.`
- `IF only a generic UI object, generic price, generic CTA, or mere absence is observed: provide no support.`
- `IF a required atom is unavailable: provide no support, preserve the base judgment, and record the evidence as missing.`
- `IF a required atom depends on a UI transition: snapshot the scoped DOM, activate the most direct in-scope control once, wait for rendered settlement, and snapshot again.`
- `IF a visible standard next step is required by the rule: follow one such step and capture the resulting scoped DOM; an unverified click is missing evidence, not a negative.`
- `INPUT evidence_ledger, with state IDs and modality availability.`
- `CHECK the pattern-specific domain/context gate.`
- `IF the core context is contradicted: RETURN NO_MATCH.`
- `PARSE the original formula while preserving every AND, OR, NOT, quantifier, and grouping.`
- `EVALUATE each atomic operand only from directly observed evidence.`
- `EVALUATE implication only as an observed premise-and-consequence proof obligation; never use vacuous truth.`
- `IF the formula result is FALSE: RETURN FALSE.`
- `IF direct_counterevidence is observed: RETURN FALSE.`
- `RETURN TRUE.`

Required observable atoms:
- Every atomic clause in the original formula is identified and evaluated in the stated scope.

Direct counterevidence:
- A directly observed atom contradicts a mandatory requirement.

## DP-046-C3 — Countdown On Ads — Semantic Framing of Ad-Watching as Exchange

Operational reading: Evaluate the source formula over directly observed evidence; preserve every Boolean operator, quantifier, comparison, unit, scope, and threshold.

Pseudocode:
- `CONTEXT GATE: identify the rule's specific object/context and mechanism anchor before evaluation.`
- `IF only a generic UI object, generic price, generic CTA, or mere absence is observed: provide no support.`
- `IF a required atom is unavailable: provide no support, preserve the base judgment, and record the evidence as missing.`
- `IF a required atom depends on a UI transition: snapshot the scoped DOM, activate the most direct in-scope control once, wait for rendered settlement, and snapshot again.`
- `IF a visible standard next step is required by the rule: follow one such step and capture the resulting scoped DOM; an unverified click is missing evidence, not a negative.`
- `INPUT evidence_ledger, with state IDs and modality availability.`
- `CHECK the pattern-specific domain/context gate.`
- `IF the core context is contradicted: RETURN NO_MATCH.`
- `PARSE the original formula while preserving every AND, OR, NOT, quantifier, and grouping.`
- `EVALUATE each atomic operand only from directly observed evidence.`
- `EVALUATE implication only as an observed premise-and-consequence proof obligation; never use vacuous truth.`
- `IF the formula result is FALSE: RETURN FALSE.`
- `IF direct_counterevidence is observed: RETURN FALSE.`
- `RETURN TRUE.`

Required observable atoms:
- Every atomic clause in the original formula is identified and evaluated in the stated scope.

Direct counterevidence:
- A directly observed atom contradicts a mandatory requirement.

## DP-051-C1 — Auto-Play — Autonomous Media Execution

Operational reading: Evaluate the source formula over directly observed evidence; preserve every Boolean operator, quantifier, comparison, unit, scope, and threshold.

Pseudocode:
- `CONTEXT GATE: identify the rule's specific object/context and mechanism anchor before evaluation.`
- `IF only a generic UI object, generic price, generic CTA, or mere absence is observed: provide no support.`
- `IF a required atom is unavailable: provide no support, preserve the base judgment, and record the evidence as missing.`
- `IF a required atom depends on a UI transition: snapshot the scoped DOM, activate the most direct in-scope control once, wait for rendered settlement, and snapshot again.`
- `IF a visible standard next step is required by the rule: follow one such step and capture the resulting scoped DOM; an unverified click is missing evidence, not a negative.`
- `INPUT evidence_ledger, with state IDs and modality availability.`
- `CHECK the pattern-specific domain/context gate.`
- `IF the core context is contradicted: RETURN NO_MATCH.`
- `PARSE the original formula while preserving every AND, OR, NOT, quantifier, and grouping.`
- `EVALUATE each atomic operand only from directly observed evidence.`
- `EVALUATE implication only as an observed premise-and-consequence proof obligation; never use vacuous truth.`
- `IF the formula result is FALSE: RETURN FALSE.`
- `IF direct_counterevidence is observed: RETURN FALSE.`
- `RETURN TRUE.`

Required observable atoms:
- Every atomic clause in the original formula is identified and evaluated in the stated scope.

Direct counterevidence:
- A directly observed atom contradicts a mandatory requirement.

## DP-051-C2 — Auto-Play — Affordance Suppression

Operational reading: Evaluate the source formula over directly observed evidence; preserve every Boolean operator, quantifier, comparison, unit, scope, and threshold.

Pseudocode:
- `CONTEXT GATE: identify the rule's specific object/context and mechanism anchor before evaluation.`
- `IF only a generic UI object, generic price, generic CTA, or mere absence is observed: provide no support.`
- `IF a required atom is unavailable: provide no support, preserve the base judgment, and record the evidence as missing.`
- `IF a required atom depends on a UI transition: snapshot the scoped DOM, activate the most direct in-scope control once, wait for rendered settlement, and snapshot again.`
- `IF a visible standard next step is required by the rule: follow one such step and capture the resulting scoped DOM; an unverified click is missing evidence, not a negative.`
- `INPUT evidence_ledger, with state IDs and modality availability.`
- `CHECK the pattern-specific domain/context gate.`
- `IF the core context is contradicted: RETURN NO_MATCH.`
- `PARSE the original formula while preserving every AND, OR, NOT, quantifier, and grouping.`
- `EVALUATE each atomic operand only from directly observed evidence.`
- `EVALUATE implication only as an observed premise-and-consequence proof obligation; never use vacuous truth.`
- `IF the formula result is FALSE: RETURN FALSE.`
- `IF direct_counterevidence is observed: RETURN FALSE.`
- `RETURN TRUE.`

Required observable atoms:
- Every atomic clause in the original formula is identified and evaluated in the stated scope.

Direct counterevidence:
- A directly observed atom contradicts a mandatory requirement.

## DP-051-C3 — Auto-Play — Semantic Framing of Auto-Play as Content Continuation

Operational reading: Evaluate the source formula over directly observed evidence; preserve every Boolean operator, quantifier, comparison, unit, scope, and threshold.

Pseudocode:
- `CONTEXT GATE: identify the rule's specific object/context and mechanism anchor before evaluation.`
- `IF only a generic UI object, generic price, generic CTA, or mere absence is observed: provide no support.`
- `IF a required atom is unavailable: provide no support, preserve the base judgment, and record the evidence as missing.`
- `IF a required atom depends on a UI transition: snapshot the scoped DOM, activate the most direct in-scope control once, wait for rendered settlement, and snapshot again.`
- `IF a visible standard next step is required by the rule: follow one such step and capture the resulting scoped DOM; an unverified click is missing evidence, not a negative.`
- `INPUT evidence_ledger, with state IDs and modality availability.`
- `CHECK the pattern-specific domain/context gate.`
- `IF the core context is contradicted: RETURN NO_MATCH.`
- `PARSE the original formula while preserving every AND, OR, NOT, quantifier, and grouping.`
- `EVALUATE each atomic operand only from directly observed evidence.`
- `EVALUATE implication only as an observed premise-and-consequence proof obligation; never use vacuous truth.`
- `IF the formula result is FALSE: RETURN FALSE.`
- `IF direct_counterevidence is observed: RETURN FALSE.`
- `RETURN TRUE.`

Required observable atoms:
- Every atomic clause in the original formula is identified and evaluated in the stated scope.

Direct counterevidence:
- A directly observed atom contradicts a mandatory requirement.
